<?xml version="1.0" encoding="utf-8"?>
<!--
    The 34dp disc behind the clock at the head of the pane — scheme.primary at
    14%, flat, exactly as _Header draws it. Flat and not graded on purpose: the
    card grades the 44dp gold disc below it and leaves this one plain, so the
    two do not compete for the same glance.
-->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/prayer_widget_green_disc" />
</shape>
