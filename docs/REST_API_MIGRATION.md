# مواصفات REST API — خطة الاستبدال الكامل لـ Supabase

> **الغرض:** هذا المستند هو المرجع التعاقدي بين تطبيق Flutter وفريق الـ Backend في حال
> الانتقال من Supabase إلى REST API مخصص. تم استخراجه من **الكود الفعلي** للتطبيق
> (كل استدعاء `supabase.from / rpc / storage / functions / auth` موجود في المشروع)
> ومن ملفات الـ migrations (`supabase/migrations/0001 → 0134`).
>
> **تاريخ الإعداد:** 2026-07-31 — فرع `operational-files`.
> **آخر تحديث:** 2026-08-24 — **الموسم رحلةٌ من البيت وإليه** (migrations
> 0129 → 0134، والقسم §32): أهمّ ما يُسأل عن رجل في الموسم — أين هو ومتى يعود —
> لم يكن في المخطط أصلاً؛ كان الطيران والباصات واجباتٍ نصية وملفات PDF معلّقة
> على ملف تشغيلي (0034 و0036)، وذلك سجلٌّ لعمل *متابعة* الرحلات لا سجلٌّ لمن
> عليها. القرار الحاكم أن المِرساة **ليست الموظف** بل مشاركته في الموسم: فموسم
> جديد مسارٌ فارغ **بحكم البناء**، ولا حاجة إلى منطقٍ ينسى سفر السنة الماضية
> لأنه لا يوجد ما يُنسخ. وجدولان لا ثلاثة: `trips` هي المركبة وجدولها ولا تعرف
> من عليها، و`journey_legs` هي هذا الرجل على تلك المركبة — فيصير تأخّرُ رحلةٍ
> فيها ستون راكباً **تعديلاً واحداً** لا ستين، ويصير الإسنادُ الجماعي ممكناً
> لأن الرحلة موجودة قبل أن يُسند إليها أحد. وأهمّ خانةٍ في التصميم كلّه أن
> `trip_id` **قابل للإفراغ**: النظام لا يسأل «أين حجز القطار؟» بل «كيف انتقل هذا
> الرجل من مكة إلى المدينة؟»، و«بسيارة خاصة» جوابٌ كامل يُرسم على المسار
> كالطيران تماماً بأيقونة سيارة — **والغياب لا يكون أحمر أبداً**، فالأحمر
> لـ`missed` و`cancelled` وحدهما: لشيءٍ وقع وساء، لا لشيءٍ لم يُكتب. وثلاث
> حالاتٍ لا تكتب إحداها الأخرى (المركبة، الرجل، موقعه): هبوطُ الطائرة لا يقول
> شيئاً عمّن كان عليها، فالتطبيق **يعرض** الاقتراح ويبقى الضغط بشرياً — وتسجيل
> الوصول (§30) يقترح كذلك ولا يكتب. والحالة الحالية **غير مخزّنة**، للحجّة التي
> ساقتها 0064 عن مجموع المجموعات: عمودٌ لها مكانٌ ثانٍ يمكن أن يخطئ. و0133 تكتب
> ما يعمل به المكتب فعلاً — القدوم والعودة طيرانٌ بين مطارٍ سوري وآخر سعودي،
> والتنقّل الداخلي قطارٌ أو سيارة أو غيرهما **لا طيران** وبين مكة والمدينة —
> مرّةً في الواجهة كي لا يُعرض ما لا يجوز، ومرّةً في trigger كي لا يُكتب من
> طريقٍ آخر، و`test/travel_rules_test.dart` يشدّ النسختين إلى بعضهما. وتنبيهُ
> العودة قبل 48 ثم 24 ساعة، لأن حجزاً يُقطع قبل يومين ورجلاً في منى لا سبب لديه
> ليفتح التطبيق.
>
> وقبله 2026-08-23 — **الصلاحيات تُسأل بالصلاحية لا بالشخص** (بلا
> migration — الوصلات هي هي): صفحة الإدارة ← الصلاحيات كانت قائمة موظفين، كلٌّ
> يفتح ورقة مفاتيح؛ وذلك جوابُ سؤال «ماذا يحقّ لهذا الرجل» — وله اليوم بابٌ
> أفضل: بطاقة الصلاحيات على صفحة الموظف نفسه. ما بقي للصفحة هو سؤال المكتب
> الآخر: «هذه الخمس، أعطِها لهؤلاء الثلاثة» — ثلاثةُ مُدخلي بيانات كانوا ثلاث
> أوراق تُقلَّب المفاتيح نفسها فيها، وصاروا سلّةً واحدة وضغطةً واحدة. تُختار
> السلّة أوّلاً ثم يُختار الناس، على **صفحة انتقاء الموظفين نفسها** التي تمرّ
> بها كل إسنادات التطبيق (§9.10) لا على قائمةٍ خاصة بهذه الشاشة، والسلّة مغلقةٌ
> تحت المتطلبات المسبقة في الطرفين — فما لا يقبله الخادم لا يمكن تركيبه هنا
> أصلاً. §4.1 و§4.5.
> وقبله 2026-08-20 — **السجلّ رفُّ مواضيع لا كومةُ صفوف** (migrations
> 0125 ثم 0127): `assign_evaluations` تكتب الجداء الديكارتي منذ 0084 — وبحقّ،
> فثلاثة قضاة على أحد عشر ملفاً ثلاثةٌ وثلاثون حكماً مستقلاً لا حكمٌ واحد
> بقوائم بداخله. لكن السجلّ كان يقرأ تلك الصفوف بطاقةً بطاقة، فمكتبٌ طلب تقييم
> تسعين موظفاً يمرّر تسعين بطاقة متطابقة الشكل قبل أن يسأل كم منها اكتمل.
> **0125 جمعتها بالدفعة، و0127 سحبت ذلك**: الدفعة حقيقةٌ عن المكتب — متى ضُغط
> «إرسال» — ولا أحد يفتح السجلّ ليسأل عنها. السؤال المطروح فعلاً عن
> **الموضوع**: هذا الملف طُرح للتقييم وكُلّف به عشرون، فإلى أين وصل ذلك؟
> فملفّان تحت التقييم بطاقتان، سواءٌ أُسندا بفعلٍ واحد أو بخمسة. سقط عمود
> `batch_id`، وصار التجميع بـ(النموذج، الجهة) في `evaluation_subjects_list`:
> العدد والمتأخّر، والمتوسّط والأعلى والأدنى — **نِسَباً لا علاماتٍ خاماً**،
> لأن ورقةً نصفَ معبّأة مجموعُها رقمٌ لا يصحّ اقتباسه، ولأن نموذجاً أُعيد
> ترقيمه يضع 38/50 و38/80 في عمودٍ واحد — ومعها **كلُّ مُكلَّف ومعرّفُ ورقته
> داخل البطاقة نفسها**، فالاسم هو الباب ولا شاشة وسيطة بينهما. ومعه انتقل
> **سجلّ التقييمات إلى «الإشراف والسجلات»**: كان يُفتح من داخل إدارة التقييم
> وحدها، وذلك صحيحٌ لأوراق نموذجٍ بعينه — وبابُها ما زال على كل بطاقة نموذج —
> وغيرُ صحيحٍ للسجلّ نفسه، فهو كل ورقة على كل نموذج ويُقرأ كما تُقرأ اللوحة.
> §26.
> وقبله 2026-08-12 — **ما لا يراه المراقِب ولا اللوحة** (migrations
> 0122–0123): سؤالٌ واحد — «هل يضمّان كل شيء داخل التطبيق؟» — وكان الجواب لا،
> في كليهما، ولسببين مختلفين تماماً.
> **السجلّ:** بنيتُه سليمة والخلل في صيانته. 0077 تركت قاعدةً مكتوبة — جدولٌ
> يُنشأ بعدها يبدأ **غير مسجَّل** ما لم تعلّق هجرتُه الـ trigger — وأربع هجرات
> لم تقرأها. أخطرها 0098: **`place_check_ins`**، أي سجلّ الحضور نفسه، يُعدَّل
> ويُحذف منه بلا أثر لمن فعل، وهو آخر ما ينبغي أن يكون قابلاً للتصحيح بهدوء.
> ومعه `report_misses` (0086) و`module_type_teams` (0115). وطبقةٌ ثانية فوق
> ذلك: جداول **تُسجَّل** في القاعدة ولا يعرف فلتر الشاشة أسماءها، فتظهر صفوفها
> بلا تسمية ولا سبيل إلى فلترتها — **قسم التقييمات كلّه** منذ 0084، وأربعة
> جداول من خيط المهمة (0117/0118)، ورموز الأماكن. أُغلق الاثنان، ويحرسهما الآن
> `audit_coverage_test.dart` الذي يقرأ الـ migrations نفسها بدل أن يُنتظر ممّن
> يكتب جدولاً جديداً أن يتذكّر. §23.
> **واللوحة:** `dashboard_stats` آخر كتابة لها **0085**، والتطبيق بلغ 0122 —
> فالبلاغات العاجلة والحضور والمهام المُسندة والتقييمات والشكاوى، خمسة أقسام
> عمل، بلا بطاقة واحدة. وهذه شاشة تُقرأ **بدلاً** من قراءة كل شيء، فمن لا يجد
> فيها بطاقةً عن الطوارئ يخلص إلى أنه لا شيء يُعرف عنها؛ ولوحةٌ تُسقط نصف
> التطبيق وهي تبدو كاملة أسوأ من لوحةٍ لم تُبنَ. أُضيفت الخمسة بصلاحية كلٍّ منها
> — تغيب عمّن لا يملكها ولا تُصفَّر — وثلاثةٌ منها **لا يمكن تقييدها بموسم**
> (جداولها بلا `season_id`، وبحقّ)، فتُحسب على نافذة ثلاثين يوماً و**تقول ذلك
> على البطاقة**: رقمٌ تحت مُنتقي المواسم يُقرأ رقمَ ذلك الموسم. §18.3.
> وقبله 2026-08-12 — **البلاغ يُشطَب، والغرفة تنتبه** (migration
> 0121): طرفا شكوى واحدة على 0088. **الطرف الأول:** الجدول وُلد بسياسات إدراج
> وقراءة وتحديث وبلا سياسة حذف إطلاقاً — وفي جدول عليه RLS يعني ذلك أن **لا
> أحد** يحذف، ولا الأدمن. مقبولٌ والسجلّ فتيّ، غير مقبول في الأسبوع الثاني من
> الموسم حين تصير القائمة التي تُقرأ الثالثة فجراً تسعة أعشارها رسائل تجربة
> ومكرَّرات رجلٍ ضغط «أرسل» أربع مرّات على شبكة رديئة. **سجلٌّ لا يُفرَّغ سجلٌّ
> يُهجَر.** فصلاحية ثالثة `incidents.delete` — لا تُطوى في `incidents.handle`،
> لأن الغلق يقول «ذهب أحد» والحذف يقول «لم يحدث هذا»، وليس الثاني الأولَ بثقةٍ
> أكبر — و`delete_incident` و`delete_all_incidents(p_only_closed)` تُرجعان
> **مسارات التخزين** التي أيتمها التسلسل ليمسحها العميل من الدلو. ومعها
> **الـ trigger الذي نسيته 0088**: الجدول غير مسجَّل في `audit_log` منذ يوم
> أُنشئ، وهي فجوة تحتملها قائمةٌ تنمو فقط ولا تحتملها قائمة صار لها حذف — «من
> حذف بلاغ الحافلة» سؤال يجب أن يكون له جواب. §30.7 و§23.
> **والطرف الثاني:** 0088 يوصل البلاغ إشعاراً — سطراً في الصندوق، وسطراً في
> شريط الهاتف على قناة `alarm`. صحيحٌ لهاتفٍ في جيب، وخاطئٌ تماماً للحالة التي
> بُنيت الميزة لها: **غرفة العمليات، حيث التطبيق مفتوحٌ على شاشة لا ينظر إليها
> أحد**. لافتةٌ تنزلق فوق قائمة ثم تنزلق خارجةً هي، في تلك الغرفة، طوارئ لم يرها
> أحد. فصار البلاغ **يُوقف التطبيق**: `AlertDialog` لا يمرّ منه شيء (لا نقرة على
> الحاجب ولا إيماءة رجوع) ونغمة **على مجرى المنبّه** — الذي لا يُسكته الوضع
> الصامت ولا «المنبّهات فقط» في وضع عدم الإزعاج — تدور حتى يضغط أحدٌ الزرّ.
> ومعها زرّ إغلاق صريح، لأن الموقظ في الثالثة فجراً قد يكون على بُعد أربع ساعات
> والشخصَ الخطأ تماماً، ونافذةٌ لا تُغلق يُسكتها بقتل التطبيق — فلا يصله التالي.
> **لا يخصّ الخادم منه شيء**: مصدراه هما تدفّق §20 نفسه ودَفعة §17، وكلاهما
> قائم. §30.8.
> وقبله 2026-08-12 — **المهمة مبادلة لا قيمة** (migrations
> 0117–0119): 0105 نمذجها قيمةً يكتبها طرف، فخرج منها ثلاثة أعطال بنيوية —
> مُسنِدٌ بلا فعلٍ واحد بعد الإسناد (يضغط المُسنَد إليه «منجزة» فتنتهي القصة
> بكلمة الطرف الوحيد صاحب المصلحة في إنهائها)، وحقل `note` واحد يستبدله
> `set_personal_task_state` في كل نداء (فسبب تأخير الأمس يُمحى مع «تم» اليوم)،
> وقرارٌ واحد يتشظّى إلى ستة صفوف لا يربطها شيء (فلا أحد يستطيع سؤال «كم
> أنجزها من الستة؟»). فالآن: **سبع حالات** ومصفوفة انتقالات مُلزِمة مصدرها
> `personal_task_transition_allowed` وحدها — و«منجزة» على المُسنَد **كلمة
> المُسنِد** بعد `submitted`؛ و**جدول تعليقات وجدول أحداث** يُقرآن خيطاً واحداً
> (وحركةٌ حملت جملةً سطرٌ واحد لا سطران)، و`note` يبقى **مشتقاً** لا مصدراً؛
> و**الدفعة** أبٌ للصفوف يُكتب معها في transaction واحدة. ومعها `seq` المنطوق
> («م-١٤٢» — نصف تنسيق البعثة على اللاسلكي)، وأولوية ثلاثية، وخطوات، وإعادة
> إسناد تحفظ الخيط، وعشرة إشعارات بعد أن كان واحداً، وتصعيد بدرجتين على آلة
> §29. **وخللان قائمان أُغلقا:** ترتيبٌ وعد به تعليقُ 0105 ولم تطلبه الاستعلامة
> قط (صار `personal_task_bucket` على الخادم)، ورقابةٌ تسمح بها سياسة 0105 ولم
> يسألها استعلامٌ قط (صارت `tasks.view_all`). **والحذف أُغلق على المُسنَد**:
> يُلغى ويُبلَّغ صاحبه، لا يختفي من قائمته ليلاً. §25.2 و§25.5.
> وقبله 2026-08-07 — **سجلّي أنا** (لا migration ولا صلاحية): §30.3
> منح كلَّ شخص قراءة تسجيلاته منذ 0098، ولم يكن للتطبيق مكانٌ يصرف فيه ذلك —
> الشاشة الوحيدة التي تقرأ الجدول كانت لوحة الغرفة خلف `checkin.board`. شاشة
> «سجلّ حضوري» بفلترة (اليوم/الأسبوع/الكل، ومكان)، تقرأ الجدول مباشرة لا عبر
> `presence_board` لأن اللوحة تطوي التاريخ إلى صفٍّ لكل مكان — والسجلّ هو السؤال
> الآخر: أين كنتُ **كل مرّة**. §30.6a.
> وقبله 2026-08-07 — **اللوحة تتعلّم أن تقول من ينقص** (migration
> 0110): `presence_board` يجيب «أين هو»، والغرفة تقضي أيامها على النصف الآخر —
> المنصب المشغول على الورق والفارغ في الأرض. ولوحة حضور تُقرأ بالبحث عن اسم
> وعدم إيجاده: يعمل مع أحد عشر اسماً، ويصير إحصاءً مع أربعمئة. `presence_gaps`
> يقلبها: المناصب على أماكن (`is_place`) بلا تسجيل خلال النافذة، الأسوأ أولاً،
> **بالهواتف** لأن كل صفّ اسمٌ يوشك أحدهم أن يتصل به. بلا صلاحية جديدة، وفي
> اللوحة نفسها لا شاشة سادسة. §30.6b.
> وقبله 2026-08-07 — **النصف الثاني من العمل بلا شبكة** (لا تغيير على
> الخادم): الطابور (§27) حلّ **الكتابة** منذ 2026-08-04، والقراءة لم تُحلّ قط —
> وهي المطلوبة أكثر. الواقف في منى بلا إشارة لا يفتح دفتر هواتف الكادر ولا قائمة
> مهامه، لا لأن الجواب مجهول بل لأنه كان على الشاشة قبل أربعين دقيقة ولم يحفظه
> شيء. `Snapshots`: آخر جواب صحيح على القرص، بالصفّ الخام وبنفس المُحلِّل، يُعرض
> **على فشل الشبكة وحده** — لا على رفضٍ ولا صلاحيةٍ سُحبت. مفاتيحه بهوية صاحبها
> وتُمسح عند الخروج (الهاتف مشترك، والقراءة تسريب لا قِدَم)، والشاشة تقول **متى**
> كان الجواب صحيحاً لا أنه محفوظ فحسب. ملحق ب.
> وقبله 2026-08-07 — **الباب في مكانه** (لا migration ولا تغيير على
> الخادم): البلاغ العاجل بُني على قاعدة «تقريرٌ استغرق ثلاثين ثانية لتأليفه لم
> يُرسَل قط» — ثم كان يُفتح من مكان واحد: بلاطة بين خمسٍ وعشرين في الرئيسية.
> صار في **شريط التطبيق على 49 شاشة**، في الموضع نفسه على كلٍّ منها، ولا يُرسم
> بلا جلسة معتمدة (فـ`raise_incident` يرفض غير المعتمد أصلاً — والرفض بعد كتابة
> الطوارئ أسوأ من زرٍّ غائب). الرئيسية وحدها بلا زرّ في الشريط، لأن فيها زرّاً
> أحمر عريضاً مقروناً بتسجيل الوصول. **ومراسلة واتساب** بجانب الاتصال حيث يُعرض
> رقم: كادر البعثة يحمل أرقاماً سورية ويقف في السعودية، والرسالة تمرّ على شبكة
> لا تحتمل مكالمة. لا شيء من هذا يخصّ الخادم.
> وقبله 2026-08-07 — **السجل ينسى، والكشوف تُقرأ أقلّ** (migration
> 0109): `audit_log` ينمو منذ 0077 بلا سقف ولا سياسة — كنسٌ شهري بـ pg_cron،
> نافذته ٨٠٠ يوم ولتسجيلات الدخول ١١٦٥، على دفعات، ويكتب سطره في السجل نفسه.
> §23.6. ومعه **تخزينٌ مؤقّت للبيانات المرجعية** في العميل (لا أثر على الخادم):
> `fetchReferenceSets` تُستدعى من تسعة مواضع وتحمل كل مدخل من كل قائمة —
> وحملُها الكامل **مقصود** (قاعدة 0040: المنتقي يعرض مداخل الموسم، والحالّ يعرف
> الكل)، فالعلاج تقليل **التكرار** لا الحجم: خمس دقائق مع إبطالٍ صريح عند كل
> كتابة. و**نقرة الإشعار تمرّ بالرئيسية** قبل الوارد، فالرجوع يعود إلى التطبيق
> لا يخرج منه.
> وقبله 2026-08-07 — **الغرفة تُبلَّغ فعلاً** (migration 0107): عشرة
> migrations تكتب في `notifications` ولم يكن أيٌّ منها يوقظ هاتفاً — الشيء
> الوحيد الذي بلغ FCM يوماً هو دالة Dart تُستدعى يدوياً من شاشة الإرسال. فبلاغ
> 0088 العاجل ينتظر أن يفتح أحدهم التطبيق، وتصعيد 0086 **صامتٌ بنيوياً** لأنه
> يمرّ ليلاً. trigger على `notifications` **لكل statement** يدفع دفعةً واحدة لكل
> `group_id` — فيصل كل كاتب هاتفاً بمجرد كتابة صفّ. الـ topic باقٍ (يُشتقّ من
> `data->>'type'`)، ودفع العميل أُزيل (كاتبان = رنّتان)، والبلاغ العاجل صار
> **قناة مستقلة** لا يُسكتها من أسكت التعاميم. §17.3 أُعيد كتابته.
> وقبله 2026-08-07 — **العميل لا يحمل مفتاحاً يمنح** (لا migration):
> `SUPABASE_SERVICE_ROLE_KEY` كان في `.env` بقيمة حقيقية، و`.env` **محزوم كأصل
> Flutter** في pubspec — أي أنه يُنسخ داخل كل APK وكل بناء ويب، ومفتاح
> `service_role` يتجاوز RLS كلها. لم يقرأه سطر Dart واحد قط: تسريبٌ خالص بلا
> مقابل. نُقل إلى `.env.seed`، ودُوِّر، و`test/env_secrets_test.dart` يمنع
> عودته — **باشتقاق المسموح مما يقرؤه التطبيق فعلاً** لا بقائمة تُصان يدوياً.
> §1.6 الجديدة تجعله ثابتاً مُلزِماً على الـ backend البديل أيضاً.
> وقبله 2026-08-06 — **كشف الإسناد واحد** (migration 0106): إسناد
> المهمة يمرّ على `assignable_employees` — كشف البعثة نفسه الذي تُملأ به
> المناصب، بفلاتره وبحثه وبالملفات التي يخدم فيها كل مرشَّح هذا الموسم —
> وحارسه يتوسّع بمنحة واحدة: `tasks.assign`. والمهمة الواحدة تُسنَد **لعدة
> أشخاص بقرار واحد** (صفٌّ لكل واحد، في statement واحد)، والمُسنِد مُسقَط من
> الكشف. §9.10 و§25.2.
> وقبله 2026-08-06 — **المهام لي، والملف يصف عمله ولا يتعقبه**
> (migration 0105، تنقض معظم 0083): لا كتالوج مهام — `module_type_tasks`
> و`module_type_task_groups` و`module_assigned_tasks` وعمود `tasks_are_assigned`
> **حُذفت كلها** (ومعها `PUT /members/{id}/tasks` §11.5 القديمة). مهام الملف
> التشغيلي صارت **وصفية**: نطاقان (`file`/`role`)، بلا حالة ولا مرفقات —
> جدولا الحالة والأدلة والدالتان `module_task_board`/`set_module_task_state`
> أُسقطت. والمتابعة انتقلت إلى **نظام مهام شخصي مستقل لا علاقة له بالملفات**:
> `personal_tasks` — ما أكتبه لنفسي لي كامل الصلاحية عليه؛ وما يُسنده إليّ
> حامل `tasks.assign` (قسم صلاحيات جديد) لا أملك عليه سوى تغيير الحالة، مع
> إشعار عند الإسناد، ودلو تخزين ثامن `tasks`، وترحيلٍ ينقل المهام الشخصية
> القديمة من الملفات إلى قوائم أصحابها. و**شاشتان منفصلتان**: «مهامي» بلا
> حراسة، و«إسناد المهام» خلف الصلاحية. §25 أُعيدت كتابته كاملاً.
> وقبله 2026-08-06 — **أعمدة الجدول تعرف أنواعها** (migration 0104):
> عمود جدول القرار كائنٌ بهويّة `{id,label,kind,set,span}` وسبعة أنواع — نصّ،
> رقم، تاريخ، وقت، مدة زمنية، مرجعي، قائمة — والصفّ يُدخَل بأداة كل عمود: منتقي
> تاريخ، منتقي وقت، قائمة مداخل الموسم للمرجعي. **وسبقه إصلاح ثلاثة أعطاب حيّة**:
> محرّر الكتلة كان يكتب في الخليّة الخطأ على أي جدول فيه توسيع (حقل «المجموع»
> يكتب فوق عدّاد أول تكتّل)، وتعديل أي عنوان كان يرمي أعمدة التكتّلات الثلاثة
> عشر، والصفّ الجديد كان يولد بأربع خلايا لجدولٍ من سبع عشرة. مدى الوقت يُخزَّن
> قياسياً `HH:mm-HH:mm` — كان جملةً بلغة الواجهة وقت الإدخال — والقارئ يصوغ
> الجملة ويحلّ المعرّفات المرجعية أسماءً. §15.
> وقبله 2026-08-06 — **قرار أم تعميم** (migration 0102): `reports.kind`
> عمود جديد بنوع `decision_kind`. القسم يحمل الاثنين منذ 0067 بلا ما يفرّق
> بينهما — والقرار **يُلزم** أحداً، والتعميم **يُعلم** الجميع. لكل **صفّ** لا لكل
> نوع: النوع يقول ما **شكل** المستند، والشكل نفسه يحمل الفعلين. و`save_report`
> أُسقطت وأُعيد إنشاؤها لأن `create or replace` لا تغيّر قائمة المعاملات.
> وحُذف حقلا «عنوان فرعي» و«ملاحظات» — مكانٌ ثانٍ لكتابة ما مكانه المحتوى.
> §15.
> وقبله 2026-08-06 — **التصدير والخريطة صارا منحاً** (migration 0100):
> `export.data` **صلاحية عليا** تُوسِّع سياسات الصفوف نفسها، فمن يحملها يُخرج ما
> لا يستطيع فتحه على شاشة — والتوسيع في الواجهة وحدها كان سيُنتج ملفاً فارغاً
> بصمت. سياجُ الشكاوى والتقييمات (لا يقرأ المدير قضيّته هو) **بقي قائماً**،
> بالتوسيع من داخل الدالّة المساعدة لا حولها. و`map.view` يحرس الخريطة. §28.2.
> وقبله 2026-08-06 — **تسجيل الوصول على الأماكن** (migrations
> 0098/0099): الحضور صار حقيقةً عن **مكان** لا عن ملف تشغيلي، بعد أن صار المكان
> مدخلاً مرجعياً في 0095. الرمز على صفحة الفندق، ويحمل **سرّاً قابلاً للتجديد**،
> والتسجيل رمزٌ **مع** موقعٍ قريب ولا شيء غيرهما. **وهذا ينقض قاعدة 0087
> صراحةً** — «يُوسم ولا يُرفض» — لأن الخريطة صارت ترسم دبابيس من هذا السجلّ.
> لوحة حضور مركزية واحدة، و`node_check_ins` يُحذف. §30.
> وقبله 2026-08-04 — **التفقّد الميداني** (0087/0094)، الذي حلّ محلّه ما فوقه.
> وقبله 2026-08-04 — **التصعيد التلقائي** (migration 0086): أول شيء في
> هذا النظام يراقب **ما لم يحدث**. مرور يومي بـ pg_cron يجد من استُحقّ عليه تقرير
> دوري ولم يرفعه، فيبلّغه، ثم يبلّغ من فوقه في اليوم التالي عبر سلّم مأخوذ من
> مستويات الملف نفسها. جدول `report_misses`، وأربع دوال، ومهمة مجدولة — §29،
> **وفيه أن `module_report_period` صارت المصدر الوحيد لتعريف الفترة**.
> وقبله 2026-08-04 — **تصدير البيانات**: شاشة واحدة في الإعدادات تُخرج
> تسع مجموعات (الموظفون، المشاركون، الملفات، أعضاؤها، مهامها، البيانات المرجعية،
> التقارير، الشكاوى، التقييمات) كـ CSV أو PDF، بأعمدة يختارها المُصدِّر. **لا
> endpoints جديدة** — يقرأ عبر ما هو موثّق أصلاً — والالتزام الوحيد على الخادم أن
> يبقى تضييق `scope=all` وحجب اسم المشتكي عاملَين، §28.
> وقبله 2026-08-04 — **العمل دون اتصال**: طابور إرسال على الجهاز
> يحفظ الكتابتين الميدانيتين (حالة المهمة، تقرير الملف) مع مرفقاتهما حين تسقط
> الشبكة، ويرسلهما حين تعود. **لا جداول ولا endpoints جديدة**، لكن الشرط الذي
> يفرضه على الخادم جوهري: كل عملية ميدانية يجب أن تحتمل إعادة تنفيذها كاملةً —
> §27، وفيه ثغرة تكرار مرفقات معروفة يُنصح بإغلاقها بقيد تفرّد. ومعه حدود حجم
> المرفقات (§19.2.1) — 25MB على العميل، و`413` بكود واضح من الخادم.
> وقبله 2026-08-04 — نظام التقييم (migration 0084): نموذج تكتبه
> الإدارة بمراحل وأسئلة وعلامات، وتقييم واحد يُفتح باسم مُقيِّم واحد عن جهة
> واحدة. ستة جداول جديدة (`evaluation_templates`, `evaluation_stages`,
> `evaluation_questions`, `evaluation_options`, `evaluations`,
> `evaluation_answers`)، قسم صلاحيات `evaluations` بأربعة أكواد **ليس فيها
> كودٌ للتعبئة** — التقييم يصل بالاسم لا بالصلاحية — وسرّية بنيوية تمنع
> المُقيَّم من معرفة من قيَّمه، وتجميد نصّ السؤال وعلامته داخل كل إجابة حتى لا
> يتغيّر معنى تقييمٍ معتمَد بتعديل نموذجه. §26، والتزامات الخادم في §26.10.
> **لا علاقة لهذا بتقييم النجوم داخل الملفات التشغيلية (0059، §13) — ذاك يبقى
> كما هو ولم يُمسّ.** وقبلها 2026-08-03 — إعادة تصميم نظام المهام (migration 0083): المهمة
> لم تعد مرتبطة بالمستخدم افتراضياً، بل بأحد ثلاثة نطاقات — الملف، الدور،
> الشخص — وصار لها **حالة** لم تكن موجودة قط. ثلاثة جداول جديدة
> (`module_tasks`, `module_task_status`, `module_task_attachments`)، وحالة
> **لكل موقع** لمهام الدور، وصلاحية `modules.tasks`، و RPCs
> (`module_task_board`, `set_module_task_state`)، وتوسعة سياسات دلو `modules`.
> §25، والتزامات الخادم في §25.8. وقبلها 2026-08-02 — إضافة الشكاوى (migrations 0079 و 0080): ثلاثة
> جداول، دلو تخزين سادس، قسم صلاحيات `complaints` بخمسة أكواد، وقاعدة تُوقف
> حساب موظف تلقائياً عند بلوغ ثلاثة مشتكين مختلفين — مع سرّية بنيوية تمنع
> المشتكى عليه من معرفة من اشتكى. §24، والتزامات الخادم في §24.9.
> وقبلها 2026-08-01 — إضافة سجل الأحداث (migration 0077): جدول
> `audit_log` مع trigger عام على كل الجداول، صلاحية `audit.view`، ثلاث RPCs
> (`audit_events`, `audit_actors`, `log_auth_event`)، وأسطر توثيق ذاتية في
> الـ Edge Functions الأربع — §23. وقبلها إصلاحات الجولة الشاملة: حفظ التقرير
> صار RPC واحدة (`save_report`, migration 0074)، سقف لقائمتي الوارد (100)
> والتقارير (200) مع إسقاط `data` من قائمة التقارير، تحقق الخادم من صيغة
> الـ topic ووجود الملف، واشتقاق `gregorian_label` من امتداد السنة الهجرية.

---

## جدول المحتويات

1. [الاصطلاحات العامة](#1-الاصطلاحات-العامة)
2. [المصادقة Authentication](#2-المصادقة-authentication)
3. [الملف الشخصي والتسجيل](#3-الملف-الشخصي-والتسجيل)
4. [الصلاحيات Permissions](#4-الصلاحيات-permissions)
5. [الموظفون Employees](#5-الموظفون-employees)
6. [الموافقات Approvals](#6-الموافقات-approvals)
7. [المواسم Seasons](#7-المواسم-seasons)
8. [أنواع الملفات التشغيلية Module Types](#8-أنواع-الملفات-التشغيلية-module-types)
9. [الملفات التشغيلية Modules](#9-الملفات-التشغيلية-modules)
10. [شجرة الملف (القطاعات والأبراج) Nodes](#10-شجرة-الملف-nodes)
11. [أعضاء الملف Members](#11-أعضاء-الملف)
12. [تقارير الملف التشغيلي Module Reports](#12-تقارير-الملف-التشغيلي)
13. [التقييمات Ratings](#13-التقييمات-ratings)
14. [البيانات المرجعية Master Data](#14-البيانات-المرجعية-master-data)
15. [القرارات Decisions (الجداول باسم `reports`)](#15-القرارات-decisions-الجداول-باسم-reports)
16. [الإشعارات Notifications](#16-الإشعارات-notifications)
17. [أجهزة الدفع Push / Device Tokens](#17-أجهزة-الدفع-push--device-tokens)
18. [لوحة المعلومات Dashboard](#18-لوحة-المعلومات-dashboard)
19. [الملفات والتخزين Storage](#19-الملفات-والتخزين-storage)
20. [البث اللحظي Realtime](#20-البث-اللحظي-realtime)
21. [منطق الخادم الإلزامي (بديل RLS / Triggers)](#21-منطق-الخادم-الإلزامي)
22. [قاموس الأخطاء](#22-قاموس-الأخطاء)
23. [سجل الأحداث Audit Log](#23-سجل-الأحداث-audit-log)
24. [الشكاوى Complaints](#24-الشكاوى-complaints)
25. [المهام Tasks — قوائم وصفية على الملف، ونظام شخصي مستقل](#25-المهام-tasks--قوائم-وصفية-على-الملف-ونظام-شخصي-مستقل)
26. [التقييم Evaluations — النموذج والتكليف](#26-التقييم-evaluations--النموذج-والتكليف)
27. [طابور الإرسال Outbox — التزامات الخادم تجاه إعادة الإرسال](#27-طابور-الإرسال-outbox--التزامات-الخادم-تجاه-إعادة-الإرسال)
28. [تصدير البيانات Export — لا endpoints، ولكن التزام واحد](#28-تصدير-البيانات-export--لا-endpoints-ولكن-التزام-واحد)
29. [التصعيد التلقائي — ملاحظة ما لم يحدث](#29-التصعيد-التلقائي--ملاحظة-ما-لم-يحدث-migration-0086)
30. [البلاغ العاجل Incidents](#30-البلاغ-العاجل-incidents-migration-0088)
31. [خريطة الموسم Season map](#31-خريطة-الموسم-season-map-migrations-0090--0093)
30. [تسجيل الوصول Check-in](#30-تسجيل-الوصول-check-in-migrations-0098--0099-تلغيان-00870094)
32. [السفر والتنقّل Travel](#32-السفر-والتنقّل-travel-migrations-0129--0134)

---

## 1. الاصطلاحات العامة

### 1.1 الأساسيات

| البند              | القيمة                                                                                                                                             |
| ------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- |
| Base URL           | `https://api.example.com/v1` (يحدده الـ backend)                                                                                                   |
| صيغة التبادل       | JSON — `Content-Type: application/json; charset=utf-8`                                                                                             |
| رفع الملفات        | `multipart/form-data`                                                                                                                              |
| المصادقة           | `Authorization: Bearer <access_token>` (JWT) على **كل** المسارات ما عدا التسجيل والدخول                                                            |
| التواريخ الزمنية   | ISO-8601 UTC — مثال `2026-07-31T09:15:00Z`                                                                                                         |
| التواريخ اليومية   | `YYYY-MM-DD` (حقول مثل `starts_on`, `date_of_birth`)                                                                                               |
| المعرّفات          | UUID v4 نصي                                                                                                                                        |
| الترقيم Pagination | `?limit=<int>&offset=<int>` حيث تُذكر                                                                                                              |
| اللغة              | كل الكيانات المعرّبة تحمل حقلين: `name_ar` (إلزامي) و `name_en` (اختياري nullable) — **الاختيار بين اللغتين يتم في التطبيق، فلا يُرسل header لغة** |

### 1.2 الصيغة الموحّدة للخطأ

```json
{
  "error": {
    "code": "forbidden",
    "message": "You do not hold modules.edit"
  }
}
```

| HTTP | متى                                                                  |
| ---- | -------------------------------------------------------------------- |
| 400  | جسم طلب ناقص/غير صالح، أو خرق قاعدة عمل (مثل حذف عنصر مرجعي مستخدَم) |
| 401  | لا توكن / توكن منتهٍ                                                 |
| 403  | مصادَق لكن لا يملك الصلاحية، أو الحساب موقوف/غير معتمد               |
| 404  | المورد غير موجود **أو غير مرئي للطالب** (لا نفرّق بينهما أمنياً)     |
| 409  | تعارض فريد (unique constraint)                                       |
| 422  | فشل تحقق حقول                                                        |

### 1.3 نموذج الصلاحيات (أساس كل شيء)

كل حساب إما `is_admin = true` (يملك كل شيء) أو يحمل مجموعة أكواد صلاحيات.
**القاعدة المعتمدة في التطبيق:** `can(code) = is_admin || permissions.contains(code)`.

كتالوج الأكواد الكامل (مطابق لجدول `permissions` — migration 0073):

| القسم             | الأكواد                                                                                                                                                     |
| ----------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------- |
| الموظفون          | `employees.view` `employees.create` `employees.edit` `employees.delete` `employees.suspend` `employees.external` `employees.documents` `employees.password` `employees.email` |
| الموافقات         | `approvals.view` `approvals.decide`                                                                                                                         |
| المواسم           | `seasons.view` `seasons.switch` `seasons.participants_view` `seasons.participants_manage`                                                                   |
| الملفات التشغيلية | `modules.view_all` `modules.create` `modules.edit` `modules.delete` `modules.activate` `modules.members` `modules.reports`                                  |
| البيانات المرجعية | `reference.view` `reference.edit` `reference.delete` `reference.import`                                                                                     |
| التقارير          | `reports.view_all` `reports.create` `reports.edit` `reports.delete` `reports.publish`                                                                       |
| الإشعارات         | `notifications.send` `notifications.broadcast_module` `notifications.broadcast_all`                                                                         |
| الصلاحيات         | `permissions.view` `permissions.manage`                                                                                                                     |
| سجل الأحداث       | `audit.view`                                                                                                                                                |
| الشكاوى           | `complaints.view` `complaints.reply` `complaints.lock` `complaints.dismiss` `complaints.delete`                                                             |
| السفر والتنقّل     | `travel.view` `travel.view_all` `travel.edit` `travel.assign` `travel.confirm` `travel.delete`                                                               |

**تقديم شكوى ليس له كود، عمداً**: أي حساب معتمد يقدّم. سجلٌّ لما يقع خطأً لا
يكتبه إلا بعضُ الناس ليس سجلاً لما يقع خطأً (§24).

توجد **متطلبات مسبقة** بين الصلاحيات (جدول `permission_prerequisites`): لا يجوز منح
صلاحية دون أساسها (مثال: `modules.members` تتطلب `employees.view`)، وسحب الأساس
يسحب توابعه تلقائياً (§21.4). ومنها واحدة تستحق الانتباه:
`complaints.dismiss` تتطلب `employees.suspend`، لأن رفض شكوى قد يرفع إيقافاً
آلياً — فمن يرفض يمسك مفتاح الإيقاف سواء قالت ورقة المنح ذلك أم لا.

### 1.4 حالات الحساب

`account_status`: `incomplete` → `pending` → `approved` | `rejected`، مع علم مستقل
`is_suspended`. **الحساب الفعّال = approved && !is_suspended** — وهو الشرط الأدنى
لأي مسار غير مسارات المصادقة/التسجيل.

### 1.5 الكيان `Profile` (الاستجابة القياسية للموظف)

يُعاد بهذا الشكل في كل مكان يُذكر فيه "Profile object":

```json
{
  "id": "uuid",
  "email": "user@example.com",
  "first_name": "أحمد",
  "father_name": "محمد",
  "surname": "الخطيب",
  "photo_url": "https://cdn.example.com/avatars/uuid/photo.jpg",
  "job_title_id": "uuid",
  "job_title": { "name_ar": "طبيب", "name_en": "Physician" },
  "gender": "male",
  "mission_type_id": "uuid",
  "mission_type": { "name_ar": "البعثة الطبية", "name_en": "Medical mission" },
  "date_of_birth": "1980-05-14",
  "city_id": "uuid",
  "city": { "name_ar": "دمشق", "name_en": "Damascus" },
  "phone_sy": "+963...",
  "phone_sa": "+966...",
  "is_admin": false,
  "is_external": false,
  "external_organization": null,
  "external_title": null,
  "is_suspended": false,
  "account_status": "approved",
  "rejection_reason": null,
  "passport_image_url": "documents/uuid/passport.jpg",
  "visa_image_url": null,
  "nusuk_card_image_url": null,
  "created_at": "2026-01-01T00:00:00Z",
  "updated_at": "2026-01-01T00:00:00Z"
}
```

> ملاحظة: `photo_url` رابط عام كامل (bucket avatars عام)، بينما حقول الوثائق
> (`passport_image_url`…) هي **مسارات تخزين** تُفتح عبر روابط موقّعة (§19).

---

### 1.6 ما يحمله العميل من مفاتيح — ثابتٌ لا يُفاوَض عليه

التطبيق يقرأ إعداداته من `.env`، وهذا الملف **محزوم كأصل Flutter** (`assets:` في
pubspec). أي أنه يُنسخ بحرفه داخل الـ APK وداخل بناء الويب، ويُقرأ بأمر `unzip`
واحد. فليست فيه سرّية على الإطلاق، مهما بدا اسمه ملفَّ إعدادات.

فالقاعدة، وهي تسبق أي تصميم لـ endpoints:

> **العميل يحمل ما يُعرِّف، ولا يحمل ما يمنح أبداً.**

| يجوز أن يُشحن | لأنه |
| --- | --- |
| `SUPABASE_URL` / عنوان الـ API | عنوان، والعناوين تُعرف |
| المفتاح العام (`anon` / publishable) | لا يفتح شيئاً وحده — **الخادم** هو من يقرّر ما يراه الطلب |
| `GOOGLE_WEB_CLIENT_ID` | يعرّف التطبيق لـ Google؛ نصفه السرّي لا يغادر خوادمها |

وكل ما عداه — `service_role`، كلمة سرّ قاعدة بيانات، مفتاح FCM للخادم، أي رمز
إداري — مكانه **خارج التطبيق**: في `.env.seed` (متجاهَل ولا يُحزَم) للتشغيل
المحلي، أو في مخزن أسرار الخادم (`supabase secrets set`، أو ما يكافئه في الـ
backend البديل).

**حدث هذا فعلاً، ولذلك كُتب البند.** `SUPABASE_SERVICE_ROLE_KEY` جلس في `.env`
بقيمة حقيقية، مشحوناً في كل بناء، وهو يتجاوز RLS بالكامل: كل وثيقة خاصة (§6.4)،
وهوية كل مشتكٍ خلف سياج §24.9، ومن قيَّم من في §26.9، وكل سرّ مكان في
`place_codes` (§30.3) — من فكّ ضغطٍ واحد. ولم يكن أيُّ سطر في التطبيق يقرؤه.

**والالتزام على الـ backend البديل هو نفسه**، ويُقال صراحةً لأن استبدال Supabase
لا يُلغي الفخّ بل يعيد رسمه: كل عملية تحتاج امتيازاً — إنشاء حساب (§5.1)، تعيين
كلمة سرّ (§5.7)، تغيير بريد (§5.7b)، حذف حساب (§5.6)، إرسال دفعة (§17) — تكون
**endpoint يحرسه الخادم بجلسة الطالب وصلاحيته**، لا مفتاحاً يُسلَّم للتطبيق
ليستعمله. هذا بالضبط ما تفعله الـ Edge Functions الخمس اليوم: تقرأ المفتاح من
أسرارها هي، وتتحقق من هوية المُستدعي، ثم تتصرّف نيابةً عنه.

`test/env_secrets_test.dart` يحرس الجهة العميلة من هذا آلياً، ولا يفعله بقائمة
أسماء ممنوعة — بل يمسح `lib/` ليرى أي مفتاح **يقرؤه التطبيق فعلاً**، ثم يرفض في
`.env` كل ما عداه. فالمفتاح الذي لا يقرؤه أحد لا يُشحن، أياً كان اسمه، وبلا
حاجة إلى أن يعرف الاختبارُ الاسمَ الجديد مسبقاً.

---

## 2. المصادقة Authentication

Supabase Auth هو مزوّد الهوية الحالي. البديل يجب أن يوفّر: JWT قصير العمر +
Refresh Token **متغيّر عند كل تجديد** (rotation) — التطبيق يعتمد على ذلك حرفياً
في ميزة "تبديل الحسابات" حيث يخزّن refresh token لكل حساب في keystore الجهاز.

> الجلسة **الأساسية** أيضاً تُخزَّن في keystore الجهاز (`SecureSessionStorage`)
> لا في SharedPreferences، والنسخ الاحتياطي لأندرويد معطَّل (`allowBackup=false`)
> — أي عميل بديل يجب أن يحافظ على المستوى نفسه.

### 2.1 إنشاء حساب

```
POST /auth/register
```

**Request:**

```json
{ "email": "user@example.com", "password": "secret123" }
```

**Response 201:**

```json
{
  "user": { "id": "uuid", "email": "user@example.com" },
  "access_token": "jwt",
  "refresh_token": "opaque",
  "expires_in": 3600
}
```

**Response 200 حين يكون تأكيد البريد مفعّلاً** — لا جلسة، والرمز في البريد:

```json
{ "user": { "id": "uuid", "email": "user@example.com" }, "session": null }
```

**العميل يفرّق بينهما بوجود الجلسة، لا بإعداد يُبنى فيه.** الطريقان مدعومان
كلاهما: تشغيل التأكيد أو إطفاؤه في المشروع يجب ألّا يستلزم إصداراً جديداً من
التطبيق.

**قواعد إلزامية على الخادم:**

- إنشاء صف `profile` تلقائياً بحالة `incomplete` (يقوم به حالياً trigger `handle_new_user`).
- تطبيع البريد وقصّ الفراغات.
- أخطاء: `409 email_exists`، `422 weak_password`.

### 2.1a تأكيد البريد برمز — لا برابط

**رمزٌ رقميّ يُدخَل في التطبيق، لا رابطٌ يُضغط في المتصفّح.** والفرق ليس
ذوقاً: رابط يصل هاتفاً يُفتح في المتصفّح الذي يختاره الهاتف — لا في التطبيق —
فيُؤكَّد الحساب في مكانٍ لا يراه صاحبه، ويبقى هو أمام شاشةٍ لم تتحرّك. ستّة أرقام
يحملها بيده تعبر هذه الفجوة بلا deep links على منصّتين ونسخة ويب.

```
POST /auth/verify-otp     { "email": "...", "token": "123456", "type": "signup" }
POST /auth/resend         { "email": "...", "type": "signup" }
```

`verify-otp` **يؤكّد الحساب ويفتح الجلسة في خطوة واحدة** — استجابته هي نفسها
استجابة 2.1 الكاملة. لا يُطلب من المستخدم أن يسجّل الدخول بعدها؛ من يفعل ذلك
يكون قد جعل إنشاء الحساب ثلاث خطوات لا خطوتين.

**إعداد المشروع (Supabase الحالي — ليس في الكود):**

1. Authentication → Providers → Email → **Confirm email = ON**.
2. Authentication → Email Templates → **Confirm signup**: يجب أن يحوي القالب
   `{{ .Token }}`. تُرك على `{{ .ConfirmationURL }}` الافتراضي، تصل الرسالة
   برابطٍ ولا رمز فيها — ولا شيء يكتبه القارئ في الحقل.

**التزامات على أي backend بديل:**

- **طول الرمز إعدادُ مشروع** (`Email OTP Length` — ستّة افتراضاً، ويُضبط من ستّة
  إلى عشرة)، ولا شيء في الـ API يُخبر العميل به. فهو رقمٌ مكتوبٌ في مكانين يجب
  أن يتطابقا — `kEmailCodeLength` في العميل والإعداد في المشروع — وإن اختلفا
  فالفشل صامتٌ وتامّ: الحقل يرفض الرمز الذي بيد القارئ ولا رسالة تقول لماذا.
- **للرمز عمر محدود** (`OTP_EXPIRY`، ساعة افتراضاً). المنتهي
  والخاطئ يعودان برمز واحد — `otp_expired` — لأن خطوة القارئ التالية واحدة في
  الحالتين، ولأن التفريق بينهما يقول لمن يخمّن أيَّ الرمزين كان موجوداً.
- **حدٌّ أدنى بين رسالتين** إلى العنوان نفسه (دقيقة في Supabase). العميل يعطّل
  زرّ الإرسال ستّين ثانية ليطابقه: زرٌّ فعّالٌ يرفضه الخادم يُقرأ كعطل لا كقاعدة.
- **`400 email_not_confirmed` على تسجيل الدخول** — لا يجوز طيّه في
  `invalid_credentials`. هذا هو الفارق الذي يمنع الفخّ الموصوف تحته.

#### الفخّ: حسابٌ أُنشئ ولم يُؤكَّد

من يُنشئ حساباً ثم يغلق التطبيق قبل إدخال الرمز يملك حساباً: كلمة سرّه صحيحة،
ودخوله مرفوض، **وعنوانه صار مأخوذاً فلا يستطيع إنشاءه من جديد**. هذا حسابٌ لا
يُوصَل إليه أبداً، وهو ما يستحدثه هذا المسار إن لم يُعالَج.

فالعلاج جزء من العقد لا تحسينٌ فوقه: تسجيل دخولٍ يعود بـ`email_not_confirmed`
**يفتح صندوق الرمز ويرسل رمزاً جديداً**، لا أن يقول «بيانات غير صحيحة» لرجلٍ
كتب بياناته صحيحة.

### 2.2 الدخول بالبريد وكلمة السر

```
POST /auth/login
```

**Request:** `{ "email": "...", "password": "..." }`
**Response 200:** نفس شكل 2.1.
**أخطاء:** `400 invalid_credentials`، و`400 email_not_confirmed` — **منفصلاً عن
الأول وجوباً**: الثاني ليس كلمة سرّ خاطئة بل إنشاء حساب توقّف في منتصفه، وجوابه
صندوق الرمز (§2.1a) لا رسالة اعتذار.

### 2.3 الدخول عبر Google

التطبيق يحصل على **Google ID Token** محلياً (google_sign_in) ثم يسلّمه للخادم.

```
POST /auth/google
```

**Request:** `{ "id_token": "<google id_token (JWT)>" }`

**متطلبات الخادم:** التحقق من التوقيع، ومن أن `aud` يساوي
`GOOGLE_WEB_CLIENT_ID` المتفق عليه، ثم إيجاد/إنشاء المستخدم بالبريد الوارد
(مع صف profile كما في 2.1 إن كان جديداً).

**Response 200:** نفس شكل 2.1.

### 2.4 تجديد الجلسة (حرِج — عليه تُبنى ميزة تبديل الحسابات)

```
POST /auth/refresh
```

**Request:** `{ "refresh_token": "opaque" }`

**Response 200:** نفس شكل 2.1 مع **refresh_token جديد** (القديم يُبطل بعد
نافذة سماح قصيرة لإعادة الاستخدام).

**ملاحظات سلوكية يعتمدها التطبيق:**

- هذا المسار نفسه هو ما يستخدمه "التبديل إلى حساب محفوظ": التطبيق يرسل
  refresh token الحساب الآخر ويستبدل الجلسة كاملة، **دون** تسجيل خروج الحساب الحالي.
- refresh token المرفوض (مُلغى أو منتهٍ) ⇒ `401 invalid_refresh_token`،
  وعندها يحذف التطبيق الحساب من قائمة المحفوظين.

### 2.5 تسجيل الخروج

```
POST /auth/logout
```

**Request:** `{ "scope": "global" | "local" }`

- `global`: إبطال كل جلسات المستخدم وكل refresh tokens.
- `local`: إبطال الجلسة الحالية فقط.

**Response:** `204`.

### 2.6 تغيير كلمة سر المستخدم نفسه

```
PATCH /auth/password
```

**Request:** `{ "new_password": "..." }` — **Response:** `204`.
**أخطاء:** `422 weak_password`.

### 2.7 تغيير البريد الإلكتروني للمستخدم نفسه

في التطبيق الحالي يمر عبر Edge Function `admin-set-email` نفسها (المستخدم يمرر
معرّفه هو): **لا تُرسل رسائل تأكيد** لأي من العنوانين — البعثة تسلّم الحسابات
يداً بيد، والعنوان يتغيّر فوراً ويبقى تسجيل الدخول قائماً.

```
PATCH /auth/email
```

**Request:** `{ "email": "new@example.com" }` — **Response:** `200 { "email" }`.

**قواعد إلزامية:**

- متاح لكل حساب **مقبول وغير موقوف** (لا يحتاج أي صلاحية).
- `400 email_invalid` — صيغة غير صالحة.
- `400 email_taken` — البريد مستخدم من حساب آخر (فرادة `auth.users`).
- البريد يُكتب مؤكَّداً، وعمود `profiles.email` مرآة تتبعه عبر trigger
  (migration 0026).

---

## 3. الملف الشخصي والتسجيل

### 3.1 ملفي الشخصي

```
GET /me/profile
```

**Response 200:** Profile object (§1.5).
**Response 404:** الحساب موجود لكن الصف غير جاهز بعد — التطبيق يعامله كـ `incomplete`.

### 3.2 صلاحياتي

بديل RPC `my_permissions`. يُستدعى بعد كل تحميل جلسة لحساب approved غير أدمن.

```
GET /me/permissions
```

**Response 200:**

```json
{ "permissions": ["modules.view_all", "reports.create"] }
```

### 3.3 تعديل ملفي (حساب معتمد)

```
PUT /me/profile
```

**Request:** (حقول الصور تُرسل **فقط** إذا تغيّرت — إرسالها null يعني "لا تغيير"،
لأن التطبيق لا يرسلها أصلاً إن لم تتبدل):

```json
{
  "first_name": "أحمد",
  "father_name": "محمد",
  "surname": "الخطيب",
  "job_title_id": "uuid",
  "gender": "male",
  "date_of_birth": "1980-05-14",
  "mission_type_id": "uuid",
  "phone_sy": "+963...",
  "phone_sa": null,
  "city_id": "uuid",
  "photo_url": "https://...",
  "passport_image_url": "documents/uuid/passport.jpg",
  "visa_image_url": null,
  "nusuk_card_image_url": null
}
```

**Response 200:** Profile object محدّث.
**حماية إلزامية:** تجاهل/رفض أي محاولة لتعديل الأعمدة المحمية
`is_admin`, `is_external`, `account_status`, `is_suspended`, `email` (§21.2).

### 3.4 إرسال الملف للاعتماد (التسجيل الأولي)

نفس حمولة 3.3 لكن `photo_url` **إلزامي**، والخادم يضبط
`account_status = "pending"` ضمن نفس العملية.

```
POST /me/profile/submit
```

**Response 200:** Profile object وحالته `pending`.

### 3.5 القوائم المتاحة قبل الاعتماد

الحسابات غير المعتمدة تحتاجها لتعبئة نموذج التسجيل — **يكفي فيها توكن صالح
دون شرط approved**:

```
GET /reference/syrian-cities
```

**Response:** `[{ "id": "uuid", "name_ar": "دمشق", "name_en": "Damascus" }]`
(المفعّلة فقط، مرتبة بـ `sort_order`).

```
GET /reference/job-titles
GET /reference/mission-types
```

**Response:** `[{ "id": "uuid", "name_ar": "طبيب", "name_en": "Physician" }]`
(المفعّلة فقط، مرتبة بـ `sort_order`).

> الوصف الوظيفي ونوع البعثة قائمتان مرجعيتان كاملتان (`job_titles`,
> `mission_types`) يديرهما المشرف من §14 كبقية القوائم — لا جدول مستقل ولا
> enum. الثلاث المتاحة قبل الاعتماد هي `syrian_cities`, `job_titles`,
> `mission_types`.

---

## 4. الصلاحيات Permissions

**صلاحية الوصول:** `permissions.view` للقراءة، `permissions.manage` للمنح/السحب.

### 4.1 الموظفون القابلون للمنح

```
GET /permissions/employees
```

حسابات `approved` وغير أدمن (الأدمن يملك كل شيء). **Response:** `[Profile]` مرتبة بالاسم الأول.

> **لم يعد العميل يناديها.** شاشة الصلاحيات تُسأل بالصلاحية أولاً (رأس الوثيقة):
> السلّة ثم الناس، والناس يُختارون على `GET /modules/assignable-employees`
> (§9.10) — الصفحة التي تُختار عليها كل إسنادات التطبيق، بالبحث والمرشِّحات وما
> يحمله كلُّ مرشَّح أصلاً. وسؤال «ماذا يحمل هذا الشخص» صار على صفحة الموظف
> نفسها. الوصلة تبقى في العقد لأنها الجواب الصحيح لسؤالها، ولأن أي backend
> بديل قد يبني عليها شاشةً أخرى — لكن لا شاشة في هذا التطبيق تناديها اليوم.
>
> **وتبعةُ ذلك تُكتب هنا لا تُكتشَف:** بوّابة `assignable_employees` هي
> `is_admin() or modules.members or tasks.assign` (0106)، وليس فيها
> `permissions.manage`. فمن يحمل منح الصلاحيات وحدَه — غيرَ أدمن ولا مديرِ
> ملفات ولا مُسنِدَ مهام — يفتح صفحةَ انتقاءٍ فارغة. الحالة نادرة عملياً
> (المانح أدمن في كل تنصيبات هذا المشروع حتى الآن) ولم تُعالَج بـ migration
> بعد؛ علاجُها سطرٌ واحد يُضاف إلى تلك البوّابة.

### 4.2 كتالوج الصلاحيات

```
GET /permissions/catalog
```

**Response:**

```json
[
  {
    "id": "uuid",
    "code": "employees.view",
    "section": "employees",
    "name_ar": "عرض الموظفين",
    "name_en": "View employees",
    "sort_order": 10
  }
]
```

### 4.3 المتطلبات المسبقة

```
GET /permissions/prerequisites
```

**Response:**

```json
{ "prerequisites": [{ "permission_id": "uuid", "requires_id": "uuid" }] }
```

### 4.4 صلاحيات مستخدم محدد

```
GET /users/{userId}/permissions
```

**Response:** `{ "permission_ids": ["uuid", "uuid"] }`

### 4.5 منح

```
POST /users/{userId}/permissions
```

**Request:** `{ "permission_id": "uuid" }` — **Response:** `201`.
الخادم يسجّل `granted_by` من التوكن، ويرفض `400 missing_prerequisite`
إذا كان أساس الصلاحية غير ممنوح (§21.4).

**ومنحٌ بالجملة على الوصلة نفسها:** `{ "permission_ids": ["uuid", ...] }` —
سلّةٌ كاملة لشخصٍ واحد في ذهابٍ وإياب واحد، وهو ما تكتب به شاشة §4.1 سلّةً على
عدّة أشخاص: نداءٌ لكل شخص، لا نداءٌ لكل صلاحية.
**وترتيب المصفوفة جزءٌ من العقد**: مُطلِق المتطلبات المسبقة يعمل صفّاً صفّاً
بترتيب الإدراج، فيجب أن يسبق كلُّ أساسٍ ما يقوم عليه، وإلا رُفض السطر بـ
`missing_prerequisite` بسبب أساسٍ كان سيصل بعده بسطرين. العميل يُرتّب
topologically قبل الإرسال، وأي backend بديل إمّا أن يحفظ الترتيب أو أن يُرتّب
بنفسه — والثاني أسلم.
**والمكرَّر لا شيء لا خطأ:** ما يحمله الشخص أصلاً يُطرح من السلّة قبل الإرسال
(قراءةٌ طازجة لـ §4.4 لكل شخص، لا اعتمادٌ على ما حمّلته الشاشة)، لأن إعادة المنح
مخالفةُ مفتاحٍ فريد لا لا-عمل. فالضغطُ ثانيةً بعد فشلٍ جزئي يُكمل الناقص ولا
يُضاعف الواصل.

### 4.6 سحب

```
DELETE /users/{userId}/permissions/{permissionId}
```

**Response:** `204`. السحب **يتسلسل** على التوابع تلقائياً (§21.4).

---

## 5. الموظفون Employees

### 5.1 إنشاء حساب موظف (بديل Edge Function `admin-create-user`)

**الصلاحية:** أدمن فقط (فحص فعلي في الدالة الحالية: approved + غير موقوف + is_admin).

```
POST /admin/employees
```

**Request:**

```json
{
  "email": "new@example.com",
  "password": "secret123",
  "first_name": "أحمد",
  "father_name": "محمد",
  "surname": "الخطيب",
  "job_title_id": "uuid",
  "gender": "male",
  "mission_type_id": "uuid",
  "phone_sy": "+963...",
  "phone_sa": null,
  "date_of_birth": "1980-05-14",
  "is_external": false,
  "external_organization": null,
  "external_title": null
}
```

**Response 201:** `{ "id": "uuid" }`

**سلوك إلزامي:** إنشاء مستخدم Auth بالبريد **مؤكَّداً مسبقاً**، ثم تعبئة الـ profile
وضبط `account_status = "approved"` مباشرة. حقلا external يُحفظان فقط إذا
`is_external = true`.
**أخطاء:** `401 unauthorized`، `403 forbidden`، `400` مع رسالة (بريد مكرر…).

### 5.2 الكادر الدائم

```
GET /employees/permanent
```

بديل view `permanent_employees`: **داخلي (غير external) + approved**.
**Response:** `[Profile]` مرتبة بالاسم الأول.

### 5.3 المشاركون الخارجيون

```
GET /employees/external?season_id={uuid}
```

- بدون `season_id`: كل approved الذين `is_external = true`.
- مع `season_id`: الخارجيون **المشاركون الفعليون** في ذلك الموسم
  (عبر `season_participants` بحالة `active`).

**Response:** `[Profile]`.

### 5.4 موظف واحد

```
GET /employees/{profileId}
```

**Response:** Profile object. يُستدعى لإعادة القراءة بعد تعديل (لأن التعديل قد **يمسح** حقلاً).

### 5.5 تعديل بيانات موظف

**الصلاحية:** `employees.edit`.

```
PATCH /employees/{profileId}
```

**Request (كل الحقول تُرسل دائماً؛ null تعني مسح القيمة):**

```json
{
  "first_name": "أحمد",
  "father_name": "محمد",
  "surname": "الخطيب",
  "job_title_id": "uuid | null",
  "gender": "male | female | null",
  "mission_type_id": "uuid | null",
  "date_of_birth": "1980-05-14 | null",
  "city_id": "uuid | null",
  "phone_sy": "+963... | null",
  "phone_sa": null
}
```

**Response:** `204`. لا يمس الأعمدة المحمية إطلاقاً.

### 5.6 حذف حساب (بديل Edge Function `admin-delete-user`)

**الصلاحية:** `employees.delete` (الأدمن يحملها ضمنياً).

```
DELETE /admin/employees/{profileId}
```

**Response 200:** `{ "id": "uuid" }`

**قواعد رفض إلزامية (كما في الدالة الحالية):**

- `400 cannot_delete_self` — حذف النفس.
- `400 cannot_delete_admin` — حذف أدمن (يُنزع عنه الأدمن أولاً).
- `404 not_found` — معرف غير موجود.
- الحذف يشمل **مستخدم Auth** فيتسلسل على: profile، عضويات الملفات، مشاركات
  المواسم، الإشعارات، أجهزة الدفع.

### 5.7 تعيين كلمة سر لموظف (بديل Edge Function `admin-set-password`)

**الصلاحية:** `employees.password`، مع رفض تغيير كلمة سر **أدمن** لغير الأدمن.

```
PUT /admin/employees/{profileId}/password
```

**Request:** `{ "password": "newSecret" }` — **Response:** `204`.

### 5.7b تغيير البريد الإلكتروني لموظف (بديل Edge Function `admin-set-email`)

**الصلاحية:** `employees.email`، مع رفض تغيير بريد **أدمن** لغير الأدمن.
(الدالة نفسها تسمح لأي مستخدم مقبول بتغيير بريده **هو** دون صلاحية — §2.7.)

```
PUT /admin/employees/{profileId}/email
```

**Request:** `{ "email": "new@example.com" }` — **Response:** `200 { "id", "email" }`.

**قواعد رفض إلزامية (كما في الدالة الحالية):**

- `400 email_invalid` — صيغة بريد غير صالحة.
- `400 email_taken` — البريد مستخدم من حساب آخر (فرادة `auth.users`).
- `403 cannot_set_admin_email` — بريد أدمن لغير الأدمن.
- `404 not_found` — معرف غير موجود.
- البريد يُكتب **مؤكَّداً** (لا تُرسل رسالة تحقق)، وعمود `profiles.email` مرآة
  تتبعه تلقائياً عبر trigger (migration 0026).

### 5.8 إيقاف / إعادة تفعيل

**الصلاحية:** `employees.suspend`.

```
PATCH /employees/{profileId}/suspension
```

**Request:** `{ "is_suspended": true }` — **Response:** `204`.

### 5.9 التصنيف الخارجي

**الصلاحية:** `employees.external`.

```
PATCH /employees/{profileId}/external-status
```

**Request:**

```json
{
  "is_external": true,
  "organization": "وزارة الخارجية",
  "external_title": "مندوب"
}
```

عند `is_external = false` يمسح الخادم الحقلين الآخرين. **Response:** `204`.

---

## 6. الموافقات Approvals

**الصلاحية:** `approvals.view` للقائمة، `approvals.decide` للقرار.

### 6.1 قائمة الانتظار

```
GET /approvals/pending
```

حسابات `pending` مرتبة بـ `updated_at` تنازلياً. **Response:** `[Profile]`.

### 6.2 اعتماد

```
POST /approvals/{profileId}/approve
```

يضبط `account_status = approved` ويمسح `rejection_reason`. **Response:** `204`.

### 6.3 رفض

```
POST /approvals/{profileId}/reject
```

**Request:** `{ "reason": "الصورة غير واضحة" }` (اختياري — الفارغ يُخزن null).
يضبط `account_status = rejected`. **Response:** `204`.

### 6.4 عرض وثيقة خاصة

**الصلاحية:** `employees.documents`. رابط موقّع لوثيقة في bucket `documents`
صلاحيته 600 ثانية — انظر §19.3.

---

## 7. المواسم Seasons

**الصلاحية:** `seasons.view` هي الباب؛ `seasons.switch` لتغيير الموسم الحالي؛
`seasons.participants_view` / `seasons.participants_manage` للمشاركين.

### 7.1 ضمان موسم السنة الحالية (بديل RPC `ensure_current_season`)

يُستدعى عند فتح شاشة المواسم: ينشئ صف الموسم لسنة هجرية إن لم يوجد، ويجعله
الحالي **إن كان أحدث** ولم يكن ثمة تثبيت يدوي لهذه السنة (§21.6).

```
POST /seasons/ensure-current
```

**Request:** `{ "hijri_year": 1448, "label": "2026/2027" }`
**Response 200:** `{ "id": "uuid" }`

> `label` هو `gregorian_label` ويشتقّه التطبيق من **امتداد السنة الهجرية
> نفسها** (ميلادية أول يومها إلى ميلادية آخر يومها عبر تحويل أم القرى) — لا من
> سنة الجهاز الحالية، التي كانت تخطئ نصف السنة (من يناير حتى رأس السنة
> الهجرية). خادم بديل يحسبه بنفسه يجب أن يحسبه بالطريقة ذاتها.

### 7.2 كل المواسم

```
GET /seasons
```

**Response (مرتبة hijri_year تنازلياً):**

```json
[
  {
    "id": "uuid",
    "hijri_year": 1448,
    "gregorian_label": "2027",
    "is_current": true,
    "pinned_for_hijri_year": null,
    "created_at": "..."
  }
]
```

### 7.3 الموسم الحالي

```
GET /seasons/current
```

**Response 200:** Season object، أو `404` إن لم يوجد.

### 7.4 تعيين الموسم الحالي (بديل RPC `set_current_season`)

```
POST /seasons/{seasonId}/set-current
```

**Request:** `{ "pinned_for_hijri_year": 1448 }` — تسجيل السنة الهجرية التي
اتُّخذ فيها القرار كي لا يدوسه التقدّم السنوي التلقائي (7.1).
**Response:** `204`. عملية ذرّية: موسم واحد فقط `is_current = true`.

### 7.5 مشاركو موسم

```
GET /seasons/{seasonId}/participants
```

المشاركون بحالة `active` مع الـ profile كاملاً. **Response:** `[Profile]`.

```
GET /seasons/{seasonId}/participants/ids
```

**Response:** `{ "profile_ids": ["uuid", ...] }` — نسخة خفيفة تُستخدم للمقارنة.

### 7.6 سِجل مشاركة موظف

```
GET /profiles/{profileId}/participation-history
```

مواسم المشاركة `active` فقط (المسحوب ليس مشاركة). **Response:** `[Season]` الأحدث أولاً.

### 7.7 الموظفون القابلون للإضافة لموسم

```
GET /seasons/assignable-employees
```

كل حسابات `approved` مرتبة بالاسم الأول. **Response:** `[Profile]`.

### 7.8 إضافة/إعادة تفعيل مشارك

```
PUT /seasons/{seasonId}/participants/{profileId}
```

Upsert على `(season_id, profile_id)` بحالة `active`. **Response:** `204`.

### 7.9 سحب مشارك

```
DELETE /seasons/{seasonId}/participants/{profileId}
```

**لا حذف فعلياً** — تُضبط الحالة `withdrawn` حفاظاً على التاريخ. **Response:** `204`.

---

## 8. أنواع الملفات التشغيلية Module Types

النوع هو "مخطط" الملف: حقوله، مستويات شجرته، مهامه ومراحلها، وأدواره بمهام كل دور.

### 8.1 الكتالوج الكامل

```
GET /module-types?active=true
```

**Response 200 (مرتبة sort_order):**

```json
[
  {
    "id": "uuid",
    "code": "tawafa_transport",
    "name_ar": "ملف النقل",
    "name_en": "Transport file",
    "end_condition_ar": "ينتهي بعودة آخر حاج",
    "end_condition_en": "Ends when ...",
    "is_active": true,
    "sort_order": 1,
    "fields": [
      {
        "id": "uuid",
        "key": "decision_date",
        "kind": "date",
        "label_ar": "...",
        "label_en": "...",
        "is_required": true,
        "sort_order": 1,
        "options": {}
      }
    ],
    "levels": [
      {
        "id": "uuid",
        "depth": 1,
        "name_ar": "قطاع",
        "name_en": "Sector",
        "reference_set_id": null,
        "secondary_reference_set_id": null,
        "fields": []
      }
    ],
    "teams": [
      {
        "id": "uuid",
        "code": "coasters",
        "name_ar": "فريق الكوسترات",
        "name_en": "Coasters team"
      }
    ],
    "roles": [
      {
        "id": "uuid",
        "code": "sector_supervisor",
        "level_id": "uuid",
        "team_id": null,
        "name_ar": "مشرف قطاع",
        "name_en": "Sector supervisor",
        "job_description_ar": "...",
        "sort_order": 1
      }
    ]
  }
]
```

> **مهم (0105):** لا كتالوج مهام في النوع بعد الآن. `task_groups` و `tasks`
> و `roles[].tasks` و `roles[].tasks_are_assigned` **حُذفت كلها** — المهام تُكتب
> يدوياً على ملف الموسم (§25)، أو لا تكون.

> **`teams` و`roles[].team_id` (0115):** تجميعٌ **مُصرَّح به** للمناصب المحمولة على
> **الملف نفسه** (`level_id = null`)، لا مُستنتَج. «مدير فريق الكوسترات» و«معاونه»
> و«عضو الفريق» فريقٌ واحد بثلاثة مناصب، وقبل ذلك كانت الصفحة ترسم بطاقة لكل
> منصب فتقول إنها ثلاثة فرق. الأكواد تتشارك بادئة `coasters_` والتجميع عليها
> يعمل اليوم ويخطئ أول مرة يُضاف فيها منصبٌ يبدأ بها فحسب — الحجّة نفسها التي
> ساقها 0089 لـ `is_place`.
>
> - `team_id = null` هو **الحال الاعتيادي** ومعناه أن المنصب قائم وحده: بطاقةٌ
>   باسمه، تماماً كما كان. عشرة أنواع من خمسة عشر لا فرق فيها إطلاقاً.
> - `team_id` **يُتجاهل** مع `level_id`: منصبٌ محمولٌ على مستوى مُجمَّعٌ أصلاً تحت
>   عقدته (القطاع/البرج)، وسحبه إلى بطاقة فريق إخراجٌ له منها.
> - **لا `sort_order` على الفريق.** ترتيب الفرق هو ترتيب مناصبها: الفريق يحلّ محلّ
>   **أول** منصب فيه. ترتيبان يعني شيئين يجب إبقاؤهما متوافقين.
> - منصبٌ يذكر فريقاً لا يُصرِّح به النوع **لا يسقط** — يُعرض وحده. منصبٌ يختفي هو
>   منصبٌ شاغرٌ لا يعلم أحد بشغوره.

### 8.2 نوع واحد

```
GET /module-types/{id}
```

**Response:** نفس عنصر 8.1، أو `404`.

---

## 9. الملفات التشغيلية Modules

### 9.1 قاعدة الرؤية (كانت RLS — تصبح منطق خادم إلزامي)

- حامل `modules.view_all` (أو أدمن): يرى **كل** الملفات.
- غيره: يرى فقط الملفات **المفعّلة** (`is_active = true`) التي يحمل فيها دوراً
  (عضوية مباشرة في `module_members` أو عبر عقدة في `module_node_members`).

### 9.2 كيان Module (الاستجابة القياسية)

```json
{
  "id": "uuid",
  "module_type_id": "uuid",
  "module_type": {
    "name_ar": "ملف النقل",
    "name_en": null,
    "end_condition_ar": "...",
    "end_condition_en": null
  },
  "season_id": "uuid",
  "season": { "hijri_year": 1448 },
  "starts_on": "2026-05-01",
  "ends_on": null,
  "start_note": "بدأ العمل مع وصول الدفعة الأولى",
  "end_note": null,
  "decision_number": "123/2026",
  "report_cadence": "daily",
  "data": { "field_key": "value" },
  "is_active": true,
  "created_by": "uuid",
  "created_at": "...",
  "updated_at": "..."
}
```

`report_cadence`: `none | daily | weekly` (قيمة enum `ReportCadence` في التطبيق).
`data`: JSON حر بمفاتيح حقول النوع.

`start_note` / `end_note`: نصّ حر اختياري — **ملاحظة بداية العمل** و**ملاحظة نهاية
العمل**، يكتبهما من يفتح الملف. لا يخلطان مع `module_type.start_condition` /
`end_condition` (§8): تلك جملة ثابتة عن **كل** ملفات النوع، وهذان جواب هذا الملف
وحده. القسم الأول من إنشاء أي ملف هو هذه الحقول ذاتها لكل الأنواع: تاريخ بدء
العمل (إلزامي)، تاريخ الانتهاء، ملاحظة البداية، ملاحظة النهاية، رقم القرار،
دورية التقارير، ثم حقول النوع في `data`.

### 9.3 قائمة الملفات

```
GET /modules?season_id={uuid}
```

`season_id` اختياري. **Response:** `[Module]` — الترتيب النهائي يفرضه الخادم:
الموسم الأحدث أولاً ثم الأحدث إنشاءً.

### 9.4 ملف واحد

```
GET /modules/{id}
```

**Response:** Module أو `404`.

### 9.5 ملف من نوع محدد في موسم محدد

النوع لا يتكرر في الموسم — يُستشار قبل عرض خيار الإنشاء.

```
GET /modules/lookup?module_type_id={uuid}&season_id={uuid}
```

**Response 200:** Module، أو `404` إن لا يوجد.

### 9.6 إنشاء (مسودة)

**الصلاحية:** `modules.create`.

```
POST /modules
```

**Request:**

```json
{
  "module_type_id": "uuid",
  "season_id": "uuid",
  "starts_on": "2026-05-01",
  "ends_on": null,
  "start_note": null,
  "end_note": null,
  "decision_number": null,
  "data": {},
  "report_cadence": "none"
}
```

**Response 201:** `{ "id": "uuid" }` — يُنشأ **غير مفعّل**، والخادم يسجّل `created_by`.
**خطأ:** `409 duplicate_module` (نوع مكرر في الموسم).

### 9.7 تعديل

**الصلاحية:** `modules.edit`.

```
PUT /modules/{id}
```

**Request:** نفس حقول 9.6 عدا النوع والموسم:

```json
{
  "starts_on": "2026-05-01",
  "ends_on": null,
  "start_note": "بدأ العمل مع وصول الدفعة الأولى",
  "end_note": null,
  "decision_number": "123",
  "data": {},
  "report_cadence": "daily"
}
```

> `ends_on: null` **تعني المسح فعلاً** — لا تتجاهلها، وكذلك `start_note: null`
> و`end_note: null`: من مسح الملاحظة قصد أن لا يبقى فيها شيء. النصّ الفارغ
> `""` يُخزَّن `null`. `report_cadence` إن غابت لا تتغير.

**Response:** `204`.

### 9.8 تفعيل / إلغاء تفعيل

**الصلاحية:** `modules.activate`.

```
PATCH /modules/{id}/active
```

**Request:** `{ "is_active": true }` — **Response:** `204`.
**أثر جانبي إلزامي:** التفعيل يُنشئ إشعاراً لكل شخص معيَّن في الملف
(كان trigger `modules_notify_activation`) — انظر §21.5.

### 9.9 حذف

**الصلاحية:** `modules.delete`.

```
DELETE /modules/{id}
```

**Response:** `204`. يتسلسل على العقد والأعضاء والتقارير والتقييمات ومرفقاتها.

### 9.10 البحث عن موظفين قابلين للإسناد (بديل RPC `assignable_employees`)

**الصلاحية:** `modules.members` **أو `tasks.assign`** (migration 0106) — الكشف
نفسه يُملأ به منصبٌ وتُسنَد عليه مهمة، والثاني لا يستلزم إدارة ملفات. بحث مُرقَّم
على الخادم مع **مطابقة عربية مطوَّعة** (همزات/تاء مربوطة موحّدة، ومسافة «عبد …»
تُغلق — دالة `ar_fold` تطبَّق على الطرفين، §21.11).

```
GET /modules/assignable-employees?season_id={uuid}&query={text}&is_external={bool}&job_title_id={uuid}&city_id={uuid}&only_free={bool}&limit=40&offset=0
```

| المعامل       | المعنى                                          |
| ------------- | ----------------------------------------------- |
| `season_id`   | إلزامي — مشاركو هذا الموسم النشطون فقط          |
| `query`       | يطابق الاسم الثلاثي **موصولاً** والمسمى الوظيفي |
| `is_external` | ثلاثي القيم: true / false / غائب (الكل)         |
| `only_free`   | استبعاد من يحمل أي دور في أي ملف بالموسم        |

**Response 200:**

```json
[
  {
    "id": "uuid",
    "first_name": "أحمد",
    "father_name": "محمد",
    "surname": "الخطيب",
    "photo_url": null,
    "is_external": false,
    "external_organization": null,
    "external_title": null,
    "job_title_name": "طبيب",
    "job_title_name_en": "Physician",
    "phone_sy": "+963...",
    "phone_sa": null,
    "account_status": "approved",
    "assignments": [
      {
        "module_id": "uuid",
        "module_name_ar": "ملف النقل",
        "place": "قطاع 1",
        "role_ar": "مشرف"
      }
    ]
  }
]
```

### 9.11 كل مشاركي الموسم النشطين (لحلّ الأسماء داخل المحرر)

```
GET /seasons/{seasonId}/active-participants
```

**Response:** `[Profile]` (بلا الموقوفين)، مرتبة بالاسم.

---

## 10. شجرة الملف Nodes

العقدة = قطاع أو برج/مخيم بحسب `level_id`. تحمل إما `reference_item_id`
(حين يستمد المستوى من البيانات المرجعية — البرج فندق) أو `label` (اسم حر).
منذ 0051 قد تحمل أيضاً `secondary_reference_item_id` (ارتباط ثانٍ — برج ↔ مجموعة).

### 10.0 لا يُنشأ شيء داخل ملف (migrations 0095 + 0096)

> **0096 لازم.** نُفِّذ 0095 بنسختين: الأولى صنعت قائمةً لكل **ملف**
> (`makkah_sectors`, `mashaaer_sectors`, `arafat_centers`, `mina_centers`,
> `mina_camps`)، والثانية قائمةً لكل **صنف**. ولم تستطع الثانية تصحيح الأولى لأن
> كل عبارات نقل البيانات فيها مشروطة بـ `n.reference_item_id is null`، وكانت
> العقد قد رُبطت. 0096 يدمج الأولى في الثانية وينقل العقد ويحذف القوائم القديمة
> بحُرّاس. على قاعدة نظيفة لا يفعل شيئاً.

`label` لم يعد يُستعمل في أيّ مستوى قائم. سبعة مستويات كانت تُكتب باليد —
قطاعات مكة والمشاعر، مراكز عرفات ومنى، مخيمات منى، شركات الخدمة، ومخيمات عرفات
التي وجّهها 0050 إلى قائمة ووُجدت في الكتالوج الحيّ بلا قائمة — صارت كلّها
تستمدّ من `reference_sets`. **الملف شجرة اختيارات وإسنادات، لا مكان إنشاء.**

**قائمة واحدة لكل صنف.** مسوّدة أولى صنعت قائمتَي قطاعات وقائمتَي مراكز، استدلالاً
من التهجئة: مكة تكتب «القطاع الأول» والمشاعر «القطاع 1». وهي قسمةٌ واحدة كُتبت
بوجهين، والأرقام تُطبَّع إلى الحروف في الترحيل. مكة تشغّل خمسةً من السبعة،
والمشاعر تشغّلها كلّها.

| القائمة | موسميّة | النوع |
| --- | --- | --- |
| `sectors` القطاعات | **نعم** (0097) | — |
| `centers` المراكز | **نعم** (0097) | `site` → المشعر |
| `camps` المخيمات | **نعم** | `site` → المشعر |
| `service_companies` شركات الخدمة | لا | — |
| `holy_sites` المشاعر | لا | منى · عرفات |

**والقوائم الستّ التي يُبنى منها الموسم كلّها موسميّة** — الفنادق والتكتلات
(0040)، المجموعات (0064)، المخيمات (0095)، القطاعات والمراكز (0097). حجّة 0095
بأن «القطاع الثالث» اسمٌ ثابت فلا يُوسم صحيحةُ المقدّمة فاسدة النتيجة: الاستيراد
(0043) هو ما يمنع إعادة الكتابة، والسؤال لم يكن عن الاسم بل عن **ما هو القطاع** —
كم عددها، أيّ الأبراج تحتها، من يشغّلها — وذلك يُحسم كل موسم. وبلا وسم، تعديل
قطاعات هذا العام يمتدّ إلى الوراء فيُعيد كتابة ما تقوله ملفات الموسم الماضي.

**والنوع جزءٌ من الهويّة لا وصفٌ عليها.** «المخيم رقم 11» موجود في منى وفي عرفات
وبينهما كيلومتر. لذلك:

- `reference_items.variant` — عمود مولَّد من `data->>'site'`، دخل في
  `uq_reference_items_name_per_season`. الاسم فريدٌ ضمن (القائمة، الموسم،
  **النوع**). القوائم بلا أنواع تُبقيه `null` فتحتفظ بقاعدة 0040 كما هي.
- `module_type_levels.reference_filter` **(جديد، jsonb)** — يُضيّق القائمة إلى
  شريحة المستوى: `{"site": "<uuid منى>"}`. بدونه كان ملف منى سيعرض مخيمات عرفات،
  ومخيمٌ في المشعر الخطأ خطأٌ لا تلتقطه قاعدة لاحقة — الصفّ سليم البنية وكاذب فقط.
  المطابقة بالاحتواء: كل مفتاح موجودٌ ومساوٍ. مفتاحٌ مجهول يُضيّق إلى **لا شيء**
  لا إلى الكل، عمداً.

| الحقيقة | مكانها | لماذا |
| --- | --- | --- |
| المشعر، موقع المخيم، طاقته | **المدخل** `reference_items.data` | تخصّ الأرض، وتُنقل للموسم التالي باستيراد 0043 |
| رقم الخيمة، الجهات المخصّصة | **العقدة** `module_nodes.data` | تخصّ حصّة المكتب من الأرض، وتتبدّل كل موسم |

**و`uq_module_nodes_entry` تغيّر شكله.** كان `(module_id, level_id,
reference_item_id)` — المدخل مرّة واحدة في الملف. صار يضمّ
`coalesce(entry_slot, '')`، و`entry_slot` عمودٌ مولَّد من `data->>'tents'`.
السبب أن العقدة ليست المخيم بل **حصّة المكتب فيه**: عرفات تحمل «المخيم رقم 16 —
خيمة 154» و«خيمة 158»، ومنى تحمل المخيم 16 بخيمة 32 وبخيام 35+36 — مخيمٌ واحد،
حصّتان، وجهات وأشخاص مختلفون في كلٍّ منهما. المستويات التي لا حقل خيمة لها
تُبقيه `null` فتحتفظ بالقاعدة القديمة: الفندق يدخل الملف مرّة، والتكتل يقف في برج
واحد. (طيّ الـ null إلى `''` داخل الفهرس هو حيلة 0040 نفسها.)

**وقائمة `syrian_cities` أُعيدت تسميتها** إلى «مكاتب إدارة الحج والعمرة» — العضو
ينتمي إلى مكتب الإدارة في محافظته، والمدينة كانت تسميةً للمكتب لا أكثر. **الرمز
`syrian_cities` باقٍ** لأن Dart يقرأه (`ReferenceSetCodes.syrianCities`) وسياسة
0030 تطابق عليه. وأُضيف لها حقل `location` من نوع `location` — والمكتب مبنى
يُقصد، و`node_location` يطابق حقول الموقع **بالنوع** (0092) فلا يحتاج سطراً.

**وما يحتاجه التطبيق سطرٌ واحد:** `ReferenceSet.itemsToOffer(seasonId, filter:)`
يضمّ التضييقين — أيّ موسم، ثم أيّ مشعر — ويستدعيه `node_editor_sheet` بـ
`level.referenceFilter`. أمّا الاختيار بين حقل نصّ ومنتقي قائمة فبقي كما كان، من
`level.isNamedByHand`.

### 10.1 الشجرة كاملة

```
GET /modules/{moduleId}/nodes
```

**Response 200 (مرتبة sort_order) — استعلام واحد يعيد كل شيء:**

```json
[
  {
    "id": "uuid",
    "module_id": "uuid",
    "level_id": "uuid",
    "parent_id": null,
    "reference_item_id": null,
    "reference_item": null,
    "secondary_reference_item_id": null,
    "label": "قطاع العزيزية",
    "data": {},
    "sort_order": 1,
    "members": [
      {
        "id": "uuid",
        "role_id": "uuid",
        "profile_id": "uuid",
        "assigned_by": "uuid",
        "profile": { "...Profile object with job_title and city..." }
      }
    ]
  }
]
```

### 10.2 إنشاء عقدة

**الصلاحية:** `modules.edit`.

```
POST /modules/{moduleId}/nodes
```

**Request:**

```json
{
  "level_id": "uuid",
  "parent_id": "uuid | null",
  "reference_item_id": "uuid | null",
  "secondary_reference_item_id": "uuid | null",
  "label": "قطاع 1 | null",
  "data": {},
  "sort_order": 0
}
```

**Response 201:** `{ "id": "uuid" }`

### 10.3 تعديل عقدة

```
PATCH /nodes/{nodeId}
```

**Request:** `{ "reference_item_id": null, "secondary_reference_item_id": null, "label": "...", "data": {} }`
(القيم null تعني المسح — التطبيق يرسل الحقول الأربعة دائماً). **Response:** `204`.

### 10.4 حذف عقدة

```
DELETE /nodes/{nodeId}
```

**Response:** `204`. يتسلسل على عقدها الفرعية وكل الأعضاء تحتها.

---

## 11. أعضاء الملف

### 11.1 أعضاء الملف نفسه (ملف بلا شجرة)

```
GET /modules/{moduleId}/members
```

**Response:** نفس شكل `members` في 10.1 (مع profile كامل)، مرتبة بالاسم
الكامل.

### 11.2 إسنادات موظف (أين يخدم؟)

```
GET /profiles/{profileId}/assignments
```

**Response 200 (الموسم الأحدث أولاً):**

```json
[
  {
    "role": { "name_ar": "مشرف قطاع", "name_en": null },
    "place": {
      "node_label": "قطاع 1",
      "level": { "name_ar": "قطاع", "name_en": null },
      "reference_item": { "name_ar": "فندق كذا", "name_en": null }
    },
    "module": { "...Module object (9.2)..." }
  }
]
```

> عنصر بلا `place` = عضوية على الملف نفسه. **الرؤية:** الملفات غير المرئية
> للطالب تُسقَط من النتيجة كلياً (كان `!inner` + RLS).

### 11.3 استبدال حاملي دور على عقدة

**الصلاحية:** `modules.members`.

```
PUT /nodes/{nodeId}/roles/{roleId}/members
```

**Request:** `{ "profile_ids": ["uuid", "uuid"] }`

**سلوك إلزامي (diff لا استبدال أعمى):**

- من في القائمة وموجود: **لا يُلمس** (كي لا يُعاد إشعاره).
- من ليس في القائمة: يُحذف.
- الجديد: يُدرج مع `assigned_by` من التوكن، **ويُنشأ له إشعار إسناد** إذا كان
  الملف مفعّلاً (§21.5).

**Response:** `204`.

### 11.4 استبدال حاملي دور على الملف

```
PUT /modules/{moduleId}/roles/{roleId}/members
```

نفس عقد 11.3 حرفياً. **Response:** `204`.

> **حُذف (0105):** `PUT /members/{memberId}/tasks` — قوائم الاختيار المسلَّمة
> فرداً فرداً (`module_assigned_tasks`، `tasks_are_assigned`) أُزيلت مع كتالوج
> المهام كله. مهام الملفات صارت **وصفية** (§25.1)، والمتابعة الشخصية في نظام
> مستقل (§25.2).

---

## 12. تقارير الملف التشغيلي

### 12.1 قاعدة الرؤية

حامل `modules.reports` يرى تقارير الجميع؛ العضو العادي يرى تقاريره فقط.

### 12.2 قائمة تقارير ملف

```
GET /modules/{moduleId}/reports
```

**Response 200 (الأحدث أولاً):**

```json
[
  {
    "id": "uuid",
    "module_id": "uuid",
    "author_id": "uuid",
    "author": { "...Profile with job_title..." },
    "period_start": "2026-05-01",
    "body": "نص التقرير",
    "created_at": "...",
    "updated_at": "...",
    "attachments": [
      {
        "id": "uuid", "report_id": "uuid", "kind": "image",
        "path": "moduleId/reports/reportId/0_photo.jpg",
        "name": "الاسم الأصلي بالعربية.jpg",
        "mime_type": "image/jpeg", "size_bytes": 12345, "sort_order": 0
      }
    ]
  }
]
```

### 12.3 رفع تقرير الفترة الحالية (بديل RPC `submit_module_report` + Storage)

**أهم قاعدة:** **الفترة تُحسب على الخادم** لا على الجهاز (ساعة الهاتف لا يُوثق بها):

- `report_cadence = daily` ⇒ فترة اليوم؛ `weekly` ⇒ فترة الأسبوع.
- **الرفع مرتين في نفس الفترة = تعديل التقرير الأول** (upsert على
  `(module_id, author_id, period_start)`)، ولهذا يوجد `removed_attachment_ids`.

يُنفَّذ كخطوتين مثل التطبيق الحالي، أو يُدمج بمسار multipart واحد — **التوصية**:

```
POST /modules/{moduleId}/reports        (multipart/form-data)
```

| الجزء                    | النوع      | الوصف                                                                   |
| ------------------------ | ---------- | ----------------------------------------------------------------------- |
| `notes`                  | text       | نص التقرير (اختياري)                                                    |
| `removed_attachment_ids` | JSON array | مرفقات تُحذف من تقرير الفترة الموجود                                    |
| `files[]`                | binary     | المرفقات الجديدة، مع `kind[]` (`image`/`file`) و `name[]` بالاسم الأصلي |

**سلوك المرفقات الإلزامي:**

- `sort_order` يبدأ بعد أعلى قيمة موجودة (إعادة الرفع لا تدوس القديم).
- مفتاح التخزين ASCII فقط (§19.5) بنمط `{moduleId}/reports/{reportId}/{n}_{key}`.
- الاسم العربي الأصلي يُخزَّن في الصف ويُعرض ويُنزَّل به.

**Response 200:** `{ "report_id": "uuid" }`

### 12.4 حذف مرفق تقرير

مضمَّن في 12.3 عبر `removed_attachment_ids` (حذف الصف + ملف التخزين معاً).

---

## 13. التقييمات Ratings

سرية بالتصميم: لا أحد يقرأ صفوف التقييم الخام لغيره أبداً.

### 13.1 تقييماتي في ملف

```
GET /modules/{moduleId}/ratings/mine
```

**Response:** `{ "ratings": { "<ratee_profile_id>": 4, "<ratee_profile_id>": 5 } }`

### 13.2 تقييم زميل (upsert)

```
PUT /modules/{moduleId}/ratings/{rateeId}
```

**Request:** `{ "stars": 4 }` (1–5). قيد فريد `(module_id, rater_id, ratee_id)`.
**قواعد:** المقيِّم والمقيَّم كلاهما عضو في الملف؛ **نافذة التقييم** يفرضها
الخادم (`module_rating_open`: الملف مفعّل وضمن مدته). **Response:** `204`.

### 13.3 سحب التقييم

```
DELETE /modules/{moduleId}/ratings/{rateeId}
```

**Response:** `204` — السحب غير إعطاء نجمة واحدة.

### 13.4 نتيجتي (بديل RPC `my_module_rating`)

ما قاله الآخرون **عني** — متوسط وعدد فقط، بلا أسماء:

```
GET /modules/{moduleId}/ratings/my-summary
```

**Response:** `{ "average": 4.2, "ratings": 7 }` — وعند لا شيء: `{ "average": null, "ratings": 0 }`.

---

## 14. البيانات المرجعية Master Data

قوائم (`reference_sets`) وعناصرها (`reference_items`). بعض القوائم **موسمية**
(الفنادق) وبعضها عام (المدن). لكل قائمة "مخطط عنصر" (`reference_set_fields`).

**الصلاحيات:** `reference.view` قراءة، `reference.edit` كتابة،
`reference.delete` حذف، `reference.import` استيراد بين المواسم.

**ثلاثة أعمدة على `reference_sets` تقرّر كيف تُعامَل القائمة، ولا يُشتقّ أيٌّ منها
من المخطّط:**

| العمود | يقرّر | مضاف في |
| ------ | ---- | ------- |
| `is_season_scoped` | هل للمدخل موسم، وهل يُعرض زرّ الاستيراد | 0040 |
| `is_place` | هل مداخلها أماكن تحمل رموز وصول وإحداثيات | 0098 |
| `section` | تحت أيّ عنوان فرعي تظهر في شاشة البيانات المرجعية | 0101 |

و`section` **مفتاح لا عنوان**: الصياغة في التطبيق لأنها محتوى بلغتين. وسبب وجوده
في القاعدة لا في Dart أن التجميع **تحريري** — لا شيء في المخطّط يقول إن الفندق
والمخيم معاً بينما المركز في مكان آخر — ولو كان في Dart لَما وجدت قائمةٌ يضيفها
ترحيلٌ لاحق مكاناً لها حتى يُعدَّل التطبيق، وهو الاقتران الذي أزالته 0095/0096.
والقيمة الفارغة تسقط في رفّ «أخرى» **ظاهرةً**: قائمةٌ لا يجدها أحد قائمةٌ لا
يصحّحها أحد.

### 14.1 القوائم كاملة

```
GET /reference-sets?active=true
```

**Response 200 (مرتبة code):**

```json
[
  {
    "id": "uuid",
    "code": "hotels_makkah",
    "name_ar": "فنادق مكة",
    "name_en": null,
    "is_season_scoped": true,
    "fields": [
      {
        "id": "uuid",
        "set_id": "uuid",
        "key": "capacity",
        "kind": "number",
        "label_ar": "السعة",
        "label_en": null,
        "target_set_id": null,
        "is_required": false,
        "sort_order": 1
      }
    ],
    "items": [
      {
        "id": "uuid",
        "set_id": "uuid",
        "season_id": "uuid",
        "name_ar": "فندق كذا",
        "name_en": null,
        "data": { "capacity": 400 },
        "is_active": true,
        "sort_order": 1
      }
    ]
  }
]
```

> `active=true` تصفّي **العناصر** غير المفعّلة (القوائم تعود كلها).
> ملاحظة PostgREST التاريخية: حقل من نوع reference يشير إلى قائمة أخرى عبر
> `target_set_id` — لذلك كانت الحقول استعلاماً منفصلاً؛ في REST تُضمَّن مباشرة.

### 14.2 استيراد عناصر قائمة من موسم لآخر (بديل RPC `copy_reference_items`)

```
POST /reference-sets/{setId}/copy-items
```

**Request:** `{ "from_season_id": "uuid", "to_season_id": "uuid" }`
**Response:** `{ "copied": 12 }`
**قواعد:** نسخ مستقلة تماماً؛ الأسماء الموجودة في الهدف تُتخطى (idempotent).

### 14.3 استيراد قطاعات من ملف آخر (بديل RPC `copy_module_sectors`)

```
POST /modules/{toModuleId}/copy-sectors
```

**Request:** `{ "from_module_id": "uuid" }`
**Response:** `{ "copied": 5 }` — عدد **القطاعات** لا الأشخاص.
**قواعد:** نفس الموسم، نفس المستوى فقط، الاسم الموجود يُتخطى، ويُنسخ مع القطاع
حاملو الأدوار التي يعرفها الملفان **بنفس الكود**.

### 14.4 إضافة عنصر

```
POST /reference-items
```

**Request:**

```json
{
  "set_id": "uuid",
  "name_ar": "فندق جديد",
  "name_en": null,
  "data": {},
  "season_id": "uuid | null"
}
```

`season_id` تكون null للقوائم غير الموسمية. **Response 201:** `{ "id": "uuid" }`.

### 14.5 تعديل عنصر

```
PATCH /reference-items/{id}
```

**Request:** `{ "name_ar": "...", "name_en": null, "data": {} }` — **Response:** `204`.

### 14.6 حذف عنصر

```
DELETE /reference-items/{id}
```

**Response:** `204`، أو **`400 reference_item_in_use`** إذا كانت عقدة/ملف/عنصر آخر
يشير إليه (كان trigger `guard_reference_item_delete` — يجب نقله للخادم §21.7).

> **«حذف الكل»** في شاشة القائمة يكرّر هذا النداء عنصراً بعنصر، لا نداءً جماعياً
> واحداً: الحارس يرفض صفاً واحداً فيسقط الحذف كله، والمطلوب أن يذهب ما يمكن
> ذهابه ويُعدّ الباقي. ويُعاد المرور ما دام مروره السابق قد حذف شيئاً — لأن عنصراً
> مرفوضاً قد يتحرّر بعد زوال ما كان يشير إليه. لا حاجة إلى endpoint جماعي.

---

## 15. القرارات Decisions (الجداول باسم `reports`)

> **قرار أم تعميم (migration 0102).** القسم يحمل الاثنين وحملهما دائماً، بلا ما
> يفرّق بينهما حتى 0102. والفرق فعلٌ لا تسمية: **القرار يُلزم** أحداً — يشكّل
> لجنة، يعيّن مشرفاً، يوزّع المخيمات — **والتعميم يُعلم** الجميع بما ينبغي أن
> يعرفوه. في قائمة واحدة لا تفرّق، يقرأ الرجل الباحث عمّا **يجب أن يفعله** فوق
> كل ما يكفيه أن يعرفه.
>
> العمود `reports.kind` بنوع `decision_kind` (`decision` / `circular`)،
> **لكل صفّ لا لكل `report_type`**: النوع يقول ما **شكل** المستند — جدول مواعيد،
> متنٌ من كتل — والشكل نفسه يحمل الفعلين، فـ«تقرير عام» يُستعمل لقرارات تشكّل
> لجاناً ولتعاميم تعلن جدولاً. الشكل والفعل سؤالان مختلفان فعمودان مختلفان.
>
> افتراضه `decision`، لأن كل صفّ قائم كُتب حين كان القسم يُقرأ على أنه القرارات
> وحدها. وما كان منها تعميماً فتعديلٌ واحد يصحّحه — وتعديلٌ يفعله إنسان عن قصد
> خيرٌ من تخمينٍ يجريه ترحيلٌ نيابةً عنه من عنوان.
>
> **تنبيه على التسمية.** ما يحمله هذا القسم هو **قرارات** البعثة — التعاميم
> المرقّمة المنشورة على الجميع. الواجهة تسمّيها «القرارات / Decisions»، وهو ما
> تسمّيه الإدارة. أمّا **المُعرِّفات فلم تتغيّر**: الجدول `reports`، والمسارات
> `/reports` و`/reports/manage`، وأكواد الصلاحيات `reports.*`، وستّ migrations
> حتى 0071 مكتوبة عليها. إعادة تسميتها تغييرُ مخطط وترحيلُ صلاحيات وإعادة منح
> لكل حساب — مخاطرة أثناء موسم، لإصلاح كلمة لا يقرأها إلا مطوّر.
>
> **لا تخلطه بـ«تقرير»**: تلك كلمة صحيحة ومستعملة لشيء آخر تماماً —
> `module_reports`، ما يرفعه العضو على ملفه التشغيلي بدوريّته (§12، migration
> 0044). جدولان وشاشتان وصلاحيتان منفصلة، والعربية تفرّق بينهما الآن.

### 15.1 قاعدة الرؤية

التقرير **المنشور** (`is_published = true`) يقرؤه أي حساب فعّال؛
**غير المنشور** لا يراه إلا حامل `reports.view_all`.
`reports.publish` صلاحية مستقلة عن `reports.edit` (النشر بثّ للبعثة كلها).

### 15.2 كتالوج أنواع التقارير

```
GET /report-types
```

**Response (مرتبة sort_order):**

```json
[
  {
    "id": "uuid",
    "code": "meals_daily",
    "name_ar": "تقرير الوجبات",
    "name_en": null,
    "once_per_season": false,
    "sort_order": 1,
    "fields": [
      {
        "id": "uuid",
        "key": "date",
        "kind": "date",
        "label_ar": "التاريخ",
        "is_required": true,
        "sort_order": 1
      }
    ],
    "columns": [
      {
        "id": "uuid",
        "key": "count",
        "label_ar": "العدد",
        "kind": "number",
        "input": "text",
        "is_expanded": false,
        "is_computed": false,
        "sort_order": 1
      }
    ]
  }
]
```

> `is_computed`: عمود يجمع الأعمدة `is_expanded` — **القيمة تُخزَّن مع الصف**
> (يعيد التطبيق حسابها عند التحرير)، فالخادم لا يحسب شيئاً.

> `once_per_season` (migration `0078`): نوعٌ يُنشأ **مرة واحدة خلال الموسم** —
> أنواع الوجبات الثلاثة (`mashaaer_meal_distribution`, `mashaaer_meal_timing`,
> `mashaaer_meal_components`) عليها `true`. القاعدة في §15.5.

### 15.3 قائمة التقارير

```
GET /reports?season_id={uuid}
```

**قاعدة الفلترة (انتبه):** مع `season_id` تُعاد تقارير ذلك الموسم **والتقارير
العامة** (`season_id IS NULL`) معاً. بدونه: كل المقروء.

**سقف القائمة:** التطبيق يطلب أحدث **200** عنصر (`ReportsRepository.listLimit`)
— دعم `?limit=` كافٍ.

**Response (مرتبة updated_at تنازلياً):** — حقل `data` (رأس التقرير jsonb)
**غير مطلوب في القائمة**؛ التطبيق لا يعرضه فيها ويقرؤه من 15.4 عند الفتح.
إعادته لا تضر، لكن حذفه من الإسقاط يوفّر حمولة فقرات كاملة لكل صف.

```json
[
  {
    "id": "uuid",
    "report_type_id": "uuid",
    "report_type": { "name_ar": "...", "name_en": null },
    "season_id": "uuid | null",
    "season": { "hijri_year": 1448 },
    "title": "تقرير وجبات يوم عرفة",
    "number": "45/2026",
    "data": { "field_key": "value" },
    "is_published": true,
    "created_at": "...",
    "updated_at": "..."
  }
]
```

### 15.4 تقرير واحد كامل

```
GET /reports/{id}
```

**Response:** عنصر 15.3 مضافاً إليه:

```json
{
  "rows": [
    {
      "id": "uuid",
      "report_id": "uuid",
      "data": { "col_key": "قيمة" },
      "sort_order": 1
    }
  ],
  "blocks": [
    {
      "id": "uuid",
      "report_id": "uuid",
      "kind": "paragraph",
      "data": { "text": "..." },
      "sort_order": 1
    }
  ],
  "attachments": [
    {
      "id": "uuid",
      "path": "reportId/0_scan.pdf",
      "name": "الأصل.pdf",
      "mime_type": "application/pdf",
      "size_bytes": 1000,
      "sort_order": 0
    }
  ]
}
```

### 15.5 إنشاء تقرير

**الصلاحية:** `reports.create` (+ `reports.publish` إذا `is_published = true`).

```
POST /reports
```

**Request:**

```json
{
  "report_type_id": "uuid",
  "season_id": "uuid | null",
  "title": "العنوان",
  "number": "45/2026 | null",
  "data": {},
  "is_published": false,
  "rows": [{ "data": { "col": "v" }, "sort_order": 1 }],
  "blocks": [
    { "kind": "paragraph", "data": { "text": "..." }, "sort_order": 1 }
  ]
}
```

> `number` الفارغ يُخزن **null** لا سلسلة فارغة (يؤثر على الفرز والبحث).

**Response 201:** `{ "id": "uuid" }` — الخادم يسجل `created_by`.
**يجب أن تكون العملية transaction واحدة** (رأس + صفوف + كتل).

**قاعدة "مرة واحدة خلال الموسم" (إلزامية على الخادم):** إذا كان النوع
`once_per_season = true` ووُجد تقرير آخر بنفس `report_type_id` ونفس
`season_id` (والـ `null` — العام — سلة قائمة بذاتها تخضع للقاعدة نفسها)،
يُرفض الطلب — **409** ورسالة تحمل الرمز `report_once_per_season` (التطبيق
يطابق عليه نصياً ويعرضه مترجماً). حالياً trigger على `reports`
(migration `0078`) يرفض الإدراج والتعديل معاً؛ التطبيق يمنعها في المحرر
قبل الإرسال، والخادم هو الضامن عند السباق أو القائمة القديمة.

> **مطابقة التطبيق الحالي:** منذ migration `0074` يستدعي التطبيق دالة واحدة
> `save_report(p_report_id, …, p_rows, p_blocks)` تنفّذ الرأس والصفوف والكتل
> في transaction واحدة (إنشاءً وتعديلاً معاً — `p_report_id = null` يعني
> إنشاء). هذا العقد هو نفسه 15.5/15.6 حرفياً؛ لم يعد التطبيق يرسل خمسة طلبات.

### 15.6 تعديل تقرير

**الصلاحية:** `reports.edit` (+ `reports.publish` لتغيير حالة النشر — §21.8).

```
PUT /reports/{id}
```

**Request:** نفس 15.5. **السلوك الإلزامي:** الصفوف والكتل **تُستبدل جملة**
(حذف الكل ثم إدراج الوارد) داخل transaction. **Response:** `204`.

### 15.7 حذف تقرير

**الصلاحية:** `reports.delete`.

```
DELETE /reports/{id}
```

**Response:** `204` (يتسلسل على الصفوف والكتل والمرفقات).

---

## 16. الإشعارات Notifications

### 16.1 النموذج

- كل صف إشعار يخص **مستلماً واحداً** (`recipient_id`).
- البثّ = صفوف كثيرة تشترك في `group_id` واحد، **والمرفقات تُعلَّق على المجموعة**
  لا على الصف (500 مستلم = ملف واحد).
- الإشعار الفردي أيضاً له `group_id` (خاص به).
- الصف قد يحمل `data` بمفاتيح توجيه: `{ "type": "module_broadcast", "module_id": "uuid" }`
  أو `{ "type": "module_assignment", "module_id": "uuid" }` — التطبيق يستخدمها للتنقل.

### 16.2 صندوق الوارد

```
GET /notifications?limit=100
```

**سقف الوارد:** التطبيق يطلب أحدث **100** صف
(`NotificationsRepository.inboxLimit`) — الجدول ينمو ما دام الحساب حياً،
والقراءة بلا سقف كانت تجرّ التاريخ كله. بثّ الـ Realtime البديل (§20) يلتزم
بالسقف نفسه.

**Response (الأحدث أولاً):**

```json
[
  {
    "id": "uuid",
    "group_id": "uuid",
    "recipient_id": "uuid",
    "sender_id": "uuid | null",
    "title": "تم إسنادك إلى ملف تشغيلي",
    "body": "نص | null",
    "data": { "type": "module_assignment", "module_id": "uuid" },
    "read_at": null,
    "created_at": "..."
  }
]
```

### 16.3 مرفقات مجموعة/مجموعات

```
GET /notifications/attachments?group_ids=uuid1,uuid2
```

**Response:**

```json
{
  "uuid1": [
    {
      "id": "uuid",
      "group_id": "uuid1",
      "kind": "image",
      "path": "groupId/0_photo.jpg",
      "name": "صورة.jpg",
      "mime_type": "image/jpeg",
      "size_bytes": 500,
      "sort_order": 0
    }
  ]
}
```

**الترتيب `sort_order` تصاعدي** — وهو التزام لا تفصيل عرض: المرفقات تُخزَّن
بالترتيب الذي اختارها به المُرسِل (`{group_id}/{i}_{key}`، §19.5)، وإعادتها
منكوسة تعني عرض آخر ما صوّره الرجل على أنه أول ما فعله.

**استثناء البلاغ العاجل.** صفّ إشعارٍ `data.type == "incident"` **لا مرفقات له
في هذا الـ endpoint، ولن يكون له**: أدلّة الطوارئ جزء من **البلاغ** لا من
الرسالة المعلنة عنه — `incident_attachments` في دلو `incidents` (§30)، بينما
`raise_incident` تكتب صفّاً لكل مستلم بمجموعةٍ خاصّة به وبلا مرفق واحد. فالصندوق
يقرؤها من هناك بمعرّف `data.incident_id`، ويوقّع لها على دلو `incidents`:

```
GET /incidents/attachments?incident_ids=uuid1,uuid2      → نفس شكل الاستجابة أعلاه
```

**التزام على أي backend بديل:** الصلاحية على الطريقين واحدة —
`incidents.receive` هي شرط قراءة الصفوف والملفات معاً (§30.7)، وهي بعينها من
ذهب إليه الإشعار. لا حاجة إلى قاعدة جديدة، وليس المطلوب نسخ الصورة إلى دلو
الإشعارات: نسخةٌ ثانية من دليلٍ يجوز شطبه (§30.7) هي دليلٌ يبقى بعد شطبه.

### 16.4 تعليم مقروء

```
PATCH /notifications/{id}/read      → 204   (فقط إن كان read_at null)
POST  /notifications/read-all       → 204   (كل غير المقروء للمستخدم)
POST  /notifications/read           → 204   (مجموعة مسمّاة)  { "ids": ["uuid", …] }
```

**لماذا ثلاثة وليس اثنين.** صار الصندوق يُقرأ نصفاً نصفاً — إشعارات، أو بلاغات
عاجلة (§16.8) — و«تعليم الكل كمقروء» مضغوطاً فوق نصفٍ يعني ذلك النصف. الطريق
الثاني يمسح كل شيء بشرطٍ على `recipient_id`، فلا يعرف عن الفلتر شيئاً: استعماله
هناك يمسح أربعين تعميماً لا يراها صاحبها من زرٍّ مكتوبٍ عليه «الكل».

**التزامات الخادم:**

1. `ids` تُصفّى على `recipient_id = المستخدم` **في الخادم**، لا في العميل. قائمة
   معرّفات قادمة من جهاز ليست إذناً بالكتابة على صفوف غيره.
2. الشرط `read_at is null` باقٍ كما في الطريقين الآخرين — تعليم المقروء مقروءاً
   لا يُحرّك تاريخه.
3. `ids` فارغة → 204 بلا كتابة. لا يُعامَل الفراغ كـ«الكل».

### 16.5 إرسال لشخص واحد

**الصلاحية:** `notifications.send`.

```
POST /notifications                  (multipart/form-data)
```

| الجزء                           | الوصف              |
| ------------------------------- | ------------------ |
| `recipient_id`                  | uuid المستلم       |
| `title`                         | إلزامي             |
| `body`                          | اختياري            |
| `files[]` + `kind[]` + `name[]` | المرفقات (اختياري) |

**سلوك الخادم الإلزامي:**

1. إدراج صف الإشعار (المصدر الموثوق) وتسجيل `sender_id`.
2. تخزين المرفقات تحت `{group_id}/{i}_{key}` (§19.5).
3. **دفع Push** لأجهزة المستلم عبر FCM — **best-effort**: فشل الدفع لا يفشل الطلب
   (يحل محل Edge Function `send-notification` §17.3).

**Response 201:** `{ "id": "uuid", "group_id": "uuid" }`

### 16.6 بثّ إلى ملف تشغيلي (بديل RPC `broadcast_to_module`)

**الصلاحية:** `notifications.broadcast_module`.

```
POST /notifications/broadcast/module          (multipart)
```

**الأجزاء:** `module_id`, `title`, `body?`, `files[]…`

**سلوك إلزامي:**

- إدراج صف لكل حامل دور في الملف (مباشرة أو عبر أي عقدة) **بعبارة واحدة** —
  الكلفة لا تنمو مع عدد الأعضاء.
- كل الصفوف تحمل نفس `group_id` و `data = { "type": "module_broadcast", "module_id": ... }`.
- دفع FCM واحد إلى topic `module_{module_id}` **بنفس** حمولة data.

**Response 200:** `{ "group_id": "uuid" }`

### 16.7 بثّ عام (بديل RPC `broadcast_to_all`)

**الصلاحية:** `notifications.broadcast_all`.

```
POST /notifications/broadcast/all             (multipart)
```

**الأجزاء:** `title`, `body?`, `season_id?` (null = كل حساب فعّال؛ قيمة = مشاركو
الموسم النشطون فقط)، `files[]…`

**سلوك:** كما في 16.6 مع topic `all`. **Response 200:** `{ "group_id": "uuid" }`

### 16.8 نصفا الصندوق — إشعار أم بلاغ؟

**لا endpoint لهذا، وهذا هو الالتزام.** التقسيم كلّه في العميل، ويُقرأ من `data`
الموجود أصلاً (§16.1): صفٌّ `data.type == "incident"` بلاغ، وما عداه ليس. لا
عمود `kind` جديد، ولا مُعامل `?filter=` على `GET /notifications` — الصندوق يصل
كاملاً كما كان، ويُقسَم بعد وصوله.

**ولماذا لا يُقسَم في الخادم:** العدد على كل شريحة عدد غير المقروء في النصف الذي
**لا** ينظر إليه القارئ. نصفٌ يصل وحده لا يعرف كم في الآخر، فيصير العدّاد طلباً
ثانياً يُعاد على كل ضغطة.

**وجهة الضغط** — كذلك من `data` وحده، ولا شيء يُضاف إلى الحمولة:

| `data.type`                                          | الوجهة                            |
| ---------------------------------------------------- | --------------------------------- |
| `incident`                                           | السجلّ، على البلاغ نفسه (§30)     |
| `module_assigned` / `module_broadcast` / `report_overdue` | الملف التشغيلي                |
| ما عداها                                             | لا وجهة — والبطاقة لا تدّعي غيرها |

`incident` يحمل `module_id` أيضاً، **والوجهة السجلّ لا الملف**: الملف هو المكان
الذي وقعت فيه الطارئة، لا الطارئة. من أيقظه «بلاغ عاجل» ذاهبٌ ليتصل بالرجل، وكلا
الرقمين في السجلّ.

---

## 17. أجهزة الدفع Push / Device Tokens

### 17.1 تسجيل جهاز

يُستدعى عند بدء الجلسة وعند تجديد توكن FCM.

```
PUT /me/devices
```

**Request:** `{ "token": "<fcm token>", "platform": "android" | "ios" }`
Upsert على `(user_id, token)` مع تحديث `updated_at`. **Response:** `204`.

### 17.2 إسقاط جهاز (كتم)

يُستدعى قبل تبديل الحساب وعند الخروج.

```
DELETE /me/devices/{token}
```

**Response:** `204` (لا يفشل إن لم يوجد).

### 17.3 مسؤولية الدفع تنتقل للخادم

> **انتقلت فعلاً في 2026-08-07 (migration 0107). هذا القسم كان وعداً وصار وصفاً.**
>
> قبله: الشيء **الوحيد** في النظام كلّه الذي بلغ FCM يوماً هو دالة Dart واحدة
> تُستدعى يدوياً من شاشة الإرسال. وعشرة migrations تكتب في `notifications`
> (0017، 0024، 0041، 0073، 0079، 0083، 0084، 0086، 0088، 0105) — **ولا واحد
> منها كان يوقظ هاتفاً**. الوارد يمتلئ وينتظر أن يُفتح.
>
> والـ Realtime هو ما أخفى العطب: مطوّرٌ والتطبيق مفتوح أمامه يرى الشارة تتحرّك
> فيستنتج أن الأمر يعمل. الهاتف في الجيب بلا socket، وهذه هي الحالة التي بُنيت
> الميزة لها.
>
> **ما كلّفه ذلك:** بلاغات 0088 العاجلة (الحافلة على طريق عرفة — تصل حين يفتح
> أحدهم التطبيق)؛ وتصعيد 0086 وهو **صامتٌ بنيوياً** لأنه يمرّ ليلاً وكل تطبيق
> مغلق حينها — أي أن الشيء الوحيد الذي يراقب ما لم يحدث كان عاجزاً عن الإبلاغ
> عمّا وجد؛ وإسناد المهام في 0105 الموصوف «مع إشعار عند الإسناد».

**الشكل الآن — والالتزام على أي backend بديل هو هذا الشكل نفسه:**

1. **الدفع بجانب الكتابة، لا بيد الكاتب.** trigger على `notifications` يعمل
   **مرة لكل statement** (`referencing new table` لا `for each row`) ويرسل
   **دفعة واحدة لكل `group_id`**. فأي كاتب — RPC، سياسة، pg_cron، أو التطبيق —
   يصل هاتفاً بمجرد أن يكتب صفاً، بلا سطر إضافي عنده وبلا أن يُنسى.
2. **`group_id` هو وحدة الدفع.** بثٌّ لخمسمئة شخص = خمسمئة صف وgroup واحد ودفعة
   واحدة. الصفّية (`for each row`) تنقض حجّة 0041 كاملةً وتعيد الخمسمئة نداء،
   **وهي تتدهور مع النجاح**: لا تظهر على ملف اختباري بثلاثة أعضاء، وتظهر يوم
   يصير الملف أربعمئة.
3. **الـ topic باقٍ**، ويُشتقّ من الصفّ لا يُخزَّن من جديد: `data->>'type'` =
   `broadcast` ⇒ topic `all`، و`module_broadcast` ⇒ `module_<uuid>`. وأي نوع آخر
   إرسالٌ لأشخاص، فيسافر كقائمة `recipient_ids`.
4. **الدفع لا يُسقط إشعاراً أبداً.** الـ trigger لا يرفع استثناءً قط (وإلا
   لأسقط الـ INSERT معه، فيتحوّل دفعٌ ضائع إلى **بلاغٍ ضائع**)، والـ HTTP
   لا تزامني فلا تُبطئ تسجيل بلاغ عاجل. صفّ الوارد يبقى مصدر الحقيقة.
5. **العميل لا يدفع إطلاقاً.** كان يدفع، وأُزيل — كاتبان لصفّ واحد يعنيان
   **رنّتين**، وفي الرسالة الوحيدة التي يهمّ فيها ذلك، في الساعة التي يهمّ فيها.
6. **البلاغ العاجل قناة مستقلة.** `priority: high` أمرٌ يخصّ **التوصيل**؛ وكيف
   يُعلن الهاتف عمّا وصل شأن القناة. فمن أسكت التطبيق لكثرة التعاميم يكون قد
   أسكت الحافلة على طريق عرفة معها. لذا `data.type = 'incident'` ⇒ قناة
   `incidents` بـ `PRIORITY_MAX` وصنف `alarm` (يعبر «عدم الإزعاج»)، والاسم
   نفسه مُتحقَّقٌ من اتساقه في ثلاثة ملفات بـ `test/push_delivery_test.dart`.
7. **استيثاق الدالة صار مسارين**: مستخدم مُعتمَد يحمل المنحة المطابقة لشكل
   الإرسال (كما كان)، **أو** حاملُ مفتاح الخدمة من Vault — وهذا الثاني بلا فحص
   صلاحية عن قصد: الصفوف **موجودة أصلاً**، وكلٌّ منها كتبته RPC أو سياسة قرّرت
   من يستحقّه. إعادة الحكم في الدالة تعني نسخةً ثانية من تسع قواعد تفويض بلغة
   أخرى، حرّةً في أن تنحرف. **الصفُّ هو التفويض.**

Edge Function `send-notification` تصبح **منطقاً داخلياً** في الـ backend
(لا endpoint يستدعيه التطبيق مباشرة):

- **فردي:** جلب توكنات المستلم من `device_tokens` والإرسال واحداً واحداً عبر
  FCM HTTP v1 (`projects/{pid}/messages:send`) بحساب خدمة Firebase.
- **بثّ:** رسالة واحدة إلى topic (`all` أو `module_{id}`).
- الرسالة: `notification: { title, body }` + `data` **بقيم نصية فقط** (FCM يرفض
  المتداخل) + `android.priority = "high"`.
- **صيغة أسماء الـ topics ثابتة تعاقدياً** — التطبيق يشترك بها بنفسه عبر
  FCM SDK: `all` و `module_{module_id}`. أي تغيير يكسر الأجهزة المنصّبة.
- **تحقق إلزامي من الـ topic** (مطبَّق في `send-notification` الحالية): يُقبل
  حرفياً `all` أو `module_<uuid>` **لملف موجود فعلاً** — أي نص آخر يُرفض بـ
  `400`، فلا يستطيع حامل صلاحية الدفع إلى topic اعتباطي.
- **سرية البث:** اشتراك الـ topics بيد الجهاز لا الخادم (وجهاز حُذف صاحبه من
  ملف يبقى مشتركاً حتى يزامن بنفسه) — فالبث **مكبّر صوت لا ظرف مغلق**: كل ما
  هو حساس يوضع في صف الوارد المحمي بـ RLS، لا في عنوان الدفعة أو نصها.

---

## 18. لوحة المعلومات Dashboard

### 18.1 مواسم اللوحة (بديل RPC `dashboard_seasons`)

```
GET /dashboard/seasons
```

**Response:**

```json
[
  {
    "id": "uuid",
    "hijri_year": 1448,
    "gregorian_label": "2027",
    "is_current": true
  }
]
```

### 18.2 الإحصاءات (بديل RPC `dashboard_stats`)

```
GET /dashboard/stats?season_id={uuid}
```

`season_id` غائب = الموسم الحالي.

**المبدأ التعاقدي الأهم:** كل قسم في الاستجابة **إما موجود أو غائب كلياً** بحسب
صلاحيات الطالب — الغياب يعني "لا يحق لك السؤال" وهو **غير** الصفر. الأعداد
تُحسب **خارج قيود الرؤية** (عدّ فعلي) ثم يُقرَّر ما يُسلَّم:

| القسم                 | شرط الظهور                                          |
| --------------------- | --------------------------------------------------- |
| `people`              | `employees.view`                                    |
| `approvals`           | `approvals.view`                                    |
| `modules`, `ratings`, `reports` (تقارير الملفات) | `modules.view_all` أو `modules.members` |
| `central_reports`     | `reports.view_all`                                  |
| `notifications`       | إحدى صلاحيات الإرسال الثلاث                         |
| `reference`           | `reference.view`                                    |
| `permissions`         | `permissions.view`                                  |

**الأقسام الأربعة المضافة (migration 0075):**

```json
{
  "central_reports": {
    "total": 6, "published": 4, "drafts": 2, "general": 1,
    "by_type": [{ "label_ar": "تقرير الوجبات", "label_en": null, "n": 3 }]
  },
  "notifications": {
    "messages": 5, "recipients": 40, "read": 30, "total_messages": 12,
    "series": [{ "day": "2026-07-30", "n": 2 }]
  },
  "reference": {
    "sets": 3, "items": 20, "active": 18,
    "season_items": 15, "general_items": 5,
    "by_set": [{ "label_ar": "فنادق مكة", "label_en": null, "n": 9 }]
  },
  "permissions": {
    "admins": 2, "grantees": 7, "grants": 21,
    "by_section": [{ "key": "modules", "count": 9 }]
  }
}
```

> `central_reports` يُحصى على الموسم المختار **والتقارير العامة** معاً (قاعدة
> 15.3 نفسها). `notifications` غير موسمي — نافذته آخر 30 يوماً بجانب إجمالي
> تاريخي. `by_section` مفاتيحه أكواد الأقسام الثمانية والتطبيق يعرّبها.

**Response 200 (الشكل الكامل):**

```json
{
  "season": {
    "id": "uuid",
    "hijri_year": 1448,
    "gregorian_label": "2027",
    "is_current": true
  },
  "people": {
    "participants": 250,
    "withdrawn": 5,
    "internal": 200,
    "external": 50,
    "by_mission": [
      { "label_ar": "البعثة الإدارية", "label_en": "Administrative mission", "count": 120 }
    ],
    "by_gender": [{ "key": "male", "count": 180 }],
    "by_job_title": [
      { "label_ar": "طبيب", "label_en": "Physician", "count": 30 }
    ]
  },
  "approvals": {
    "pending": 3,
    "approved": 260,
    "rejected": 4,
    "incomplete": 9
  },
  "modules": {
    "total": 14,
    "active": 9,
    "draft": 5,
    "ended": 2,
    "running": 7,
    "nodes": 120,
    "members": 480,
    "unstaffed": 1,
    "by_type": [
      { "label_ar": "ملف النقل", "label_en": null, "total": 1, "active": 1 }
    ]
  },
  "reports": {
    "total": 300,
    "authors": 45,
    "series": [{ "day": "2026-07-01", "n": 12 }]
  },
  "ratings": {
    "count": 800,
    "rated_people": 300,
    "average": 4.1,
    "distribution": [
      { "stars": 1, "count": 10 },
      { "stars": 5, "count": 400 }
    ]
  }
}
```

> حساب غير فعّال أو موسم غير موجود ⇒ `{ "season": null }`.
> `reports.series`: آخر 30 يوماً بحسب **اليوم الذي يغطيه التقرير** لا يوم كتابته.

### 18.3 خمسة أقسام لحقت بالتطبيق (migration 0123)

آخر كتابة لـ `dashboard_stats` كانت **0085**، والتطبيق بلغ 0122. وفي ما بينهما
اكتسب سجلّ البلاغات العاجلة، ولوحة الحضور، ونظام المهام المُسندة، والتقييمات،
والشكاوى — خمسة أقسام عمل كاملة، لم تحمل اللوحةُ منها شيئاً.

وهذا ليس نقص ميزة بقدر ما هو **شاشة كفّت بهدوء عن أن تكون صحيحة**. اللوحة تُقرأ
ثقةً بها كما لا تُقرأ قائمة — فهي ما يُنظر إليه **بدلاً** من النظر في كل شيء —
فمن يقرأ عشر بطاقات ولا يجد بينها بطاقةً عن الطوارئ يخلص إلى أنه لا شيء يُعرف
عن الطوارئ. ولوحةٌ تُسقط نصف التطبيق وهي تبدو كاملة أسوأ من لوحةٍ لم تُبنَ.

**الصلاحية لكل قسم** — وتُطبَّق كما تُطبَّق العشرة قبلها: القسم **يغيب** عمّن لا
يملكها، ولا يُصفَّر. الصفر ادّعاءٌ عن البعثة («لا بلاغات»)، والغياب «ليس لك أن
تسأل»، وهما جملتان مختلفتان.

| القسم | الصلاحية | النطاق |
| --- | --- | --- |
| `incidents` | `incidents.receive` | **غير مقيَّد بالموسم** |
| `checkin` | `checkin.board` | الموسم |
| `tasks` | `tasks.view_all` | **غير مقيَّد بالموسم** |
| `evaluations` | `evaluations.view` | الموسم |
| `complaints` | `complaints.view` | **غير مقيَّد بالموسم** |

**ثلاثة منها لا يمكن تقييدها بموسم، ويجب أن تقول ذلك على البطاقة.** الجداول
الثلاثة لا تحمل `season_id` — ولا ينبغي أن تحمله: المهمة المُسندة إلى رجل تبقى
له حتى يُجاب عنها، والشكوى عن سلوك لا عن موسم، والطوارئ تخصّ الساعة التي وقعت
فيها. فتُحسب على **نافذة** بدلاً من ذلك (آخر ٣٠ يوماً بجوار مجموع كلّي)، وهو
الشكل نفسه الذي اختارته 0085 للإشعارات وللسبب نفسه. ورقمٌ يقع تحت مُنتقي المواسم
يُقرأ على أنه رقم ذلك الموسم، فترك التخطيط يوحي بما لا تسنده البيانات هو الخلل
عينه الذي أُضيف هذا القسم لتصحيحه — ولذلك تحمل بطاقات الثلاثة سطراً يقول
«لكل المواسم».

```json
{
  "incidents": {
    "total": 40, "open": 3, "in_progress": 2, "closed": 35, "recent": 9,
    "avg_minutes_to_handle": 12.5,
    "series": [{ "day": "2026-08-01", "n": 2 }]
  },
  "checkin": {
    "total": 900, "people": 120, "places": 14, "today": 31, "recent": 400,
    "series": [{ "day": "2026-08-01", "n": 31 }]
  },
  "tasks": {
    "total": 88, "open": 20, "late": 4, "awaiting_review": 6, "blocked": 1,
    "escalated": 2, "assignees": 17, "recent": 30,
    "by_state": [{ "key": "submitted", "count": 6 }],
    "by_priority": [{ "key": "high", "count": 5 }]
  },
  "evaluations": {
    "total": 25, "submitted": 18, "draft": 7, "late": 2, "evaluators": 9,
    "average_pct": 81.4,
    "by_target": [{ "key": "employee", "count": 20 }]
  },
  "complaints": {
    "total": 12, "open": 5, "locked": 3, "dismissed": 4, "recent": 2,
    "by_target": [{ "key": "employee", "count": 11 }]
  }
}
```

**أربع نقاط يلتزم بها أي backend بديل، وكلٌّ منها قرار لا تفصيلة:**

1. **`tasks` يعدّ المُسنَد وحده.** الشرط `created_by <> profile_id` مكرَّرٌ داخل
   الدالّة ولا يُورَث: الدالّة `security definer`، والسياسة التي تحرس `tasks.view_all`
   لا تسري داخلها. دفتر الرجل الخاصّ ليس في هذه الأرقام ولا يجوز أن يكون.
2. **`complaints` أعداد فقط، والتقسيم بـ«نوع الجهة» لا بالأسماء.** 0079 جعلت
   السجلّ سرّياً بنيةً — المشتكى عليه لا يعرف من اشتكى — واللوحة أسهل موضع في
   أي تطبيق لنقض ذلك سهواً. «إحدى عشرة عن موظفين» جملة لا تسمّي أحداً.
3. **`evaluations.average_pct` نسبةٌ لا متوسطَ درجات.** 0084 تُجمّد `score`
   بجانب `max_score` عمداً، لأن النموذج الذي كُسبت عليه سيُعدَّل، و«٣٨» بلا «من
   ٥٠» لا تعني شيئاً بعد سنة. جمعُ درجاتٍ من نماذج مختلفة جمعٌ لأشياء من مقامات
   مختلفة؛ النسبة وحدها تنجو منه. و`null` حين لا تسليم — لا صفر.
4. **`incidents.avg_minutes_to_handle` يقيس الغرفةَ لا البعثة**، وهو الرقم
   الوحيد على الصفحة كلها كذلك: كم جلس البلاغ قبل أن يتولّاه أحد. ممكنٌ فقط لأن
   0088 تختم `handled_at` على **أول** من يتولّاه ولا تكتب فوقها. و`null` حين لم
   يتولَّ أحدٌ شيئاً — لا صفر، فالصفر يُقرأ خدمةً فورية.

**`checkin.today` محسوب على الخادم** ولا يُقرأ من طرف `series`: السلسلة مقصوصة
على ثلاثين يوماً للرسم، وبطاقةٌ تأخذ رقمها الرئيسي من ذيل رسمٍ بيانيّ هي بطاقة
تنكسر يوم يقصّره أحد.

**لم يُنفَّذ هذا الـ migration على قاعدة حيّة بعد.**

---

## 19. الملفات والتخزين Storage

### 19.1 الحاويات (Buckets) وقواعد وصولها

| Bucket          | الخصوصية | المحتوى                  | نمط المسار                                                     | من يقرأ                              |
| --------------- | -------- | ------------------------ | -------------------------------------------------------------- | ------------------------------------ |
| `avatars`       | **عام**  | الصور الشخصية            | `{userId}/{name}`                                              | الجميع (URL عام دائم)                |
| `documents`     | خاص      | جواز/تأشيرة/بطاقة نسك    | `{userId}/{name}`                                              | صاحبها + حامل `employees.documents`  |
| `modules`       | خاص      | مرفقات الملفات وتقاريرها | `{moduleId}/{key}` و `{moduleId}/reports/{reportId}/{n}_{key}` | عضو الملف / حسب رؤية التقرير (§12.1) |
| `notifications` | خاص      | مرفقات الإشعارات         | `{groupId}/{i}_{key}`                                          | مستلم في المجموعة فقط                |
| `reports`       | خاص      | مرفقات التقارير المركزية | `{reportId}/{n}_{key}`                                         | حسب رؤية التقرير (§15.1)             |
| `travel`        | خاص      | كشوف الرحلات وتذاكرها     | `trips/{tripId}/{n}_{key}` و `legs/{legId}/{n}_{key}`          | راكب الرحلة + `travel.view` (§32.12) |

### 19.2 رفع ملف

```
POST /files/{bucket}                 (multipart/form-data)
```

| الجزء          | الوصف                      |
| -------------- | -------------------------- |
| `path`         | المسار الكامل داخل الحاوية |
| `file`         | المحتوى الثنائي            |
| `content_type` | اختياري                    |

**سلوك:** upsert (الكتابة فوق الموجود مسموحة). **الصلاحية:** حسب قواعد الحاوية
(الكتابة في `modules/…/reports/…` لعضو الملف؛ في `documents/{uid}` لصاحبها؛ إلخ).

**Response 201:** `{ "path": "..." }` — ولحاوية `avatars` أيضاً:
`{ "public_url": "https://cdn.../avatars/uid/photo.jpg" }`

#### 19.2.1 حدود الحجم (التزام على الطرفين)

الشبكة التي تُرفع عليها هذه الملفات هي شبكة المشاعر في أيام التشريق — أسوأ شبكة
في السنة تحمل أهم مرفقات السنة. فالحدّ ليس تحسيناً بل شرط وصول:

| الطرف   | الحدّ         | كيف يُطبَّق                                                                                   |
| ------- | ------------- | --------------------------------------------------------------------------------------------- |
| العميل  | **25 MB**     | يرفض الاختيار قبل بدء الرفع ويُظهر سبباً بالعربية (`attachmentTooLarge`)                        |
| الصور   | 2048px / q80  | يُعاد ترميزها على الجهاز قبل الرفع — لا تصل صورة أصلية من كاميرا الهاتف إلى الشبكة أبداً        |
| الخادم  | **50 MiB**    | حدّ الحاوية (`supabase/config.toml`). يجب أن يبقى **أعلى** من حدّ العميل لا مساوياً له           |

**التزام الخادم:** يردّ `413 Payload Too Large` مع `{"error": {"code": "file_too_large",
"max_bytes": 52428800}}` بدل خطأ عام — العميل يترجم الكود إلى جملة، ولا يستطيع
ترجمة نصٍّ إنجليزي جاء من الخادم.

**ملاحظة:** حدّ الصور مطبَّق على الجهاز فقط (`lib/core/attachments/attachment_picker.dart`)
لأن إعادة الترميز على الخادم تعني أن الملف قد عبر الشبكة أصلاً — وهو ما يُراد منعه.
الخادم لا يحتاج أن يعرف عنه شيئاً.

### 19.3 رابط موقّع (كل الحاويات الخاصة)

```
POST /files/sign
```

**Request:**

```json
{
  "bucket": "modules",
  "path": "moduleId/reports/reportId/0_photo.jpg",
  "expires_in": 600,
  "download": false,
  "download_name": "الاسم العربي.jpg"
}
```

**Response 200:** `{ "url": "https://...signed..." }`

**قواعد:**

- التوقيع **يمر بفحص الرؤية** — من لا يحق له قراءة الملف لا يحق له توقيعه (`403`).
- `download = true` ⇒ الرابط يقدّم الملف كتنزيل باسم `download_name`
  (header `Content-Disposition: attachment; filename*=UTF-8''...`) بدل عرضه.
- المدد المستخدمة في التطبيق: 600 ثانية (وثائق ومرفقات ملفات)، 3600 ثانية
  (إشعارات وتقارير).

### 19.4 حذف ملف

```
DELETE /files/{bucket}?path={storage path}
```

**Response:** `204`. (يستخدمه حذف مرفق تقرير الملف §12.3، وكنس صور البلاغ
المحذوف §30.7.)

**دلو `incidents` — من يحذف (0121):** كانت `incident_files_delete` لكاتب الملف
(أي المُبلِّغ) وللأدمن. وُسِّعت لحاملي `incidents.delete`، وإلّا لكان الشطب يترك
الصور خلفه: `delete_incident` تُرجع المسارات، ومن لا يستطيع مسحها من الدلو يُنتج
دلواً خاصاً يتراكم فيه ما لا يقرؤه أحد.

### 19.5 قاعدة مفاتيح التخزين (يلتزم بها الطرفان)

أسماء الملفات تصل **بالعربية** ومفتاح التخزين يجب أن يكون ASCII:

- يُشتق المفتاح بإبدال كل ما ليس `[A-Za-z0-9._-]` بـ `_`، ودمج التكرارات،
  وقص البادئات/اللواحق، وقصّ الأساس إلى 60 حرفاً، مع الإبقاء على الامتداد.
- إن أفنى التعقيم الاسم كلياً (اسم عربي صرف) يُستخدم fallback فريد داخل المجلد
  (فهرس رقمي أو مفتاح الحقل).
- **الاسم الأصلي يُخزَّن دائماً في صف قاعدة البيانات** (`name`) وهو ما يُعرض ويُنزَّل به.

---

## 20. البث اللحظي Realtime

التطبيق يستخدم Supabase Realtime **لتدفّق واحد فقط**: صندوق إشعارات المستخدم
(إعادة قراءة القائمة عند كل تغيير).

**قارئان لهذا التدفّق لا قارئ واحد (0121).** الصندوق نفسه هو الأول، و**إنذار
البلاغ العاجل** هو الثاني: يُصغي إلى التدفّق ذاته ويلتقط منه ما كان
`data.type == "incident"` ليُوقف التطبيق بنافذة وصوت (§30.8). لا يغيّر ذلك من
العقد شيئاً — التدفّق واحد والصفوف هي هي — لكنه يرفع كلفة سقوطه: socket ساقط كان
يعني «الصندوق يتأخّر حتى السحب للتحديث»، وصار يعني أيضاً «الإنذار يعتمد على
مسار الدفع وحده». ولذلك للإنذار مصدران لا مصدر واحد.

**البديل المطلوب — الحد الأدنى:**

```
WS /ws/notifications          (Authorization عبر query أو header)
```

- عند أي **INSERT / UPDATE / DELETE** على إشعارات المستخدم المتصل، يُرسل الخادم
  حدثاً (يكفي `{ "type": "changed" }` — التطبيق يعيد الجلب عبر §16.2).
- **الشبكة لا يوثق بها:** التطبيق يملك مساري fallback (fetch يدوي + سحب للتحديث)،
  فانقطاع الـ socket يجب ألا يفقد شيئاً — الحقيقة دائماً في `GET /notifications`.
- بديل مقبول إن تعذر WebSocket: **polling** بفاصل قصير، أو دفع FCM صامت
  (data-only) يحرّض إعادة الجلب.

---

## 21. منطق الخادم الإلزامي

> **هذا أهم قسم في المستند.** في Supabase تقوم RLS والـ triggers بعمل لا يظهر
> في أي استدعاء من التطبيق. عند الانتقال إلى REST **كل** ما يلي يجب أن يُعاد
> بناؤه في الـ backend وإلا انكسر النظام بصمت.

### 21.1 مصفوفة الرؤية (بديل RLS)

| المورد           | من يقرأ                                                                                         | من يكتب                             |
| ---------------- | ----------------------------------------------------------------------------------------------- | ----------------------------------- |
| profiles (الغير) | حامل `employees.view`؛ وكل حساب فعّال يرى **زملاءه في ملف مشترك** (دالة `shares_a_module_with`) | `employees.edit` للحقول الوصفية فقط |
| profile (نفسه)   | صاحبه دائماً                                                                                    | صاحبه (الحقول غير المحمية)          |
| modules          | §9.1                                                                                            | حسب صلاحيات modules.\*              |
| module_reports   | كاتبها، أو حامل `modules.reports`                                                               | كاتبها ضمن الفترة                   |
| module_ratings   | **صفوف المقيِّم نفسه فقط** — لا استعلام يعيد تقييمات الغير                                      | المقيِّم ضمن النافذة                |
| notifications    | `recipient_id = current_user` حصراً                                                             | الإدراج عبر مسارات الإرسال فقط      |
| reports          | §15.1                                                                                           | reports.\*                          |
| reference data   | حساب فعّال (+ `syrian_cities`، `job_titles`، `mission_types` لأي مُصادَق)                                       | reference.\*                        |
| seasons          | حساب فعّال                                                                                      | seasons.\*                          |
| device_tokens    | صاحبها فقط                                                                                      | صاحبها فقط                          |
| trips            | `travel.view`، أو راكبٌ عليها                                                                    | `travel.edit`؛ والحالة لـ`travel.confirm` |
| journey_legs     | `travel.view`، **أو صاحبها هو** (§32، بلا صلاحية)                                               | `travel.assign` / `travel.confirm`؛ وصاحبها عبر `confirm_leg` وحده |

### 21.2 الأعمدة المحمية في profiles

`is_admin`, `is_external`, `account_status`, `is_suspended`, `email`,
`rejection_reason` — لا تتغير إلا عبر مساراتها المخصصة (5.8، 5.9، 6.x) وبصلاحياتها.
أي `PUT /me/profile` يذكرها ⇒ تُتجاهَل أو `403`.

### 21.3 البريد الإلكتروني

`profiles.email` **مرآة** لبريد Auth (كان trigger `sync_profile_email`) —
يقرأ فقط، ويتحدث تلقائياً إن تغير بريد الدخول.

### 21.4 المتطلبات المسبقة للصلاحيات (كان triggers 0073)

- **منح** صلاحية دون أساسها المسجَّل في `permission_prerequisites` ⇒ `400 missing_prerequisite`.
- **سحب** صلاحية ⇒ سحب **كل** توابعها تلقائياً (cascade) في نفس العملية.

### 21.5 إشعارات الإسناد والتفعيل (كان triggers 0017/0024/0058)

يُنشأ إشعار (صف + push) تلقائياً عند:

1. **إضافة عضو** إلى ملف **مفعّل** (مباشرة أو على عقدة) — عنوانه إسناد،
   وdata تشير إلى الملف.
2. **تفعيل ملف** — إشعار لكل شخص معيَّن فيه سلفاً.

لا إشعار عند الإضافة لملف ما يزال مسودة (سيصلهم عند التفعيل).

### 21.5b قواعد السفر (كانت triggers 0129 → 0133)

أربع عشرة قاعدة، مجدولةً كاملةً في **§32.14**، ولا يُكتفى بذكرها هنا لأن ثلاثاً
منها تُكسَر بصمت إن أُهملت: **مصدر واحد للخطة** (BR-4)، و**حالة الرحلة لا تكتب
حالة الحركة** (BR-6)، و**إعادة الإسناد لا تحذف** (BR-5). وأخصّها بالذكر BR-6:
لا trigger ولا مهمة خلفية تحوّل هبوطَ طائرةٍ إلى وصولِ ركّابها.

### 21.6 منطق الموسم الحالي (كان RPC 0016)

- موسم واحد فقط `is_current` في كل لحظة.
- `ensure-current` لا يتقدم تلقائياً فوق اختيار يدوي مثبَّت لنفس السنة الهجرية
  (`pinned_for_hijri_year`).

### 21.7 حماية حذف العنصر المرجعي (كان trigger 0019/0024/0030)

حذف `reference_item` مرفوض (`400 reference_item_in_use`) إذا أشارت إليه:
عقدة ملف (بأي من المرجعين)، أو profile (المدينة)، أو عنصر مرجعي آخر
(حقل من نوع reference داخل `data`).

### 21.8 حُرّاس التعديل (كان triggers 0073)

- **modules:** تعديل `is_active` يتطلب `modules.activate` حصراً؛ باقي الحقول
  `modules.edit` — ولا يجوز خلطهما في طلب واحد بصلاحية واحدة.
- **reports:** تغيير `is_published` يتطلب `reports.publish`؛ باقي الحقول `reports.edit`.

### 21.9 قيود الفرادة (تُترجم إلى 409)

| الجدول              | القيد                                                      |
| ------------------- | ---------------------------------------------------------- |
| modules             | `(module_type_id, season_id)`                              |
| module_reports      | `(module_id, author_id, period_start)` — عبر upsert لا رفض |
| module_ratings      | `(module_id, rater_id, ratee_id)` — upsert                 |
| season_participants | `(season_id, profile_id)` — upsert                         |
| device_tokens       | `(user_id, token)` — upsert                                |
| user_permissions    | `(user_id, permission_id)`                                 |

### 21.10 حساب فترة تقرير الملف

`period_start` يُحسب على الخادم من `report_cadence` وتوقيت الخادم
(اليوم لليومي، بداية الأسبوع للأسبوعي) — **لا يُستقبَل من العميل أبداً**.

### 21.11 المطابقة العربية المطوَّعة (بحث 9.10)

توحيد قبل المقارنة على **الطرفين**: `أ إ آ ← ا`، `ى ← ي`، `ة ← ه`، حذف التشكيل،
وإغلاق المسافة في أسماء «عبد …» المركّبة (`عبد الله ← عبدالله` — migration 0128) —
بحيث يجد "احمد" "أحمد" ويجد "عبدالله" "عبد الله". (كانت دالة SQL `ar_fold`.)

---

## 22. قاموس الأخطاء

| `error.code`            | HTTP | السياق                                              |
| ----------------------- | ---- | --------------------------------------------------- |
| `invalid_credentials`   | 400  | دخول خاطئ                                           |
| `email_exists`          | 409  | تسجيل ببريد مستخدم                                  |
| `weak_password`         | 422  | كلمة سر قصيرة/ضعيفة                                 |
| `invalid_refresh_token` | 401  | توكن تجديد ملغى/منتهٍ — التطبيق يحذف الحساب المحفوظ |
| `unauthorized`          | 401  | لا توكن                                             |
| `forbidden`             | 403  | صلاحية ناقصة أو حساب موقوف/غير معتمد                |
| `not_found`             | 404  | غير موجود أو غير مرئي                               |
| `cannot_delete_self`    | 400  | حذف النفس (5.6)                                     |
| `cannot_delete_admin`   | 400  | حذف أدمن (5.6)                                      |
| `missing_prerequisite`  | 400  | منح صلاحية دون أساسها (4.5)                         |
| `reference_item_in_use` | 400  | حذف عنصر مرجعي مستخدم (14.6)                        |
| `duplicate_module`      | 409  | نوع مكرر في موسم (9.6)                              |
| `rating_closed`         | 400  | تقييم خارج النافذة (13.2)                           |
| `invalid_topic`         | 400  | اسم topic غير صالح (17.3)                           |
| `report_once_per_season` | 409 | تقرير ثانٍ لنوع يُقدَّم مرة واحدة في الموسم (15)     |
| `complaint_target_missing` | 400 | شكوى على نوع بلا تحديد ما هي عليه (24.1)          |
| `complaint_target_wrong_set` | 400 | عنصر مرجعي من مجموعة غير التي أُعلنت (24.1)     |
| `complaint_is_immutable` | 400 | محاولة تغيير هدف شكوى أو مقدِّمها بعد إنشائها (24.1) |
| `complaint_locked`      | 400  | ردّ على نقاش أُغلق (24.7)                           |
| `complaint_body_required` | 400 | ردّ فارغ (24.7)                                    |
| `complaint_not_found`   | 404  | شكوى غير موجودة أو القارئ ليس طرفاً فيها (24.5)     |
| `task_not_found`        | 404  | مهمة غير موجودة أو غير مرئية للقارئ (25.2)          |
| `task_title_required`   | 422  | مهمة بلا عنوان (25.2)                               |
| `task_transition_not_allowed` | 400 | حركة خارج مصفوفة دورة الحياة (25.5)            |
| `task_comment_required` | 422  | `blocked` أو `returned` أو تعليق بلا نصّ (25.5)      |
| `task_assignee_not_found` | 404 | إعادة إسناد إلى شخص غير موجود (25.2)               |
| `task_cannot_reassign_to_author` | 400 | إعادة إسناد المهمة إلى كاتبها — تصير دفتراً خاصاً وتختفي من المتابعة (25.2) |

---

## 23. سجل الأحداث Audit Log

> أُضيف في migration 0077. **مصدر الكتابة الوحيد هو الخادم**: في Supabase الحالي
> يكتب trigger عام (`audit_row_change`) على كل جداول القاعدة التشغيلية سطراً لكل
> INSERT/UPDATE/DELETE مع صورة الصف قبل وبعد وقائمة الحقول المتغيّرة. أي backend
> بديل **ملزم** بنفس المبدأ: التسجيل في طبقة البيانات لا في التطبيق، ولا يوجد أي
> مسار كتابة من العميل.

### 23.1 الكيان `AuditEvent`

```json
{
  "id": 12345,
  "occurred_at": "2026-08-01T09:15:00Z",
  "actor_id": "uuid | null",
  "actor_name": "أحمد محمد الخطيب",
  "actor_photo_url": "https://... | null",
  "action": "insert | update | delete | login | logout",
  "table_name": "profiles",
  "record_id": "uuid | null",
  "record_label": "أحمد محمد الخطيب",
  "old_data": { "...": "الصف كاملاً قبل — للتعديل والحذف" },
  "new_data": { "...": "الصف كاملاً بعد — للإضافة والتعديل" },
  "changed_fields": ["phone_sa", "job_title_id"]
}
```

- `actor_name` لقطة تُحفَظ وقت الحدث وتبقى بعد حذف الحساب؛ عند العرض يُفضَّل
  الاسم الحالي إن كان الحساب ما يزال موجوداً.
- `record_label` تسمية بشرية للسجل تُحسب وقت الكتابة (اسم الموظف، نوع الملف،
  عنوان التقرير…) لتبقى بعد حذف السجل نفسه.
- `table_name` تحمل قيمتين زائفتين إضافيتين: `auth` (الدخول/الخروج وأفعال
  الحسابات) و `storage` (رفع/حذف الملفات).
- تعديل تقرير عبر `save_report` يظهر سطراً واحداً على `reports` مع
  `changed_fields: ["content"]` عندما لا يتغير غير الصفوف/البلوكات
  (لا تُسجَّل `report_rows`/`report_blocks` صفاً صفاً). البث الإشعاري يظهر
  سطراً واحداً مع `new_data.recipients` = عدد المستلمين. `device_tokens`
  لا تُسجَّل إطلاقاً.
- **فخّ يستحقّ النصّ (0121 + 0122):** 0077 علّقت الـ trigger على كل جدولٍ كان
  موجوداً **يوم نفاذها**؛ أي جدول يُنشأ بعدها يبدأ **غير مسجَّل** ما لم تعلّقه
  هجرته بنفسها. أربع هجرات لم تقرأ الملاحظة، وبقيت جداولها خارج السجلّ من يوم
  إنشائها:

  | الجدول | الهجرة | أُغلق في | ما كان يضيع |
  | --- | --- | --- | --- |
  | `incidents` / `incident_attachments` | 0088 | **0121** | كل تغيير حالة، وكل حذف بعد أن صار للسجلّ حذف |
  | `place_check_ins` | 0098 | **0122** | **سجلّ الحضور نفسه** — تعديل صفٍّ أو حذفه بلا أثر لفاعله |
  | `report_misses` | 0086 | **0122** | ما رصده التصعيد الليلي متأخراً، وإلى أي درجة حمله |
  | `module_type_teams` | 0115 | **0122** | بيانات مرجعية، وأخواتها `module_type_*` كلها مسجَّلة منذ 0077 |

  أخطرها `place_check_ins`: سجلّ حضورٍ يمكن **تصحيحه بهدوء** ليس سجلّ حضور —
  قيمة «سجّل وصوله في منى الساعة ١٤:٢٠» كلها في أنها لم تُكتب لاحقاً.

  وأُضيفت مع كلٍّ منها حالتُه في `record_label`: صفوف `incidents` و
  `place_check_ins` و`report_misses` لا تحمل أياً من الأعمدة الستّة التي ترجع
  إليها الدالّة، فكانت سطورها تُكتب **معرّفاً عارياً** — صارت أول ٨٠ حرفاً من
  البلاغ، و«الاسم — المكان»، و«الاسم — الفترة» على التوالي.

  أي backend بديل: **التسجيل افتراضٌ لا اشتراك**، وجدولٌ جديد يخرج منه هو خطأ
  لا خيار. ويحرس هذا الآن `test/audit_coverage_test.dart`، الذي يقرأ الـ
  migrations نفسها ويرفض أي جدول مسجَّل لا يبلغه فلتر الشاشة.
- **الفلتر جزء من العقد، لا زينة (0122).** جدولٌ يُسجَّل ولا يعرف الفلتر اسمه
  تظهر صفوفه في القائمة غير المفلترة **بلا تسمية** — يُرسم `evaluation_answers`
  بالإنجليزية وسط جُمل عربية — ولا سبيل إلى الوصول إليها. وكان في هذه الحال:
  قسمُ **التقييمات** الستّة كلّه منذ 0084، وأربعةُ جداول من خيط المهمة الذي بنته
  0117/0118، ورموزُ الأماكن وسياستها. صارت لها أقسامها وتسمياتها.

### 23.2 `GET /audit-events` — قراءة السجل

**كان:** RPC `audit_events`. **الصلاحية:** `audit.view` (أو أدمن).

| Query param | النوع        | المعنى                                            |
| ----------- | ------------ | ------------------------------------------------- |
| `limit`     | int (≤200)   | حجم الصفحة، افتراضي 50                            |
| `before_id` | bigint       | keyset pagination: أعد ما هو أقدم من هذا المعرّف |
| `actor_id`  | uuid         | حسب الفاعل                                        |
| `actions`   | csv          | `insert,update,...`                               |
| `tables`    | csv          | أسماء جداول (يُرسل التطبيق مجموعة القسم المختار)  |
| `from`,`to` | ISO-8601     | حدود زمنية (`to` حصرية)                           |
| `q`         | string       | بحث مطوَّع عربياً في `record_label` و `actor_name` |
| `season_id` | uuid         | حسب الموسم (0116)                                 |
| `seasonless`| bool         | الأسطر التي **لا موسم لها**. تغلب `season_id`     |

⇒ `200` مصفوفة `AuditEvent` مرتبة `id desc`، ولكل سطر `season_id` منذ 0116.

#### 23.2a الموسم — ثلاث حالات لا اثنتان (migration 0116)

`audit_log.season_id` **يُختم وقت الكتابة** من الصف المُدقَّق، لا يُستنتج من مدى
تواريخ لاحقاً. والتقريب بالتاريخ هو ما يستبدله هذا: موسمان يتداخلان بيوم واحد
جوابان مختلفان، وأعمال ١٤٤٧ تجري بينما يُحضَّر ١٤٤٨ إلى جانبها.

سُلَّم الاشتقاق، بالترتيب، في `audit_season_of(table, data)`:

1. الصف يحمل `season_id` بنفسه — `modules`, `reports`, `place_check_ins`,
   `evaluations`, `season_participants`.
2. جدول `seasons` نفسه: الموسم هو **مُعرّف الصف**، فيدخل إنشاءُ ١٤٤٨ ضمن ١٤٤٨.
3. الصف يحمل `module_id` ⇒ موسم الملف. **هذه الدرجة هي المهمة**:
   `module_nodes` و`module_members` و`module_tasks` و`module_reports` لا موسم
   لها، وبناء الملف هو بالضبط ما يبحث عنه من يفلتر بالموسم.
4. الصف يحمل `node_id` ⇒ موسم الملف عبر العقدة (`module_node_members`).
5. وإلا `null`.

والدالة **لا ترفع استثناءً أبداً** (`when others then return null`): تعمل داخل
مُشغِّل كل جدول مُدقَّق، ومُعرّفٌ مشوَّه أو صفٌّ ذهب يجب ألا يُبطل التعديل الذي
وُجدت لتسجيله — سطرٌ بلا ختم خيرٌ من حفظٍ مرفوض.

**الحالات الثلاث إلزامية:** موسم بعينه / **بلا موسم** / الكل (الافتراضي). و«بلا
موسم» ليست بقايا: الحسابات والصلاحيات والبيانات الرئيسية ورموز الأماكن هي أعمالٌ
تتجاوز المواسم، وهي **غالب** السجل. فمرشّحٌ لا يستطيع إلا التضييق **إلى** موسم
يُخفي أكثر السجل لحظة لمسه.

> **عند إعادة البناء:** أضف المعاملين إلى **`audit-events` و`audit-summary`
> معاً وبالتهجئة نفسها.** قاعدة 0111: ترويسةٌ تعدّ مجموعة أوسع من القائمة تحتها
> ترويسةٌ تخالف صفحتها.

### 23.3 `GET /audit-actors` — قائمة الفاعلين

**كان:** RPC `audit_actors`. **الصلاحية:** `audit.view`.
⇒ `[{ "actor_id": "uuid", "actor_name": "...", "photo_url": "... | null" }]`
مميّزة ومرتبة بالاسم — خيارات فلتر "الشخص".

### 23.3a `GET /audit-seasons` — قائمة المواسم

**كان:** RPC `audit_seasons` (0116). **الصلاحية:** `audit.view`.
⇒ `[{ "season_id": "uuid", "hijri_year": 1448, "n": 120 }]` مرتبة بالسنة تنازلياً.

المواسم التي **يحمل السجل أسطراً لها فقط**: 0109 يقلّم السجل إلى سنتين، وقائمةٌ
مبنيةٌ على `seasons` وحدها تعرض ١٤٤٥ ثم تجيب «لا شيء» — وخيارٌ جوابه الوحيد صفحةٌ
فارغة ليس خياراً.

### 23.4 `POST /audit-events/auth` — دخول/خروج

**كان:** RPC `log_auth_event(p_event)`. يستدعيه التطبيق بعد كل دخول ناجح
(بكلمة السر، Google، تبديل حساب) وقبل الخروج. الجسم: `{ "event": "login" | "logout" }`.
Best-effort في التطبيق: فشله لا يفشل الدخول. حساب جديد لم يُكمل ملفه بعد
يُسجَّل بالبريد الإلكتروني بدل الاسم.

### 23.5 التزامات الخادم (بديل Triggers 0077)

1. **سطر لكل كتابة** على كل الجداول التشغيلية أياً كان مسارها، مع استثناءات
   §23.1 (الإشعارات سطر واحد للبث، صفوف التقارير عبر رأس التقرير،
   `device_tokens` مستثناة، تعديلات `updated_at` وحدها لا تُسجَّل).
2. **أفعال الحسابات الإدارية** (إنشاء/حذف حساب، إعادة تعيين كلمة سر، تغيير
   بريد — §5.1/5.6/5.7/5.7b) تكتب سطرها بنفسها ناسبةً الفعل لمستدعيه، لأنها
   تجري بصلاحيات النظام؛ `new_data.op` تحمل:
   `create_user | delete_user | set_password | set_email`
   (كلمة السر لا تُسجَّل أبداً — الفعل فقط).
3. **رفع/حذف الملفات** في التخزين يُسجَّل بسطر `table_name: "storage"` يحمل
   `bucket` و `path`.
4. السجل **قراءة فقط** للعميل، ولا يُعدَّل ولا يُحذف منه شيء عبر أي API.

### 23.6 الاحتفاظ — السجل ينسى ما قبل الموسم الماضي (migration 0109)

0077 وضع trigger على **كل** الجداول ولم يضع سقفاً لما يكتبه. وهذا صحيح لما هو
عليه — سجلٌّ لا يقرؤه أحد حتى يقع نزاع يجب أن يكون قد حفظ الصفّ طوال الوقت،
وسجلٌّ فيه ثغرات لا يجيب عن شيء — لكن جدولاً ينمو بلا توقّف قرارٌ **مؤجَّل لا
متجنَّب**. كل تعديل ملف شخصي، وكل إسناد، وكل إعادة تسمية مدخل مرجعي، بـ
`old_data` و`new_data` معاً، ما دام المشروع يعمل.

| ما يُحذف | بعد | لماذا |
| --- | --- | --- |
| كل شيء | **800 يوم** | سنتان هجريتان (٧٠٩ أيام) وأكثر من ثلثَي ثالثة. السؤال الذي يجيب عنه السجل — «من غيّر هذا ومتى» — يُطرح داخل الموسم أو أثناء ترتيب الذي يليه؛ وبحلول الثالث تصير الملفات نفسها هي السجل |
| `login` / `logout` | **1165 يوماً** | أصغر الصفوف (بلا `old_data` ولا `new_data`)، فحفظها شبه مجاني — وهي الأرجح طلباً متأخراً: «هل كان هذا الحساب يُستعمل في الأسابيع قبل إيقافه؟» سؤالٌ يُطرح بعد وقت طويل ولا يجيب عنه شيء آخر |

**والقياس بالأيام لا بالمواسم عمداً**: السجل ليس مقيّداً بموسم ولا يجب أن يكون —
فيه تسجيلات دخول وصفوف من جداول لا تخصّ سنة.

**التزامات الخادم:**

1. **الحذف على دفعات** (١٠ آلاف)، لا في عبارة واحدة. عبارةٌ واحدة على سنتين من
   جدول بهذا العرض تأخذ قفلاً ومعاملةً بحجم يُلاحَظ على مشروعٍ كتّابُه موظفون
   ميدانيون يضغطون «حفظ» — والغرض من تشغيلها ٣:٢٠ ليلاً ألا يلاحظها أحد.
2. **الكنس يكتب سطره في السجل نفسه.** سجلٌّ يُسقط صفوفاً بصمت أسوأ من سجلٍّ
   ينمو، لأن الفجوة لا تُميَّز عندئذٍ عن «لم يحدث شيء». السطر يحمل العدد والنافذة.
3. **الدالة لا ينفّذها أحد يستطيع التطبيق الدخول به.** أشدّ من 0086: هذه تحذف
   تاريخاً، وحسابٌ يستطيع نداءها بـ `p_keep_days => 0` يمحو أثر ما فعله للتوّ.
4. **لا كنس عند الترحيل.** الملف يجدول ولا ينفّذ — ترحيلٌ يحذف سنتين لحظة
   تطبيقه يفعل الشيء الوحيد الذي يحترز منه، وداخل معاملة ترحيل، على جدول قد
   يكون ضخماً، في أي يوم صادف النشر. أول كنسة قرارٌ يدوي: `select prune_audit_log();`

### 23.7 `GET /audit-summary` — السجل يصف نفسه (migration 0111)

**كان:** RPC `audit_summary`. **الصلاحية:** `audit.view` (أو أدمن) — نفس باب
§23.2، لأن عدّ صفوف لا يجوز للقارئ قراءتها إخبارٌ بما فيها.

`audit_events` يعطي خمسين سطراً في المرة، مرقّمة بالمفتاح، الأحدث أولاً. هذا هو
الشكل الصحيح لقراءة سجلّ سطراً سطراً، والخاطئ لكل سؤال عن السجلّ **نفسه**: هل
كان الثلاثاء الماضي أكثر عملاً من الذي قبله، هل الحذف من شخص واحد أم من الجميع،
هل كان الأسبوع الهادئ هادئاً أم هو الأسبوع الذي توقّف فيه الـ trigger.

**والعميل ممنوع من رسم ذلك من الصفحة التي بين يديه**: خمسون صفاً ليست عيّنة من
السجل بل طرفه الأحدث، ومخطّطٌ مبنيّ عليها يقول «النشاط ينخفض» كلما مرّر القارئ
إلى الوراء بما يكفي — ويقولها بنبرة الصورة الواثقة. العدّ يجري حيث الصفوف.

| Query param | النوع    | المعنى                                                |
| ----------- | -------- | ----------------------------------------------------- |
| `actor_id`  | uuid     | حسب الفاعل                                            |
| `actions`   | csv      | `insert,update,...`                                   |
| `tables`    | csv      | أسماء جداول (مجموعة القسم المختار)                    |
| `from`,`to` | ISO-8601 | حدود النافذة (`to` حصرية)                             |
| `q`         | string   | بحث مطوَّع عربياً في `record_label` و `actor_name`     |
| `days`      | int      | عرض النافذة الافتراضية حين لا يُرسل `from`، افتراضي 30 |
| `season_id` | uuid     | حسب الموسم (0116) — **بالتهجئة نفسها التي في §23.2**   |
| `seasonless`| bool     | الأسطر التي لا موسم لها. تغلب `season_id`              |

```json
{
  "from": "2026-07-11T00:00:00Z",
  "to": "2026-08-09T21:00:00Z",
  "bucket": "day | week | month",
  "total": 1284,
  "actors": 17,
  "series":    [ { "day": "2026-07-11T00:00:00Z", "n": 42 } ],
  "by_action": [ { "key": "update", "n": 903 } ]
}
```

**التزامات الخادم:**

1. **نفس المرشِّحات حرفياً** كـ §23.2 عدا `limit` و `before_id` (وهما عن
   الترقيم لا عن أيّ الأحداث موجود). رأسٌ يعدّ مجموعة أوسع من القائمة تحته رأسٌ
   يخالف الصفحة التي يجلس عليها، ولا سبيل للقارئ أن يعرف أيّهما يكذب.
2. **`to` حصرية في الاثنين**، فيتّفقان عند الحدّ بدل أن يختلفا بيوم كامل.
3. **النافذة** هي مدى القارئ حين يحدّده، وآخر `days` يوماً حين لا يحدّده — ولذلك
   تعود في الجواب وتُقال فوق المخطّط بالكلمات، لأن معناه يختلف بينهما.
4. **`bucket` يُختار من عرض النافذة ويعود مع الجواب**: يوم حتى ٦٢ يوماً، ثم
   أسبوع حتى ٤٠٠، ثم شهر. مخطّطٌ نقاطه أسابيع وعناوينه أيام أسوأ من لا مخطّط.
5. **السلسلة مسدودة الفجوات** (`generate_series` + `left join`): يومٌ لم يقع فيه
   شيء يوم عليه صفر لا يوم غائب عن المحور — وخطٌّ يُرسم من الاثنين إلى الخميس
   فوق ثلاثاء وأربعاء غائبين يخترع حركةً في الأيام التي هدأت فيها البعثة.
6. **`actors` أشخاص متمايزون، والنظام ليس شخصاً**: صفوف الـ triggers بلا فاعل،
   وعدّها واحداً يضخّم «من كان يعمل» بواحد دائماً.
7. **فهرس على `occurred_at desc`.** §23.2 يقرأ بالمعرّف ويخدمه المفتاح الأساسي؛
   هذه تقرأ بالزمن وكانت ستمسح جدولاً يحدّه 0109 بسنتين من كل كتابة في المشروع.
8. **فشلها ليس فشل الشاشة.** التطبيق يطلبها بعد الصفحة وعلى مسار خطأ مستقلّ:
   القائمة هي ما فُتحت الشاشة لأجله، والرأس فوقها، وسقوط أحدهما لا يُسقط الآخر.

---

## 24. الشكاوى Complaints

> أُضيف في migrations 0079 و 0080. **هذا القسم أخطر ما في المستند**: فيه قاعدة
> تُوقف حساباً بلا تدخّل بشري، وفيه سرّية لا يجوز للتطبيق أن يكون هو من يحرسها.
> كل ما تحت §24.9 «التزامات الخادم» ملزم حرفياً لأي backend بديل.

### 24.1 الكيان `Complaint`

```json
{
  "id": "uuid",
  "created_at": "2026-08-02T09:15:00Z",
  "target_type": "employee | module | report | hotel | cluster | group | other",
  "target_id": "uuid | null",
  "target_label": "أحمد محمد الخطيب",
  "complainant_id": "uuid | null",
  "complainant_name": "string | null",
  "complainant_photo_url": "string | null",
  "body": "نص الشكوى",
  "is_locked": false,
  "is_dismissed": false,
  "reply_count": 3,
  "attachment_count": 2,
  "my_role": "complainant | accused | manager"
}
```

- `target_label` **لقطة** تُكتب وقت التقديم (كما `audit_log.actor_name`)، فتبقى
  الشكوى مقروءة بعد حذف الفندق أو الموظف الذي قُدِّمت عليه.
- الهدف يُخزَّن في القاعدة كأربعة مفاتيح أجنبية اختيارية تحت enum واحد
  (`target_profile_id` / `target_module_id` / `target_report_id` /
  `target_item_id`) مع قيد يفرض «واحد فقط ويطابق النوع». `target_id` في الـ JSON
  هو `coalesce` عليها.
- `hotel` و `cluster` و `group` كلها صفوف في `reference_items`، ويميّزها
  `reference_sets.code`. الخادم **ملزم** بالتحقق أن المجموعة تطابق النوع
  المُعلَن (`complaint_target_wrong_set`).
- **`complainant_*` تكون `null` حين لا يجوز للقارئ أن يعرف.** انظر §24.9.

### 24.2 الكيان `ComplaintMessage` (فقاعة في النقاش)

```json
{
  "reply_id": "uuid | null",
  "created_at": "2026-08-02T10:00:00Z",
  "body": "نص",
  "author_id": "uuid | null",
  "author_name": "string | null",
  "author_photo_url": "string | null",
  "author_role": "complainant | accused | manager",
  "is_mine": false,
  "attachments": [ { "...": "Attachment" } ]
}
```

`reply_id = null` هي الشكوى نفسها — رأس النقاش. `author_role` يعود **دائماً**
حتى حين تُحجب الهوية، ليُسمّى الطرف بلا تسمية الشخص.

### 24.3 `GET /complaints` — السجل

**كان:** RPC `complaints_list`.
**الصلاحية:** لا شيء لـ `scope=mine`؛ `complaints.view` لـ `scope=all`.

| Query param | النوع | المعنى |
| --- | --- | --- |
| `scope` | `mine \| all` | ما قدّمتُه أنا، أو كل شيء |
| `target_type` | enum? | تضييق بالنوع |
| `target_id` | uuid? | شكاوى شيء بعينه (أُضيف في 0080) |
| `include_dismissed` | bool | الافتراضي true |
| `query` | string? | بحث عربي مطبَّع على `target_label` و `body` |
| `limit` / `before` | int / timestamptz | ترقيم keyset، سقف 200 |

⇒ `200` مصفوفة `Complaint` مرتبة `created_at desc`.
**لا يُرجع أبداً** صفاً يكون فيه القارئ هو المشتكى عليه — ذلك عمل §24.4 وحده.

### 24.4 `GET /me/complaints-against` — ما قُدِّم ضدي

**كان:** RPC `complaints_against_me`. **الصلاحية:** حساب مسجَّل فقط.

بلا أي معامل يحدد الشخص، عمداً: لا أحد آخر يمكن سؤالها عنه. ⇒ `200` مصفوفة
`{ id, created_at, body, is_locked, is_dismissed, reply_count, attachments }`
— **بلا أي عمود هوية إطلاقاً**.

> **ملزم:** لا تُشترط حالة «معتمد» هنا. الإيقاف الذي تفرضه هذه الميزة يُسقط
> `is_approved()`، ومن لا يقرأ ما أوقفه لا يستطيع الدفاع عن نفسه.

### 24.5 `GET /complaints/{id}/thread` — النقاش

**كان:** RPC `complaint_thread`. ⇒ `200` مصفوفة `ComplaintMessage` مرتبة زمنياً،
رأس النقاش أولاً. `404 complaint_not_found`، `403` لمن ليس طرفاً.

### 24.6 `GET /profiles/{id}/complaint-standing` — الأرقام

**كان:** RPC `complaints_against`. **الصلاحية:** صاحب الملف أو `complaints.view`.

⇒ `{ distinct_complainants, open_complaints, dismissed_complaints,
is_auto_suspended, forgiven_count }` — **أرقام فقط، ولا اسم واحد**.

### 24.6b `GET /complaints/targets` — ما يمكن الاشتكاء عليه

**كان:** RPC `complaint_targets` (0082). **الصلاحية:** من يملك تقديم شكوى — أي
حساب معتمد، ولا شيء غير ذلك.

| Query param | النوع | المعنى |
| --- | --- | --- |
| `target_type` | enum | `employee \| module \| report \| hotel \| cluster \| group` |
| `query` | string? | بحث بالاسم |
| `limit` | int | الافتراضي 100، سقف 200 |

⇒ `200` مصفوفة `{ id, name, photo_url }` — **ثلاثة أعمدة فقط**.

> **ملزم:** هذا المسار **لا يفحص** `employees.view` ولا `modules.view_all` ولا
> أي صلاحية عرض. تقديم الشكوى بلا صلاحية عمداً، فلو حجبت قائمة الأسماء خلف
> صلاحية الدليل لصار الحق قائماً بلا وسيلة لاستعماله: يفتح الموظف النموذج فلا
> يجد من يسمّيه. وبالمقابل **لا يُوسَّع** ما يُرجَع: اسم ومعرّف وصورة، لا هاتف
> ولا وثيقة ولا حالة حساب — الدليل يبقى خلف صلاحيته.
>
> ما يظهر في كل قائمة: الموظفون المعتمدون عدا نفسه؛ الملفات **المفعّلة** فقط
> (المسودة لم تُسلَّم لأحد)؛ التقارير المنشورة ومسوداتُه هو؛ عناصر المرجع
> الفعّالة. و `other` بلا قائمة أصلاً.
>
> الاسم يأتي من `audit_record_label` نفسها التي تكتب `target_label` على الشكوى،
> فيكون المعروض في القائمة هو المحفوظ على الشكوى — لا اسمان لشيء واحد.

### 24.7 الكتابة

| Endpoint | كان | الصلاحية |
| --- | --- | --- |
| `POST /complaints` ⇒ `{id}` | RPC `file_complaint` | أي حساب معتمد |
| `POST /complaints/{id}/replies` ⇒ `{id}` | RPC `reply_to_complaint` | طرف في النقاش، أو `complaints.reply` |
| `PATCH /complaints/{id}/lock` | RPC `set_complaint_lock` | `complaints.lock` |
| `PATCH /complaints/{id}/dismiss` | RPC `set_complaint_dismissed` | `complaints.dismiss` |
| `DELETE /complaints/{id}` | RLS | `complaints.delete`، أو صاحبها ما لم يُردّ عليها |

**ترتيب إلزامي للمرفقات:** إنشاء الصف ← أخذ الـ id ← رفع الملفات تحته ← تسجيل
صفوف المرفقات. الرفع قبل وجود الصف مرفوض، لأن قاعدة التخزين تبحث عن الشكوى
بالمجلد الأول من المسار.

### 24.8 التخزين

دلو خاص `complaints`. المسار:
`{complaint_id}/{i}_{file}` و `{complaint_id}/replies/{reply_id}/{i}_{file}`
— معرّف الردّ **يتداخل تحت** معرّف الشكوى ليكفي مسندُ قراءة واحد.
**ولا يُوضع معرّف المشتكي في المسار أبداً.**

### 24.9 التزامات الخادم (ملزمة حرفياً)

1. **الإيقاف التلقائي.** إذا بلغ عدد **المشتكين المختلفين** (لا عدد الشكاوى)
   بشكاوى غير مرفوضة على موظف واحد **3**، يُوقَف حسابه في نفس معاملة التقديم.
   النطاق: كل التاريخ. يجب أن يُعاد الحساب أيضاً عند الحذف وعند رفع/إعادة
   الرفض، وتحت قفل يمنع شكويين متزامنتين من قراءة «اثنان» كلتيهما.
2. **إيقاف آلي واحد فقط يُرفع آلياً.** يُوسم الإيقاف الآلي
   (`auto_suspended_at`)؛ ما قرّره إنسان لا تمسّه القاعدة أبداً.
3. **الرفع اليدوي يسامح.** حين يرفع إنسانٌ إيقافاً، يُحفظ عدد المشتكين وقتها
   (`auto_suspend_forgiven_count`) ولا تُعيد القاعدة الإيقاف إلا بتجاوز هذا
   العدد. بدون ذلك تُعيد أولُ شكوى تالية الإيقافَ فوق قرار المدير خلال ثوانٍ.
4. **حراسة الأعمدة.** `is_suspended` و `auto_suspended_at` و
   `auto_suspend_forgiven_count` لا يكتبها إلا حاملُ `employees.suspend`، أو
   القاعدةُ نفسها من داخل معاملتها. في Supabase يتحقق ذلك بعلَم معاملة **مع**
   `pg_trigger_depth() > 1` **مع** حصر الاستثناء بعمودين — الثلاثة معاً، لأن
   أياً منها وحده قابل للتحايل.
5. **السرّية بنيوية لا تجميلية.** المشتكى عليه لا يُمنح قراءة الصف أصلاً؛ يقرأ
   عبر §24.4 و §24.5 فقط. ويشمل الحجب **ردود المشتكي داخل النقاش** و **صورته**
   كما يشمل اسمه.
6. **المدير ليس مديراً لقضيته.** من يحمل `complaints.view` لا يرى الشكوى
   المقدَّمة عليه هو — وإلا صارت الصلاحية باباً لكشف الهوية.
7. **السجل لا يفشي.** تُسقَط `complainant_id` و `author_id` و `body` من
   `audit_log`، ويُترك الفاعل فارغاً عند التقديم والردّ (الرفض والقفل والحذف
   تُنسب لفاعلها). والإيقاف الآلي يُسجَّل بلا فاعل، وإلا سمّى المشتكي الثالث.
8. **الرافع لا يُسمّى في التخزين.** يجب تصفير مالك الكائن في دلو `complaints`،
   وإلا سلّم صفُّ الكائن هويةَ المشتكي لمن يقرأ المرفق.
9. **الإشعار لا يُسمّي.** إشعار «قُدِّمت شكوى بحقك» يُكتب بمرسِل فارغ وبنص خالٍ
   من الأسماء، وكذلك حمولة الـ push.
10. **`complaints.dismiss` تستلزم `employees.suspend`.** الرفض يرفع إيقافاً،
    فمن يرفض يمسك مفتاح الإيقاف؛ ورقة المنح يجب أن تقول ذلك.

---

## 25. المهام Tasks — قوائم وصفية على الملف، ونظام شخصي مستقل

> **migration 0105 (تنقض معظم 0083).** التجربة أظهرت شيئين: لا أحد يكتب
> كتالوجاً — المهام تُكتب يدوياً في الموسم على الملف نفسه — والملف لا يريد
> متتبِّعاً: قوائمه تقول ما **هو** العمل، وأعضاؤه يتقاسمونه بينهم خارج
> التطبيق. أما ما يتتبعه الناس فعلاً فهو قائمتهم الخاصة، فانتقل التتبع إلى
> حيث الشخص — كما انتقل تسجيل الوصول إلى حيث المكان (0098).
>
> **حُذف بالكامل:** كتالوج النوع (`module_type_tasks`،
> `module_type_task_groups`)، قوائم الاختيار (`module_assigned_tasks`،
> عمود `tasks_are_assigned`)، جدولا الحالة والأدلة (`module_task_status`،
> `module_task_attachments`)، الدالتان `module_task_board` و
> `set_module_task_state` وكل مساعديهما، ومسارات `{module_id}/tasks/…` في دلو
> `modules` مع سياساتها.

### 25.1 قوائم الملف الوصفية (`module_tasks`)

مهمة الملف **وصف لا تفاعل**: عنوان، شرح، تاريخ استحقاق اختياري. لا حالة، لا
ملاحظة، لا مرفقات، لا «من حرّكها». نطاقان فقط:

| النطاق | تعني | يقرؤها |
| ------ | ---- | ------ |
| `file` | عمل الملف كله — يتقاسمه الأعضاء فيما بينهم | كل أعضاء الملف |
| `role` | واجبات منصب (رئيس قطاع، مشرف برج…) أياً كان شاغله | كل أعضاء الملف |

**الجدول** (ما بقي منه): `id`, `module_id`, `scope` (`file`|`role` — القيمة
`personal` باقية في الـ enum ومحظورة بالقيد), `role_id` (مطلوب لـ `role`،
ممنوع لـ `file`), `title_ar`, `title_en?`, `description_ar/_en?`, `due_on?`,
`sort_order`, `created_by`, `created_at`, `updated_at`. حُذف `profile_id` و
`group_id`.

**Endpoints:**

```
GET    /modules/{id}/tasks          → القائمة مرتبة: file ثم role، ثم sort_order ثم title_ar
POST   /modules/{id}/tasks          → إنشاء (scope, role_id?, title_ar, …)
PATCH  /module-tasks/{taskId}       → تعديل (العنوان والوصف والتاريخ — لا يقبل scope ولا role_id)
DELETE /module-tasks/{taskId}
```

**الصلاحية:** الكتابة الثلاث للمدير وحامل `modules.tasks` حصراً — الكود باقٍ
وهذا كل ما بقي له. القراءة لعضو الملف أو `modules.view_all` أو المدير.
تغيير النطاق ليس تعديلاً: نقل مهمة من منصب إلى الملف كله مهمة أخرى.

### 25.2 المهام الشخصية (`personal_tasks`) — نظام مستقل

> **migrations 0117 + 0118 + 0119.** ما بناه 0105 كان صحيحاً في موضعه وناقصاً
> في نموذجه: المهمة **مبادلة بين شخصين** لا قيمة يكتبها أحدهما. فترتّب على ذلك
> ثلاثة نواقص بنيوية — مُسنِدٌ بلا فعلٍ بعد الإسناد، وحقل `note` واحد يُمحى مع
> كل تحديث، وقرارٌ واحد يتشظّى إلى ستة صفوف يتيمة. §25.5 يشرح ما تغيّر.

**لا علاقة له بالملفات التشغيلية إطلاقاً.** لا `module_id`، لا موسم. مهمة =
صف واحد على قائمة شخص واحد، والتمييز الوحيد مقارنة واحدة:

| | `created_by = profile_id` | `created_by ≠ profile_id` |
| --- | --- | --- |
| ما هي | كتبها لنفسه | **أُسندت إليه** من حامل `tasks.assign` |
| صلاحية صاحبها | كاملة: تعديل، حذف، حالة، مرفقات، حذف | تحريك الحالة (ومنها «أرسِل للقبول») + التعليق + الأدلة + تعليم الخطوات. **ولا شيء غير ذلك**، ولا حذف |
| صلاحية كاتبها | — (هو صاحبها) | كاملة، وكذلك المدير وحامل `tasks.view_all` |
| «منجزة» | يقولها صاحبها | **يقولها المُسنِد وحده**، بعد `submitted` |

**الجدول:** `id`, `seq` (الرقم المنطوق «م-١٤٢» — `identity`, فريد على مستوى
البعثة), `profile_id` (صاحب القائمة), `created_by` (الكاتب), `batch_id?`
(الدفعة), `title` (لغة واحدة — قائمة خاصة لا أوراق إدارة), `description?`,
`due_on?`, `state` (`task_state` — سبع قيم، §25.5), `priority`
(`high|normal|low`), `kind` (`task|follow_up|request`), `note?` (**آخر ما قيل،
مشتقٌّ من آخر تعليق — لا مصدر**), `started_at?`, `submitted_at?`,
`completed_at?`, `accepted_by?`, `escalated_at?`, `escalation_rung`,
`sort_order`, `created_at`, `updated_at`, `updated_by`.

**`personal_task_attachments`:** نفس أعمدة كل جداول المرفقات
(`task_id`, `kind`, `path`, `name`, `mime_type`, `size_bytes`, `sort_order`)
**+ `comment_id?`**: مرفقٌ عُلّق مع تعليق يظهر في فقاعته، و`null` يعني مُقدَّماً
ضد المهمة نفسها (سلوك 0105، فلم ينتقل شيء).

**الجداول المضافة:** `personal_task_comments` (النقاش، مع علم
`is_transition` لما ولّدته حركة حالة), `personal_task_events` (السجل المقروء
— **ليس `audit_log`**: ذاك للتدقيق ويُنسى بعد موسمٍ ونصف (§23.6)، وهذا يُعرض
لطرفَي المبادلة ويعيش ما عاشت المهمة), `personal_task_steps` (قائمة تحقّق —
**لا مهام فرعية**: بلا حالة ولا صاحب ولا خيط), `personal_task_batches`
(القرار الواحد).

**Endpoints:**

> **شاشتان لا واحدة.** «مهامي» (`/tasks`) تقرأ `GET /me/tasks` وحدها وهي
> **بلا حراسة** — الجميع يملك قائمة بحكم وجوده. و«إسناد المهام»
> (`/tasks/manage`) تقرأ `GET /me/assigned-tasks` وحدها و**تُحرَس بـ
> `tasks.assign`**: الكتابة على قوائم الآخرين سلطة، وعمل المرء ليس سلطة،
> ولا يجتمعان في صفحة.

```
GET    /me/tasks                    → قائمتي، مُرشَّحة ومرتَّبة ومُصفَّحة على الخادم
                                      ?view=today|week|overdue|open|done|all   (الافتراض open)
                                      &state=&priority=&kind=&q=&limit=&offset=
                                      q يطابق العنوان والوصف **ورقم seq**
                                      الترتيب **مُلزِم**: bucket(state) → due_on nulls last
                                      → priority → created_at desc   (بديل RPC my_personal_tasks)
                                      كل صف يحمل: العدّادات (تعليقات/مرفقات/خطوات)
                                      و**actions[]** — ما يجوز للقارئ تحريكها إليه الآن
GET    /me/assigned-tasks           → ما كتبتُه على قوائم الآخرين — يتطلب tasks.assign
                                      ?scope=mine|all  — all يتطلب tasks.view_all
                                      (منتقي الأشخاص = `assignable_employees` §9.10، انظر أدناه)
GET    /me/review-queue             → ما ينتظر قبولي (scope=mine|all, state=submitted)
GET    /me/task-stats               → { open_count, overdue_count, review_count, done_count }
                                      done_count = آخر ٣٠ يوماً لا الكل: رقمٌ لا ينزل ليس إنجازاً

GET    /tasks/{id}                  → المهمة كاملة: الصف + الخطوات + الأدلة + actions
GET    /tasks/{id}/thread           → التعليقات والأحداث **مدموجة**، تصاعدياً.
                                      حركةٌ حملت جملةً تظهر **سطراً واحداً** لا سطرين

POST   /tasks                       → { profile_ids?: [uuid], title, description?, due_on?,
                                        priority?, kind?, steps?: [text] }
                                      بلا profile_ids أو بمعرّفي = على قائمتي (متاح للجميع)؛
                                      بمعرّفات غيري = إسناد، يتطلب tasks.assign.
                                      **قائمة لا معرّفاً واحداً**، و**أكثر من واحد ⇒ دفعة**:
                                      الخادم يكتب صف `personal_task_batches` وصفوف المهام
                                      **في transaction واحدة**. ستّ رحلات كانت ستُنجح أربعة
                                      وتُخفق اثنين بلا ما يقول أيّهما — وقرارٌ بلا أبٍ يجعل
                                      «كم أنجزها من الستة؟» سؤالاً لا تجيبه الداتا.
                                      الردّ: [{ id, seq }] بترتيب الكتابة.
PATCH  /tasks/{id}                  → العنوان/الوصف/التاريخ/الأولوية/النوع — للصلاحية الكاملة
DELETE /tasks/{id}                  → **للمهمة الشخصية فقط** (`created_by = profile_id`).
                                      المُسنَدة **تُلغى ولا تُحذف** — 0117 أغلق حذفها
PUT    /tasks/{id}/state            → { state, note? } — بديل RPC set_personal_task_state.
                                      يتحقق من مصفوفة §25.5، ويكتب حدثاً، ويختم الأوقات.
                                      **الاسم p_note باقٍ ولا يُغيَّر**: طوابير على أجهزة
                                      في الميدان تُسمّي وسيطها، وتغييره يُعطّلها يوم النشر.
                                      معناه الآن: **نصّ تعليق يُضاف**، لا حقلٌ يُستبدل
PUT    /tasks/{id}/assignee         → { profile_id } — إعادة إسناد تحفظ الخيط والأدلة
POST   /tasks/{id}/comments         → { body } → comment_id
PUT    /tasks/{id}/steps            → { labels: [text] } — استبدال كامل، للصلاحية الكاملة
PATCH  /task-steps/{id}             → { is_done } — الضغطة الواحدة في الميدان
POST   /tasks/{id}/attachments      → نفس عقد §12.3 + comment_id? — يكتبها **من يقرأ المهمة**
DELETE /task-attachments/{id}       → لمن يحرّك حالتها، أو لكاتب التعليق المعلَّقة عليه

GET    /task-batches                → دفعاتي، مع (total, done, submitted, blocked, overdue)
GET    /task-batches/{id}/tasks     → صفوف الدفعة، كلٌّ باسم حامله وحالته
```

> **«اقبل الجاهز» و«ذكّر المتبقّي» ليسا endpoints.** كلاهما تكرارٌ في العميل
> على `PUT /tasks/{id}/state` و`POST /tasks/{id}/comments`. المقصود مقصود: كل
> قبولٍ قرارٌ مستقل يكتب حدثه ويرسل إشعاره، وendpoint جماعي كان سيصير طريقةً
> لاتخاذ ستة قرارات دون قراءة أيٍّ منها. ما يُوفَّر ست ضغطات، لا ستة أحكام.

**قواعد ملزمة (بديل RLS):**

1. **`created_by` هو حامل التوكن دائماً** — صف يدّعي كاتباً آخر توقيع مزوّر.
2. الإسناد (`profile_id` لغير الكاتب) يتطلب `tasks.assign` أو مديراً.
3. **الصلاحية الكاملة** (تعديل/إلغاء/إعادة إسناد/خطوات): الكاتب؛ والمدير أو
   حامل `tasks.assign` على المهام **المُسندة** فقط. ما كتبه شخص لنفسه دفتره
   الخاص — حتى المدير لا يقرؤه.
4. **القراءة:** صاحب القائمة، والكاتب، وعلى المهام المُسندة فقط: المدير وحاملا
   `tasks.assign` و`tasks.view_all`.
5. **تحريك الحالة:** صاحب القائمة ومن يملك الصلاحية الكاملة — **كلٌّ ضمن
   مصفوفة §25.5 ولا شيء خارجها**. خارجها ⇒ `task_transition_not_allowed`.
6. تُقرأ هذه القواعد من **صف المهمة** على الخادم، لا من حمولة الطلب.
6b. **`state` إلى ما هي عليه أصلاً ⇒ ناجح صامت، لا خطأ.** طابور أُعيد إرساله
   بعد عودة الشبكة يجب ألّا يُخفق لأنه وصل مرّة.
6c. **`actions[]` تُحسب على الخادم بالدالّة نفسها التي ستجيب الكتابة.** لا يجوز
   للعميل أن يشتقّ ما يجوز من الحالة: نسخةٌ ثانية من القواعد في Dart هي رأيٌ
   ثانٍ في سؤالٍ لا يحتمل إلا جواباً واحداً، وأوّل خطأ تُنتجه زرٌّ يعرضه العميل
   وترفضه القاعدة.
6d. **التعليق إلزامي مع `blocked` و`returned` حصراً** — كلاهما دعوى بلا قيمة
   بغير جملتها. فارغ ⇒ `422 task_comment_required`.
6e. **الأختام الزمنية يكتبها الخادم لا العميل:** `started_at` **مرة واحدة**
   (رجلٌ تعثّر ثم عاد لم يبدأ مرتين)، `submitted_at` عند الادّعاء ويبقى بعد
   الإعادة، `completed_at` + `accepted_by` عند القبول ويُفرَّغان عند إعادة الفتح.
7. **منتقي الأشخاص هو `assignable_employees` (§9.10) نفسه** — كشف البعثة الذي
   تُملأ به المناصب: بحث عربي مطوي في الأسماء الثلاثة والمسمّى الوظيفي، وفلاتر
   بالمنصب والمدينة وداخلي/خارجي و«الشاغرون فقط»، وصفحات بأربعين، **ومعه
   الملفات التي يخدم فيها كل مرشَّح هذا الموسم** — وهي الحقيقة التي يُتَّخذ عليها
   معظم هذه القرارات. **migration 0106** يوسّع حارسه بمنحة واحدة فقط:
   `is_admin() or modules.members` صارت `... or tasks.assign` — فحاملُ
   `tasks.assign` الذي لا يدير ملفات كان سيفتح كشفاً فارغاً بصمت. توسيعٌ ضيّق:
   ما تعيده الدالّة أسماءٌ ومناصب وهواتف وإسنادات، و`tasks.assign` يتطلّب أصلاً
   `employees.view` التي تفتحها كلها على شاشة الموظفين.
8. **الطالب نفسه مُسقَط من الكشف عند الإسناد، بنيةً لا أدباً.** مهمة يُسندها
   المرء لنفسه هي `created_by = profile_id`، أي **مهمة شخصية عادية**: تهبط في
   «مهامي» ولا تظهر أبداً بين المُسنَدات، فتُقرأ كإسنادٍ فشل. الإسقاط في العميل
   (`exclude`) لا في الدالّة: ملء منصبٍ بالنفس فعلٌ مشروع، والدالّة مشتركة.

**التخزين:** دلو خاص جديد `tasks`، المسار `{task_id}/{i}_{file}`. القراءة
لمن يقرأ المهمة؛ الكتابة والحذف لمن يجوز له تغيير حالتها. حذف المهمة يحذف
ملفاتها (على العميل قبل حذف الصف، أو على الخادم — المهم ألا تبقى يتيمة).

**الإشعارات (0119).** 0105 أرسل إشعاراً واحداً في عمر المهمة ثم صمت — وكل ما
يهمّ الطرفين يقع **بعده**. الآن trigger واحد على `personal_task_events` يُبلّغ
**الطرف الذي لم يفعل** (لا الفاعل: لا أحد يحتاج إخباراً بقراره)، وtrigger ثانٍ
على التعليقات:

| الحدث | إلى | `type` |
| ----- | --- | ------ |
| أُسندت | صاحب القائمة | `personal_task_assigned` |
| أُرسلت للقبول | المُسنِد | `personal_task_submitted` |
| قُبلت | صاحب القائمة | `personal_task_done` |
| أُعيدت (ومعها نصّ السبب) | صاحب القائمة | `personal_task_returned` |
| تعثّرت (ومعها نصّ السبب) | المُسنِد | `personal_task_blocked` |
| أُلغيت | صاحب القائمة | `personal_task_cancelled` |
| تعليق جديد | الطرف الآخر | `personal_task_commented` |
| نُقلت | الحاملان القديم والجديد | `personal_task_assigned` / `_reassigned` |
| متأخرة | المُسنِد ثم المدير | `personal_task_overdue` |
| تستحق غداً | صاحب القائمة | `personal_task_due_soon` |

**مهمة الشخص لنفسه لا تُشعِر أحداً** — قاعدة 0105 كما هي، وتصعيدها كذلك
مستثنى: دفتر المرء ليس شأن أحد.

**التصعيد (0119، على آلة §29):** درجتان ولا ثالثة — لا هرم في هذا النظام،
وذلك بالضبط ما يجعله ليس الملفات التشغيلية. `due_on + 1` ⇒ المُسنِد والحامل،
`due_on + 3` ⇒ المدير. `escalation_rung` يمنع التكرار، ويُصفَّر بـ trigger عند
الإنجاز أو الإلغاء أو نقل التاريخ أو إعادة الإسناد. الجدولة
`escalate-late-tasks` الساعة 17:00 UTC خلف حارس `pg_cron` نفسه.

**طابور الإرسال:** أربع عمليات تُطابَر لأنها تُكتب على الأقدام —
`personal.task_state` (كان `module.task_state`) و`personal.task_comment`
و`personal.task_step` و`personal.task_create`. و**ثلاث لا تُطابَر عمداً**:
الإسناد والقبول والإعادة. تلك قرارات تُتّخذ على مكتب، وإسنادٌ يصل بعد ست ساعات
أسوأ من إسنادٍ ظهر أنه لم يُرسَل — انظر §27.

### 25.3 الصلاحيتان

| الرمز | الوصف | يتطلب |
| ----- | ----- | ----- |
| `tasks.assign` | كتابة مهمة على قائمة شخص آخر، والقلم الكامل على المُسنَدات (القبول والإعادة والإلغاء وإعادة الإسناد) | `employees.view` |
| `tasks.view_all` | **(0117)** رؤية كل المهام المُسندة في البعثة لا ما أسنده حاملها وحده | `tasks.assign` |

قسم أعلى مستقل `tasks` (ترتيبه 15) — الكود ليس تحت `modules.` عمداً.
`tasks.assign` مُنحت في ترحيل 0105 لكل حاملي `modules.tasks`. وهي **حارس شاشة
كاملة** (`/tasks/manage`) لا زرّاً داخل «مهامي».

**`tasks.view_all` لا تُمنح لأحد في الترحيل** — الرقابة على أحمال الآخرين قرارٌ
يتّخذه شخص، لا افتراضٌ يرثه أحد. وسببُ وجودها أن سياسة 0105 كانت **تسمح**
بالقراءة الأوسع ولم يسألها استعلامٌ في التطبيق قط (`fetchAssignedByMe` تقصرها
على `created_by = me`): رقابةٌ مسموحة وغير قابلة للوصول أسوأ من ممنوعة، لأن
غيابها لا يكتشفه أحد. **ولا تفتح دفتر أحد**: شرط `created_by <> profile_id`
قائمٌ أمام كل فرع تمسّه.

### 25.4 الترحيل (ما فعله 0105 بالبيانات)

- المهام الشخصية المكتوبة على الملفات (`scope='personal'`) **نُقلت** إلى
  `personal_tasks` بحالتها وملاحظتها وتاريخها؛ أدلتها في دلو `modules` لم
  تنتقل (نقل كائنات التخزين بين الدلاء غير ممكن من SQL).
- صفوف الحالة لمهام الملف والدور ماتت مع جدولها — لم تعد شيئاً يُقرأ.
- كائنات `{module_id}/tasks/…` تُحذف على قدر ما يسمح الحارس
  (`storage.protect_delete`)؛ ما بقي يُنظّف من اللوحة.

### 25.5 دورة الحياة (migrations 0117–0119)

> **العلّة الواحدة.** 0105 نمذج المهمة **قيمةً**، وهي **مبادلة**. عنه نتجت
> ثلاثة أعطال: مُسنِدٌ لا يملك فعلاً واحداً بعد الإسناد فتنتهي القصة بضغطة
> الطرف الوحيد صاحب المصلحة في إنهائها؛ وحقل `note` واحد يستبدله
> `set_personal_task_state` في كل نداء فيُمحى سبب تأخير الأمس مع «تم» اليوم؛
> وستة صفوف لا يربطها شيء بينما القرار الذي وَلَدها واحد.

**سبع حالات.** الأربع الجديدة موجودة لأن المبادلة الحقيقية تحتاجها ولا شيء
غير ذلك:

| الحالة | القيمة | معناها، ومَن يُدخِلها |
| ------ | ------ | -------------------- |
| لم تبدأ | `not_started` | الافتراض عند الإنشاء |
| قيد التنفيذ | `in_progress` | صاحب القائمة. تختم `started_at` أول مرة فقط |
| متعثّرة | `blocked` | صاحب القائمة، **بسبب إلزامي**. تُشعِر المُسنِد |
| بانتظار القبول | `submitted` | صاحب القائمة، **على المُسنَدة فقط**. **دعوى لا خاتمة** |
| منجزة | `done` | على الشخصية: صاحبها. على المُسنَدة: **المُسنِد وحده** |
| مُعادة | `returned` | المُسنِد، **بتعليق إلزامي** يقول ما الناقص |
| ملغاة | `cancelled` | القلم الكامل. **بديل الحذف** للمُسنَدة: تُخفيها وتُبقي أثرها |

`cancelled` خروجٌ من الدورة لا مرحلة فيها، ولا تدخل في أي حساب تقدّم.

**مصفوفة الانتقالات — مُلزِمة على الخادم، ومصدرها الوحيد
`personal_task_transition_allowed`.** الأعلام الثلاثة هي كل المفردات:
`assigned` (كتبها غيره)، `owner` (القارئ صاحب القائمة)، `full` (القلم الكامل).

| من | إلى | مَن | الشرط |
| -- | --- | --- | ----- |
| `not_started` \| `blocked` \| `returned` | `in_progress` | owner | — |
| `done` | `in_progress` | full | إعادة فتح؛ تُفرَّغ `completed_at` و`accepted_by` |
| `not_started` \| `in_progress` | `blocked` | owner | تعليق إلزامي |
| `not_started` \| `in_progress` \| `blocked` \| `returned` | `submitted` | owner | **assigned فقط** |
| `not_started` \| `in_progress` \| `blocked` | `done` | owner | **الشخصية فقط** (`created_by = profile_id`) |
| `submitted` | `done` | full | **assigned**؛ يختم القابل والوقت |
| `submitted` | `returned` | full | **assigned**؛ تعليق إلزامي |
| أي حالة | `cancelled` | full | — |
| `cancelled` | `not_started` | full | استرجاع |

ما عدا ذلك ⇒ `400 task_transition_not_allowed`. ولاحظ الصفّين اللذين يحملان
كل الفكرة: **`in_progress → done` ممنوعة على المُسنَدة**، و**`submitted → done`
ليست لصاحبها**.

**النقاش بديل الحقل.** كل ما يُقال يُضاف صفاً في `personal_task_comments` ولا
يُستبدل شيء. حركةٌ حملت جملةً تُخزَّن كتعليق `is_transition = true` مرتبطاً
بالحدث عبر `payload.comment_id`، و`personal_task_thread` تُعيدها **سطراً
واحداً**: «أعادها وقال: الكشف ناقص» فعلٌ واحد، لا رجلٌ شرح ثم — بلا صلة —
فعل. وعمود `note` يبقى **مشتقاً**: نسخة من آخر ما قيل تُغني قائمةَ ثلاثين صفاً
عن join، وليست مكان الذاكرة.

**الدفعة.** أكثر من مُسنَد إليه ⇒ صف `personal_task_batches` أبٌ للصفوف، في
transaction واحدة. الصف ما زال يخصّ رجلاً واحداً ويُحمل وحده؛ الجديد أنه
**يذكر ما كان جزءاً منه**، فيصير «كم أنجزها من الستة؟» سؤالاً له جواب.

**الخطوات لا مهام فرعية.** المهمة الفرعية كانت ستحتاج حالةً وصاحباً وتاريخاً
وخيطاً، و«تجهيز مخيّم منى» لا يريد خمسة صفوف في قائمة — يريد أربعة صناديق
داخله يؤشّرها رجلٌ واقفٌ في المخيّم بإبهام واحد. الاستبدال كامل
(`PUT /tasks/{id}/steps`) لأن قائمة خمس تسميات تُرسَل كاملةً تحتمل الإرسال
مرتين، وسيلَ add/remove/reorder من طابورٍ لا يحتمله. **والتأشيرات تُحفظ
بالتسمية**: من أعاد ترتيب صناديقه الأربعة لم يُلغِ تأشير اثنين.

**ما لم يُنفَّذ عمداً:** الربط بالملفات التشغيلية أو الحوادث
(`personal_task_links`)، ومراحل الموسم، والمتابعون. الأول يخالف ظاهر نصّ 0105
(«لا علاقة له بالملفات إطلاقاً») ويحتاج قراراً صريحاً قبل كتابته؛ والأخيران
ليسا على الطريق الحرِج.


## 26. التقييم Evaluations — النموذج والتكليف

> **migration 0084.** للبعثة نوعان مما يُسمّى بالعربية «تقييم»، وهذا المستند
> يفصل بينهما فصلاً تامّاً:
>
> - **§13 تقييم النجوم** (0059): خمس نجوم بين زملاء داخل ملف تشغيلي **منتهٍ**،
>   مجهول للجميع بلا استثناء، بلا أسئلة وبلا مجموع. **باقٍ كما هو، ولم تمسّه
>   هذه الترقية بحرف.**
> - **§26 التقييم بالنموذج** (0084، الجديد): ورقة تكتبها الإدارة، يعبّئها شخص
>   **مسمّى بالاسم** عن جهة **مسمّاة**، وتنتهي بعلامة من مجموع.
>
> خلطهما في جدول واحد كان الخيار الآخر ورُفض: أحدهما رأيُ زميل والآخر وثيقة
> إدارية، وكل قاعدة على أحدهما خاطئة على الآخر.

### 26.1 نصفان يلتقيان عند لحظة التكليف

| النصف       | ما هو                                                              | الصلاحية              |
| ----------- | ------------------------------------------------------------------ | --------------------- |
| **النموذج** | مراحل ← أسئلة ← إجابات، ولكلٍّ علامته                              | `evaluations.templates` |
| **التكليف** | صفٌّ واحد: هذا النموذج، عن هذه الجهة، بيد **مُقيِّم واحد**         | `evaluations.assign`  |
| **التعبئة** | الإجابة والاعتماد                                                  | **لا صلاحية لها**     |

**غياب صلاحية التعبئة هو التصميم لا سهوٌ فيه.** التقييم يصل إلى مُقيِّمه
**بالاسم**، كما يصل الملف إلى أعضائه بالإسناد. صلاحيةٌ هنا تعني «من نثق به
يقيّم من يشاء»، وتقييمٌ لم يطلبه أحد ليس تقييماً.

**وفتحُ التقييم يقع في «إدارة التقييم» وحدها — لا في سجل التقييمات.** السجلّ
سجلٌّ لما جرى، وشاشةٌ تسجّل العمل وتُصدره معاً غرفتان ببابٍ واحد. والفتحُ يجري
**واقفاً على النموذج** الذي ستُعبَّأ عليه الورقة، وهو أيضاً الترتيب الحقيقي
للفعل: النموذج هو ما يحسم أيَّ نوعٍ من الجهات يجوز تسميته بعده، فمن يبدأ من
النموذج لا يختار تركيبةً سيرفضها الخادم.

يترتّب على ذلك أن باب «إدارة التقييم» يقبل **أياً من كودين** —
`evaluations.templates` أو `evaluations.assign` — لا الأول وحده: من يملك الفتح
دون الكتابة يجب أن يصل إلى الشاشة التي يُفتح منها، وإلا صارت صلاحيته بلا باب
تُمارَس منه. وما يفعله كلٌّ منهما داخلها يبقى مفترقاً: أدوات التحرير تسأل
`templates`، وزرّ الفتح يسأل `assign`، والخادم يرفض أياً منهما لغير حامله على
كل حال.

### 26.1b النموذج لفئةٍ لا لجهةٍ بعينها

`target_type` على النموذج هو **فئة** الجهة — «موظف»، «ملف تشغيلي»، «فندق» —
وليس الجهة نفسها. النموذج يُكتب مرة ويُفتح عليه عشرات التقييمات عن جهات مختلفة
وفي مواسم مختلفة؛ ربطُه بملفٍ بعينه يعني إعادة كتابة النموذج بمراحله وأسئلته
وعلاماته لكل ملف، وهو ما لا يفعله أحد مرتين.

فالجهة تُسمّى **عند فتح التقييم** (`assign_evaluation`) لا عند كتابة النموذج،
وهناك وحدها يُستدعى `evaluation_targets` (§26.7). وهذه نقطةٌ أخفقت الواجهة في
قولها فقُرئ صفُّ «يُقيِّم» على أنه منتقٍ فشلت قائمته في التحميل — الصفُّ الآن
يحمل سطراً يقول ما يفعله، ومحرّرُ النموذج يحمل زرَّ «تقييم جديد» ليكمل الطريق
بدل أن ينتهي عند حافة.

### 26.2 المرحلة قسمٌ لا جولة

«المرحلة» في هذا النظام **قسم داخل ورقة واحدة**: «فريق التروية» مرحلة 1 و«الإعاشة»
مرحلة 2 من **التعبئة نفسها**، بيد **الشخص نفسه**، في **جلسة واحدة**. الشاشة
ترسمها خطوات لأن ستين سؤالاً لا تُقرأ في عمود واحد — لا لأن كل خطوة يملكها أحد
غير الآخر. كل سؤال يتبع مرحلة، حتى في نموذج بمرحلة واحدة: جعلُها اختيارية يعني
شكلين للشجرة نفسها ومسارين في كل ما يمشي عليها.

### 26.3 نوعا السؤال

| النوع    | له علامة؟ | له إجابات جاهزة؟ | إجباري؟          |
| -------- | --------- | ---------------- | ---------------- |
| `choice` | **نعم**   | نعم (إجابتان فأكثر)، **لكلٍّ علامتها** | اختياري الضبط |
| `text`   | **لا**    | لا               | اختياري الضبط    |

- **علامة السؤال** هي كامل ما يستحقه، و**لا تتجاوزها علامةُ أي إجابة**.
- **السؤال التحريري بلا علامة إطلاقاً** — لا له ولا لإجابته. `check` في القاعدة:
  `kind <> 'text' or points = 0`. علامةٌ على فقرة يجب أن يمنحها أحد، وذاك الأحد
  هو المُقيِّم يصحّح لنفسه.
- **الإجباري/الاختياري يُسأل عن النوعين معاً**: سؤالٌ تحريري بلا علامة قد يكون
  الشيء الوحيد الذي وُجدت الورقة لجمعه.

### 26.4 المجموع مُشتقٌّ لا مُخزَّن

مجموع النموذج = مجموع علامات أسئلته. مجموع المرحلة = مجموع أسئلتها. **لا عمود
لأيٍّ منهما**: عمودٌ يحمله سيكون نسخة ثانية من حقيقةٍ تتغيّر مع كل تعديل سؤال،
والنسخة هي التي ستُعرض.

### 26.5 الجداول الستة

```
evaluation_templates (id, title, description, target_type, is_active,
                      created_by, created_at, updated_at)
  └── evaluation_stages    (id, template_id, title, description, sort_order)
        └── evaluation_questions (id, stage_id, kind, text, points,
                                  is_required, sort_order)
              └── evaluation_options (id, question_id, text, points, sort_order)

evaluations (id, template_id, season_id, target_type, target_*_id, target_label,
             evaluator_id, assigned_by, note, due_on,
             status, submitted_at, score, max_score, created_at, updated_at)
  └── evaluation_answers (id, evaluation_id, question_id, option_id,
                          stage_title, question_text, question_kind,
                          answer_label, stage_order, question_order,
                          text_answer, points, max_points)
```

`target_type` من `evaluation_target_type` = القيم السبع نفسها التي للشكاوى
(`employee, module, report, hotel, cluster, group, other`)، والجهة **أربعة
مفاتيح أجنبية اختيارية تحت enum** لا `target_id uuid` عارياً — للسبب نفسه في
§24.2.

**`on delete restrict` على `template_id`** وليس `cascade`: نموذجٌ عُبِّئ صار
تاريخاً، وحذفُه يجب أن يفشل بصوت عالٍ لا أن يأخذ العلامات معه. إخراجُ نموذج من
الخدمة هو `is_active = false`.

**ولا عمود يجمع الصفوف** — لا `batch_id` ولا سواه. 0125 أضافت واحداً وسحبته
0127: السجلّ يُجمَّع عند القراءة بـ(`template_id`, الجهة) في
`evaluation_subjects_list` (§26.7)، وهو تجميعٌ لا يحتاج أن يكون مكتوباً في
الجدول لأن كل ما يقوم عليه مكتوبٌ فيه أصلاً.

### 26.6 الإجابة تحمل نصّها — القاعدة المركزية

**كل صفّ في `evaluation_answers` يحمل نسخة من نصّ السؤال وعلامته ومرحلته.**

النماذج تُعدَّل: يُعاد صوغ سؤال في شوّال، والإجابة التي أُعطيت في رجب يجب أن تظلّ
تُقرأ على أنها جوابٌ عمّا سُئل فعلاً. لذلك:

- `question_id` و `option_id` مؤشّران بـ `on delete set null` — يذهبان ولا يأخذان
  السجلّ معهما.
- `question_text`, `answer_label`, `question_kind`, `stage_title`,
  `stage_order`, `question_order`, `points`, `max_points` تُملأ بـ trigger عند
  الكتابة، **ولا يرسلها العميل أبداً**.
- **تقييم معتمَد يُرسم من إجاباته وحدها.** تقييمٌ ما زال مسوّدة يُرسم من النموذج،
  لأنه ما زال يُعبَّأ عليه.

هذا هو `complaints.target_label` (§24.2) مطبَّقاً على ورقة كاملة.

### 26.7 القراءة

| المسار                                                       | يقابل RPC                | من يقرأ                                     |
| ------------------------------------------------------------ | ------------------------ | ------------------------------------------- |
| `GET /evaluation-forms`                                      | `evaluation_forms_list`  | حامل أي من الثلاث صلاحيات                   |
| `GET /evaluation-forms/{id}`                                 | `evaluation_form`        | أي مسجَّل — والمُقيِّم يقرأ نموذجه **ولو أُوقف** |
| `GET /evaluations?scope=mine\|all`                           | `evaluations_list`       | `mine` للجميع، `all` تطلب `evaluations.view` |
| `GET /evaluations/subjects?scope=mine\|all&template_id=`     | `evaluation_subjects_list` | نفس شرط السابق — والبطاقة لا تقول أكثر من صفوفها |
| `GET /evaluations/{id}`                                      | `evaluation_sheet`       | المُقيِّم، أو الإدارة، أو **الجهة نفسها منقّحاً** |
| `GET /me/evaluations-about-me`                               | `evaluations_about_me`   | صاحبها وحده — **بلا أي عمود يسمّي المُقيِّم** |
| `GET /evaluations/standing?target_type=&target_id=`          | `evaluations_about`      | الإدارة، وصاحبها عن نفسه                    |
| `GET /evaluation-targets?target_type=&q=`                    | `evaluation_targets`     | حامل `evaluations.assign`                   |

**ولا مسار لاختيار المُقيِّم.** كان هناك واحد (`evaluation_evaluators`) وحُذف:
كان يقرأ `job_titles.name_ar` وهو عمود لم يوجد قط (اسمه `name` منذ 0002، و0046
يشرح لماذا لم يُعَد تسميته حين أُضيف `name_en` بجانبه) — والأهم أنه كان يجيب عن
سؤالٍ للتطبيق جوابٌ أفضل عنه أصلاً. اختيار المُقيِّم يجري على صفحة إسناد
الموظفين نفسها (`assignable_employees` — §9.10، بمرشّحات 0065): بحثٌ في الخادم،
وتصفية بالمسمّى الوظيفي والمدينة، و — وهو بيت القصيد عند تكليف تقييم — **قائمة
بكل ملفٍ يحمله المرشّح هذا الموسم**. من يحمل برجين ومطبخاً ليس الرجل الذي تُعطى
له مهمّة رابعة، وهذه حقيقة يجب أن تُرى وقت الاختيار لا أن تُكتشف بعد أسبوع.

يترتّب عليه أن **بركة المُقيِّمين هي مشاركو الموسم الحالي** لا كل حساب معتمَد،
وهو تضييق مقصود: من ليس في الموسم لا يؤدّي عمل الموسم.

`evaluation_sheet` يعيد الورقة كاملة في نداء واحد: الترويسة + المراحل + الأسئلة
+ ما أُجيب. وفيه `my_role` ∈ `evaluator | subject | manager` و `can_fill`
منطقيّة — **يقرّرهما الخادم**، ولا يشتقّهما العميل من الحالة والدور، لأن
الجوابين اثنان عن سؤال لا يحتمل إلا واحداً.

### 26.8 الكتابة

| المسار                                    | يقابل RPC                      | الصلاحية              |
| ----------------------------------------- | ------------------------------ | --------------------- |
| `PUT /evaluation-forms/{id?}`             | `save_evaluation_form`         | `evaluations.templates` |
| `POST /evaluations`                       | `assign_evaluations`           | `evaluations.assign`  |
| `PUT /evaluations/{id}/answers`           | `save_evaluation`              | **المُقيِّم وحده**     |
| `POST /evaluations/{id}/reopen`           | `reopen_evaluation`            | `evaluations.assign`  |
| `PATCH /evaluations/{id}`                 | `update_evaluation_assignment` | `evaluations.assign`  |
| `DELETE /evaluations/{id}`                | —                              | `evaluations.delete`، أو من فتحه ولم يُمسّ بعد |

**`POST /evaluations` يأخذ مصفوفتين لا معرّفين مفردين**، ويكتب **الجداء
الديكارتي**: تقييمٌ لكل جهة عند كل مُكلَّف. الإدارة لا تسمّي قاضياً واحداً وجهةً
واحدة — تسمّي الثلاثة الذين سيُقيِّمون والأحد عشر ملفاً المطلوب تقييمها، وتعني
الثلاثة والثلاثين.

```json
{
  "template_id": "uuid",
  "target_type": "module",
  "target_ids": ["uuid", "uuid", "…"],
  "evaluator_ids": ["uuid", "uuid", "uuid"],
  "note": "…",
  "due_on": "1447-…"
}
```
⇒ `201 { "ids": ["…", "…"] }` — كلها أو لا شيء، في معاملة واحدة.

**ولا يجوز أن يتقاسم مُقيِّمان ورقة واحدة.** ليس ذلك نسخة أصغر من هذا بل نسخة
مكسورة منه: `evaluation_answers` فريدة على (التقييم، السؤال)، فإجابةُ الثاني
تدوس إجابة الأول بدل أن تقف بجانبها، وعلامةٌ واحدة تُضطر أن تنوب عن حكمين،
وسرّية §26.9 معرَّفة **لكل مُقيِّم** فلا يبقى لها ما تتعلّق به. استقلالُ الأحكام
هو مقصد التقييم، والاستقلالُ صفٌّ لكلٍّ منها.

`assign_evaluation` المفردة باقية غلافاً رفيعاً على الجمعية، حتى يكون لسؤال «ماذا
يجري عند فتح تقييم» جوابٌ واحد.

**`PUT /evaluation-forms` معاملة واحدة** تكتب الشجرة كلها: المُرسَل بـ `id`
يُحدَّث، والغائب يُحذف، و`sort_order` هو موضع العنصر في المصفوفة — وهو ما يجعل
السحب لإعادة الترتيب **حفظاً** لا نداءً لكل صف.

**`PUT /evaluations/{id}/answers` يرسل حالة الورقة كاملة** لا تعديلاً جزئياً:
الإجابات الغائبة من المصفوفة **تُحذف**، لأن مسح سؤال هو ألا تقول عنه شيئاً.

```json
{
  "answers": [
    { "question_id": "uuid", "option_id": "uuid", "text": null },
    { "question_id": "uuid", "option_id": null,   "text": "ملاحظة تحريرية" }
  ],
  "submit": true
}
```

⇒ `200 { "status": "submitted", "score": 38, "max_score": 50 }`

### 26.9 السرّية — من يعرف من قيَّم

القاعدة: **المُقيَّم لا يعرف من قيَّمه أبداً. حاملُ `evaluations.view` يعرف.**
المُقيِّم ليس سرّاً عن الإدارة، وإنما عمّن حكم عليه.

وهي **بنيوية** لا قراراً يتّخذه التطبيق، تماماً كما في §24:

1. **لا صفّ يقرؤه أصلاً.** سياسة `SELECT` على `evaluations` تستثني صراحةً كلَّ
   صفّ يكون القارئ هو جهته: `target_type <> 'employee' or target_profile_id is
   distinct from auth.uid()`. صفٌّ يستطيع اختياره هو صفٌّ يستطيع قراءة
   `evaluator_id` منه.
2. **بابٌ واحد ينقّح.** `evaluations_about_me` **لا يختار عمود المُقيِّم أصلاً**
   — لا يُفرَّغ حقلٌ لم يُقرأ، وهذه فائدة الدالة على العرض (view).
3. **المدير محجوبٌ عن ورقته هو.** وإلا صارت `evaluations.view` هي طريقة معرفة
   من قيَّمك، وأولُ من يلاحظها هو من أُخفيت عنه.
4. **السجلّ ينقّح.** trigger مخصّص على `evaluations` و `evaluation_answers`
   يُسقط `evaluator_id` و `text_answer` من الحمولة، ويكتب **بلا فاعل** حين يكون
   الفاعلُ المُقيِّمَ نفسه. أفعال الإدارة (فتح، إعادة فتح، حذف) تحتفظ بفاعلها —
   وتسمية المدير هي بالضبط ما وُجد السجلّ له.
5. **المسوّدات محجوبة عن الجهة.** `evaluations_about_me` يعيد المعتمَد وحده:
   علامةٌ لم تُعتمد ليست حكماً بعد.

### 26.10 التزامات الخادم (ملزمة حرفياً)

1. **لا endpoint يقبل `score` ولا `max_score` ولا `status`.** العلامة **تُحسب من
   الإجابات** داخل `save_evaluation` ولا تُقبل من أحد — لا من المُقيِّم ولا من
   المدير الذي فتح التقييم. في Postgres تُنفَّذ هذه بأن **لا سياسة `UPDATE`
   على `evaluations` إطلاقاً**؛ في REST تُنفَّذ برفض الحقول الثلاثة في كل مسار.
2. **`score` = مجموع علامات الإجابات. `max_score` = مجموع علامات النموذج.**
   من مصدرين مختلفين عمداً: سؤالٌ اختياري تُرك فارغاً يظلّ محسوباً على المجموع
   الذي كان يستحقّه، وإلا تحسّنت الورقة بتخطّي أسئلتها الصعبة.
3. **الاعتماد يتحقّق من النموذج كما هو الآن.** كل سؤال إجباري في النموذج
   **لحظة الاعتماد** يجب أن يكون مُجاباً؛ النموذج قد يكون كسب سؤالاً بعد فتح
   الورقة، وورقةٌ اعتُمدت على شروط الأمس ورقةٌ فيها ثقب. الخطأ:
   `evaluation_incomplete`.
4. **التجميد إلزامي.** كل إجابة تُخزَّن ومعها نصّ سؤالها وعلامته ومرحلتها
   (§26.6). الخادم يملؤها؛ العميل لا يرسلها ولا يُصدَّق لو أرسلها.
5. **الجهة والنموذج والمُقيِّم ثوابت.** `PATCH` لا يقبل أياً منها: تغييرُ أحدها
   هو فتح تقييم آخر، وله صفّه. الخطأ: `evaluation_is_immutable`.
6. **لا يُفتح تقييم على نموذج موقوف** (`evaluation_form_not_active`)، ولا على
   نموذج نوعُ جهته غير نوع الجهة المطلوبة (`evaluation_form_wrong_target_type`).
   أما إتمام ورقةٍ مفتوحة على نموذج أُوقف لاحقاً فمسموح — الإيقاف يمنع البدء لا
   الإنهاء.
7. **علامة الإجابة ≤ علامة سؤالها**، والسؤال التحريري بلا إجابات جاهزة. القيدان
   **مؤجَّلان إلى نهاية المعاملة** (deferred) لا مُتحقَّق منهما جملةً جملة: خفضُ
   سؤال من 10 إلى 5 مع خفض إجابته من 10 إلى 3 تعديلٌ سليم يفشل إن سبق أحدهما
   الآخر، وترتيبُ الحفظ يجب ألا يكون جزءاً من القواعد. الخطآن:
   `evaluation_option_worth_more_than_question` و
   `evaluation_written_question_takes_no_options`.
8. **`season_id` يُختم من الخادم** من الموسم الحالي عند الإنشاء، ولا يرسله العميل.
9. **`target_label` يُختم مرة واحدة عند الفتح** من الدالة نفسها التي تسمّي
   الصفوف في السجلّ (`audit_record_label`) — لا دالة تسمية ثانية، لأن الاثنتين
   تفترقان.
10. **الإشعارات.** عند الفتح ⇒ إشعار للمُقيِّم باسم من كلّفه. عند الاعتماد وكانت
    الجهة موظفاً ⇒ إشعار له **بلا مُرسِل** (`sender_id = null`): صندوقُ الوارد
    آخرُ مكان يُفشى فيه السرّ، وسيُفشى كاملاً.
11. **الحذف.** حذف التقييم يحذف إجاباته (`cascade`). حذف النموذج **يفشل**
    ما دام عليه تقييم واحد (`restrict`) — وهذا الرفض ميزة لا عائق: العلامات
    المكتوبة عليه تاريخ.
    لكن الرفض يجب ألا يكون طريقاً مسدوداً: `GET /evaluations?template_id=`
    (§26.7) هو ما يُرى منه ما يقف على النموذج، و `DELETE /evaluations/{id}`
    (§26.8) هو ما يُزال به — والواجهة توصل بينهما من بطاقة النموذج نفسها.
12. **تحذير للترقيات اللاحقة.** do-block في 0077 الذي يعلّق trigger السجلّ العام
    على كل جداول المخطّط **يجب ألا يُعاد تشغيله بعد 0084**: سيستبدل بالمنقِّحَين
    (الشكاوى والتقييم) العامَّ، ويبدأ بكتابة `complainant_id` و `evaluator_id`
    في سجلٍّ يقرؤه المدراء.
13. **بطاقة الموضوع لا تقول أكثر ممّا تقوله صفوفها** (0127).
    `evaluation_subjects_list` يحمل شرطَي `evaluations_list` حرفياً: لا يُرجع
    صفّاً قارئُه هو جهتُه، و`scope=all` تطلب `evaluations.view`. وعلاماته
    **على المعتمَد وحده، ونسبةً لا علامةً خاماً**: ورقةٌ نصفُ معبّأة مجموعُها
    رقمٌ لا يصحّ اقتباسه (البند 2 أعلاه هو الحجّة نفسها لورقةٍ واحدة)، ونموذجٌ
    أُعيد ترقيمه بين إسنادٍ وآخر يجعل الخام غير قابل للمقارنة أصلاً. والتجميع
    بـ(`template_id`, الجهة) لا بالجهة وحدها: الملف نفسه على ورقتين سؤالان،
    ومتوسّطهما معاً لا يجيب أيّاً منهما.

### 26.11 الصلاحيات الأربع

| الكود                   | ما يفتحه                                       | يشترط              |
| ----------------------- | ---------------------------------------------- | ------------------ |
| `evaluations.view`      | السجلّ كله، بعلاماته وبأسماء مُقيِّميه         | `employees.view`   |
| `evaluations.templates` | كتابة النماذج وتعديلها                         | `evaluations.view` |
| `evaluations.assign`    | فتح تقييم وتسمية مُقيِّمه، وإعادة فتح المعتمَد | `evaluations.view` |
|                         | *(ويفتح له باب «إدارة التقييم» للقراءة — §26.1)* |                    |
| `evaluations.delete`    | حذف تقييم                                      | `evaluations.view` |

**ولا كود خامس للتعبئة** — §26.1.

### 26.12 قاموس أخطاء §26

| الكود                                          | متى                                             |
| ---------------------------------------------- | ----------------------------------------------- |
| `evaluation_not_found`                         | لا تقييم بهذا المعرّف، أو لا حقّ للقارئ فيه     |
| `evaluation_form_not_found`                    | لا نموذج بهذا المعرّف                           |
| `evaluation_form_not_active`                   | محاولة فتح تقييم على نموذج موقوف                |
| `evaluation_form_wrong_target_type`            | النموذج لنوع جهة غير المطلوب                    |
| `evaluation_form_in_use`                       | تغيير نوع جهة نموذج فُتحت عليه تقييمات          |
| `evaluation_target_missing` / `_wrong_set`     | الجهة غير مسمّاة، أو من مجموعة أخرى             |
| `evaluation_evaluator_missing`                 | فتح دفعة بلا مُكلَّف واحد على الأقل             |
| `evaluation_is_immutable`                      | تعديل الجهة أو النموذج أو المُقيِّم             |
| `evaluation_already_submitted`                 | كتابة في ورقة معتمَدة                           |
| `evaluation_incomplete: <نص السؤال>`           | اعتماد وسؤال إجباري بلا إجابة                   |
| `evaluation_question_not_on_this_form`         | إجابة عن سؤال ليس من نموذج هذه الورقة           |
| `evaluation_option_not_on_this_question`       | إجابة مختارة ليست من إجابات سؤالها              |
| `evaluation_option_worth_more_than_question`   | علامة إجابة تتجاوز علامة سؤالها                 |
| `evaluation_written_question_takes_no_options` | إجابات جاهزة على سؤال تحريري                    |

---

## 27. طابور الإرسال Outbox — التزامات الخادم تجاه إعادة الإرسال

**لا endpoint جديدة في هذا القسم.** الطابور يعيش على الجهاز بالكامل
(`lib/core/offline/`) ويستدعي نفس عمليات §12.3 و §25.2. لكنه يغيّر شرطاً واحداً
على الخادم تغييراً جوهرياً، ولذلك يُوثَّق هنا: **كل عملية ميدانية يجب أن تحتمل
إعادة التنفيذ كاملةً.**

### 27.1 لماذا

شبكة المشاعر تغيب ساعات، لا دقائق. قبل الطابور كان الموظف الذي ينقل مهمة إلى
«منجز» أو يرفع تقرير المساء وهي غائبة **يخسر ما كتبه** — وهو واقف في مخيم ولن
يعيد كتابته. الآن تُحفظ العملية على جهازه مع صورها وتُرسل حين تعود الشبكة.

### 27.2 ما يُطابَر وما لا يُطابَر

| العملية                              | تُطابَر؟ | لماذا                                                            |
| ------------------------------------ | -------- | ----------------------------------------------------------------- |
| `set_personal_task_state` (+ مرفقاتها) | **نعم**  | تُكتب واقفاً في الميدان (§25.2؛ كانت `set_module_task_state` قبل 0105) |
| `add_personal_task_comment` (+ مرفقاته) | **نعم** | منذ 0117 هنا يقع الكلام، ويُقال في الميدان (`personal.task_comment`) |
| تعليم خطوة (`PATCH /task-steps/{id}`) | **نعم**  | الضغطة الواحدة أمام الشيء نفسه (`personal.task_step`)              |
| إنشاء مهمة **لنفسي**                 | **نعم**  | تذكيرٌ يُكتب واقفاً (`personal.task_create`)                        |
| **الإسناد لغيري، والقبول، والإعادة**  | **لا**   | قرارات مكتب. إسنادٌ يصل بعد ست ساعات أسوأ من إسنادٍ ظهر أنه لم يُرسَل: المُسنِد مضى، والمتلقّي لا يعرف أهو مطلوب بعد |
| `submit_module_report` (+ مرفقاته)   | **نعم**  | تقرير المساء من المخيم (§12.3)                                     |
| كل ما عداها                          | لا       | يُكتب جالساً على اتصال، ويمكن إبلاغ صاحبه بالفشل                    |

**قاعدة الالتقاط:** لا يُحفظ في الطابور إلا الفشل الذي سببه **الشبكة**. رفض
الخادم (صلاحية سُحبت، ملف حُذف، فترة أُغلقت) يُرفع إلى المستخدم فوراً كما كان —
طابور يبتلع الرفض يكذب على صاحبه.

### 27.3 التزامات الخادم

0. **`PUT /tasks/{id}/state` إلى حالةٍ هي عليها أصلاً ⇒ ناجح صامت.** بغير ذلك
   يعود طابورٌ أُرسل مرتين بـ `task_transition_not_allowed`، فيبدو رفضاً وهو
   وصول. وباسم الوسيط `p_note` لا يُمَسّ: أجهزةٌ في الميدان كتبت طوابيرها
   بالاسم القديم (§25.2).
1. **إعادة تنفيذ العملية كاملةً لا تُنتج أثراً مضاعفاً.** الترتيب المعتمد حالياً:
   رفع الملفات أولاً ثم إدراج صفوفها في **statement واحد أخير**، مع ترقيم
   `sort_order` من الموجود فعلاً في الجدول. فمحاولة ماتت قبل الإدراج لم تترك
   صفوفاً، والمحاولة التالية ترقّم من نفس النقطة وتكتب فوق مفاتيح التخزين نفسها
   بـ `upsert`. **على أي backend بديل الالتزام بهذا الترتيب.**
2. **`submit_module_report` تبقى «مرة واحدة لكل فترة»** وتُرجع نفس `report_id` عند
   التكرار (§12.3). هذه الخاصية هي ما يجعل إعادة الإرسال آمنة.
3. **ثغرة معروفة وغير مغطاة:** محاولة نجح إدراجها وضاع ردّها في الطريق ⇒ إعادة
   الإرسال تجد الصفوف وترقّم بعدها، فتصل المرفقات **مرتين**. الكلفة صورة مكرّرة
   لا صورة خاطئة. إغلاقها يحتاج قيد تفرّد على
   `(report_id, path)` و `(status_id, path)` — **غير موجود، ويُنصح بإضافته**
   ومعه `ON CONFLICT DO NOTHING` في الإدراج.
4. **ترتيب الوصول مضمون من العميل:** الطابور يرسل بالأقدم أولاً، فحالتا مهمة
   واحدة (`in_progress` ثم `done`) تصلان بترتيبهما. لا يحتاج الخادم منطقاً لهذا،
   لكنه **يجب ألّا يعيد ترتيبها** أو يعالجها بالتوازي داخل نفس الجلسة.
5. **رمز خطأ الشبكة:** يميّز العميل «الشبكة ساقطة» بنص الاستثناء
   (`SocketException`, `ClientException`, `TimeoutException`, …) — انظر
   `lib/core/utils/network_error.dart`. أي بوابة تحوّل انقطاع الشبكة إلى **502/504
   بجسم JSON** ستكسر هذا التمييز، فيُعامل الانقطاع كرفض ويُفقد العمل. **إن كان
   ولا بد، فليكن الردّ بلا جسم أو بمهلة صريحة.**

### 27.4 سلوك العميل (للعلم فقط)

- **تراجع أُسّي:** 5s → 15s → 45s → 2m → 5m → 15m → 30m (سقف).
- **عودة الشبكة تُلغي التراجع** ولا تنتظره — الإشعار بعودة الراديو معلومة جديدة
  تُبطل تأجيلاً سببه غيابه.
- **ثماني محاولات** ثم يتوقف العنصر وينتظر **إنساناً**: يظهر في `/outbox` بزرّي
  «إعادة المحاولة» و«حذف». الحذف حقّ صاحب العمل وحده.
- المرفقات **تُنسخ** إلى مجلد التطبيق لحظة اختيارها (لا يُعتمد على مسار الكاميرا)
  وتُمسح بعد الإرسال.

---

## 28. تصدير البيانات Export — لا endpoints، ولكن التزام واحد

**لا endpoint جديدة.** شاشة التصدير (`/export` في الإعدادات) تقرأ عبر **نفس**
الاستدعاءات الموثّقة أعلاه، وتكتب الناتج ملفاً على الجهاز. الخادم لا يعلم أن
تصديراً جرى.

### 28.1 ما يُصدَّر وبأي استدعاء

| المجموعة              | الاستدعاء المستخدم                          | الصلاحية التي تُظهرها          |
| --------------------- | ------------------------------------------- | ------------------------------- |
| الموظفون              | `permanent_employees` + `profiles` (§5)     | `employees.view`                |
| مشاركو الموسم         | §7 المشاركون                                | `seasons.participants_view`     |
| الملفات التشغيلية     | §9 قائمة الملفات                            | — (RLS)                         |
| أعضاء ملف             | §11.1 و §10 العقد                           | — (RLS)                         |
| مهام ملف (وصفية)      | `GET /modules/{id}/tasks` (§25.1)           | — (RLS)                         |
| البيانات المرجعية     | §14 المجموعات وعناصرها                      | `reference.view`                |
| التقارير المركزية     | §15 القائمة                                 | — (RLS)                         |
| الشكاوى               | RPC `complaints_list` بـ `scope=all` (§24.3) | `complaints.view`               |
| التقييمات             | RPC `evaluations_list` بـ `scope=all` (§26.7)| `evaluations.view`              |

### 28.2 التصدير صلاحية عليا (migration 0100)

**كان هذا القسم يقول إن «التصدير لا يوسّع ما يراه أحد، ويجب أن يبقى كذلك». لم
يعد كذلك، بقرار.** `export.data` صار **ثاني أقوى منح في النظام بعد المشرف
العام**: من يحمله يُخرج أي نوع من البيانات، ولو لم يستطع فتح شاشة واحدة منها.

**ولماذا وُسِّعت السياسات لا الواجهة؟** لأن التوسيع في Dart وحده كان سيُنتج **ملفاً
فارغاً بصمت**: عرضُ «الموظفين» لمن لا يحمل `employees.view` يعني استدعاءً يردّ
`profiles_select` عليه بلا صفوف، ولا خطأ يُرفع. وملفٌّ فارغ يُسلَّم على أنه تصدير
أسوأ من الرفض — صاحبه يمضي به معتقداً أن البيانات معه، ويكتشف حين يُسأل عن اسم
ليس فيه.

| السياسة | وُسِّعت؟ | لماذا |
| ------- | ------ | ----- |
| `profiles_select` | **نعم** | الموظفون |
| `season_participants_select` | **نعم** | مشاركو الموسم |
| `reference_items` | لا حاجة | مفتوحة لكل معتمَد منذ 0019 |
| `can_read_all_complaints()` | **نعم، من الداخل** | ↓ |
| `can_read_all_evaluations()` | **نعم، من الداخل** | ↓ |
| الملفات وأعضاؤها ومهامها | **لا** | تدور على العضوية أو `modules.view_all`. تسليم تشكيل الموسم كلّه بمنحٍ اسمه «تصدير» قرارٌ يُتّخذ وحده |

**والسياج الذي بقي قائماً، وهو كل العناية في 0100.** سياستا الشكاوى والتقييمات
تحجبان عن المدير قضيّته هو:

```sql
or (can_read_all_complaints()
    and (target_type <> 'employee'
         or target_profile_id is distinct from auth.uid()))
```

و0079 يقول لماذا في أحدّ جملة فيه: بغير ذلك تصير `complaints.view` **الطريقَ إلى
معرفة من اشتكى عليك، وأول من يلاحظ ذلك هو من أُخفي عنه**.

فـ `or has_permission('export.data')` مضافةً إلى السياسة مباشرةً كانت **ستهدم
السياج بالكامل** — يُصدّر المديرُ جدول الشكاوى ويقرأ من اشتكى عليه، وهو بعينه
الهجوم الذي كُتب 0079 لمنعه، قادماً من باب لا يحرسه أحد. فالتوسيع دخل **داخل
الدالّة المساعدة** التي بُني السياج حولها: يُصدّر حاملُ المنح كل شكوى في الموسم
**إلا ما كان عنه هو**. `test/export_permission_test.dart` يحرس ذلك بقراءة 0100
والتأكّد أن السياستين لم تُمسّا.

وما يبقى على أي backend بديل:

1. يطبّق التضييق نفسه على `scope=all` — الشاشة تطلب «الكل» والخادم يقرّر ما هو
   «الكل» لهذا المستخدم؛
2. يبقي **حجب اسم المشتكي** (§24.9) عاملاً في `complaints_list`؛
3. **يحرس `/export` بـ `export.data`**، ولم يكن محروساً قبل 0100 لأن الشاشة كانت
   تحرس نفسها بعرض ما يستطيع القارئ فتحه أصلاً — وتلك الحجّة هي بالضبط ما سقط.

**والخريطة كذلك محروسة بـ `map.view`** منذ 0100 — الشاشةُ يومها، والصفوفُ كذلك
منذ 0112. كان الاستدعاء يضيّق لكل قارئ (عضوٌ يرى أماكن ملفاته)، ثم لم يبقَ ما
يُضيَّق به حين صارت الدبابيس أماكن لا عقدَ ملفات، فانتقل الحرس إلى الدالّة نفسها:
انظر §31.1-0112.

### 28.3 ملاحظات على الملفات (لا تخصّ الخادم)

- **CSV**: يبدأ بـ BOM (وإلا قرأ إكسل العربية على صفحة ANSI فخرجت طلاسم)،
  ونهايات أسطر CRLF، وسطر `sep=,` لأن الفاصل الافتراضي في ويندوز العربي **فاصلة
  منقوطة**. وكل خلية تبدأ بـ `=`/`+`/`-`/`@` تُسبَق بفاصلة عليا — نصّ حرّ كتبه
  موظف لا يجوز أن يُنفَّذ كصيغة عند فتح الملف.
- **PDF**: بخط التطبيق نفسه (itfQomra) بعد **تحويله من CFF/OpenType إلى TrueType**
  في `assets/fonts/pdf/` — مكتبة `pdf` لا تستطيع تضمين CFF إطلاقاً. تغطية الخط
  للأشكال المتصلة يحرسها `test/pdf_font_test.dart`، لأن نقصها لا يُنتج خطأ بل
  صفحة مربّعات فارغة.
- اسم الملف **ASCII ومؤرَّخ** (`employees-2026-08-04.csv`)؛ العنوان العربي داخل
  الملف حيث يسلم.

---

## 29. التصعيد التلقائي — ملاحظة ما لم يحدث (migration 0086)

كل ما سبق في هذا المستند **ردّ فعل** على فعل: يُنشأ ملف، تُحرَّك مهمة، تُقدَّم
شكوى، فيستجيب trigger أو RPC. لا شيء في النظام كان يراقب **الغياب**. وفي موسم
مدّته أيام، الغياب هو الحدث: مخيّم لم يصل تقريره مساءً هو المخيّم الذي يجب أن
يذهب أحد إليه، ولم يكن يُعرف ذلك إلا بعد انتهاء الموسم عند قراءة الملف.

### 29.1 النطاق — ضيّق عن قصد

**التقرير الدوري فقط**، وبدوريّة `daily` أو `weekly` **فقط**. ملف بدوريّة `once`
يطلب تقريراً بلا تاريخ، فلا يوم يتأخر فيه — واختراع موعد له اختراعٌ لقرارٍ لم
تتّخذه الإدارة.

> **المهام المتأخرة لحقت بها في 0119**، على هذه الآلة نفسها وبساعتها نفسها
> (17:00 UTC خلف حارس `pg_cron`)، وبجدول منفصل تماماً: `escalate_late_tasks`
> لا يمسّ `report_misses` ولا يقرؤه. درجتان لا أربع — المُسنِد ثم المدير — لأن
> نظام المهام الشخصية **بلا هرم يُصعَد فيه**، وذلك بالضبط ما يجعله ليس الملفات
> التشغيلية. التفصيل في §25.2.

### 29.2 ما أُضيف

| الكائن                                  | الدور                                                       |
| --------------------------------------- | ------------------------------------------------------------ |
| `module_report_period(cadence)`          | تعريف «الفترة الحالية»، **مستخرج** من `submit_module_report`  |
| جدول `report_misses`                     | سجلّ ما هو **مستحقّ ولم يُرفع** (لا سجلّ ما أُرسل)             |
| `module_report_escalation_targets(…)`    | من فوق فلان، درجةً درجة                                       |
| `escalate_missing_reports()`             | المرور اليومي                                                 |
| مهمة pg_cron `escalate-missing-reports`  | `0 17 * * *` — أي ٢٠:٠٠ بتوقيت مكة                            |

### 29.3 السلّم

مأخوذ من بنية الملف لا مخترَعاً: النوع يعلن **مستوياته** (الأول هو الأخرج) وكل
node يسمّي أباه (0024). فمن فوق مشرف البرج هو من يحمل منصباً على قطاع ذلك البرج،
ومن فوقه من يحمل منصباً على الملف نفسه.

| الدرجة | من يُبلَّغ                    | متى            |
| ------ | ----------------------------- | -------------- |
| ٠      | صاحب التقرير نفسه             | مساء اليوم     |
| ١      | حاملو المناصب على العقدة الأب | اليوم التالي   |
| ٢      | حاملو المناصب على الملف       | اليوم الذي يليه|

ثم يتوقف. الإشعار في الدرجتين ١ و٢ **يسمّي الشخص** — مشرف يُقال له «أحدهم متأخر»
أُعطي همّاً لا عملاً، والغرض من رفع الأمر أن يذهب من فوقه فيسأل.

### 29.4 التزامات الخادم

1. **`module_report_period` هي مصدر الحقيقة الوحيد للفترة.** لو اختلف تعريف
   المراقِب عن تعريف المُقدِّم بساعة، لَنبّه رجلاً عن نافذة رفع فيها بالفعل، أو
   سكت عن نافذة لم يرفع فيها. **أي backend بديل يجب أن يشتقّ الاثنين من تعبير
   واحد.**
2. **التوقيت UTC** — كما كان `submit_module_report` دائماً. ليس مثالياً لبعثة
   تعمل على UTC+3، و**لم يُصحَّح عمداً**: تصحيحه في أحد الموضعين دون الآخر يصنع
   بالضبط الاختلاف الذي تمنعه النقطة ١. إن صُحِّح فليُصحَّح **هنا، مرة واحدة**.
3. **الأسبوعي لا يُنبَّه إلا في آخر أيامه** (`current_date >= period_start + 6`).
   نظام يذكّر كل ليلة من الاثنين سيُتجاهَل بحلول الأربعاء.
4. **`escalate_missing_reports()` ممنوعة على `authenticated`.** تكتب في صناديق
   الآخرين؛ لو تُركت متاحة لأمكن لأي حساب تفريغ السلّم كله في إشعارات القيادة.
   يشغّلها المجدول بصلاحية مالك القاعدة.
5. **الإشعار بلا مُرسِل** (`sender_id = null`) — النظام يلاحظ، لا شخص يشتكي.
6. `data.type = 'report_overdue'` مع `module_id`؛ التطبيق يفتح الملف عند اللمس
   (قائمة الأنواع المسموحة في `app_notification.dart`).

### 29.5 التشغيل

`pg_cron` امتداد **يجب تفعيله على المشروع أولاً**. الـ migration يتحقق منه ويطبع
`notice` بدل أن يفشل — كي لا يُعطِّل كل migration بعده:

```sql
create extension pg_cron;
-- ثم أعد تشغيل 0086 كاملاً — كل عباراته قابلة لإعادة التنفيذ
```

**بلا `with schema`:** ملفّ تحكّم pg_cron يثبّت مخططه على `cron` وهو غير قابل
للنقل، فتسمية مخطط آخر **خطأ** لا تفضيل. وعلى Supabase يمكن تفعيله أيضاً من
Database → Extensions — وهو الشيء نفسه بزرّ.

**علامة أن التفعيل لم يحدث:** الـ migration يمرّ بنجاح ولا يجدول شيئاً. الجدول
والدوال موجودة ولا أحد يستدعيها — وهو ما يشبه ميزة عاملة تماماً، إلى أن يمرّ أول
تقرير غائب بلا أن يلاحظه أحد.

**نُفِّذ على قاعدة حيّة** (2026-08-04). يبقى التحقق من أمرين لا يظهران وقت
التنفيذ: أن `pg_cron` مفعَّل فعلاً — وإلا طبع الـ migration `notice` ومضى بلا
جدولة — وأن أول مرور ليلي أنتج ما يُتوقَّع:

```sql
select * from cron.job where jobname = 'escalate-missing-reports';
select escalate_missing_reports();   -- يُعيد عدد الإشعارات المُرسَلة
select count(*), rung from report_misses group by rung;
```

---

## 30. تسجيل الوصول Check-in (migrations 0098 + 0099، تُلغيان 0087/0094)

النظام يعرف من **يحمل** منصباً منذ 0024. ما لم يستطع قوله قط هو هل هو **واقف**
هناك الآن، وهو السؤال الذي تطرحه غرفة العمليات فعلاً.

**بُني هذا مرّتين، والفرق بينهما هو ما صار عليه المكان.** 0087 جعل التسجيل حقيقةً
عن **ملف تشغيلي**: `node_check_ins.node_id` يشير إلى عقدة، والورقة تُفتح من صفحة
الملف، واللوحة لكل ملف. كان ذلك صحيحاً ما دام المكان **عقدة**. ثم غيّر
0095/0096/0097 ما هو المكان: الفندق والمخيم صارا **مدخلين في قائمة مرجعية**،
يُختاران داخل الملفات لا يُنشآن فيها. وبقي التسجيل مشيراً إلى الورق بعد أن انتقل
المكان من تحته — ولذلك كان المخيم الواحد المُدخَل في ملفين يحمل سجلَّين، وفندقٌ
في المدينة لا يقف في ملف أصلاً لا يحمل حضوراً مهما نام فيه من نام.

### 30.1 القاعدة التي انقلبت

0087 حاجّ — بإسهاب وبحقّ — أن التسجيل المشكوك فيه **يُحفظ ويُوسم ولا يُرفض
أبداً**، لأن الرفض يترك رجلاً واقفاً في مكانه بإشارة رديئة عاجزاً عن الإبلاغ.

كان ذلك صحيحاً ما دام الحضور ملاحظةً في سجلّ لا يرسمها أحد. وكفّ عن الصحّة في
0090، حين بدأت `season_map` ترسم دبابيس من هذا السجلّ: **تسجيلٌ يمكن رفعه من غرفة
فندق في مكة لمخيّم في منى يُفسد كل دبّوس أخضر على الخريطة**، وخريطةٌ تكذب أسوأ من
لا خريطة — لأن الغرفة تكفّ عن الاتصال للتأكّد.

فالقرب **مفروض** الآن، و**دقّة الهاتف تُطرح قبل فرضه**: فِكسٌ يقول «٥٠٠م ± ٤٠٠م»
ليس دليلاً على أن أحداً كان في المكان الخطأ ويُقبل؛ و«٥٠٠م ± ٨م» دليلٌ ويُرفض.
وهي حسبة `isFarFromPlace` نفسها التي كتبها 0087، منقولةً من الوسم إلى المنع.

**والثمن يُقال صراحةً: مكانٌ بلا إحداثيات لا يقبل شيئاً.** تقرير 0098 يعدّها في
عمود `unpinned`، والموسم لا يبدأ حتى يصير صفراً.

### 30.2 ما أُضيف

| الكائن | الدور |
| ------ | ----- |
| `reference_sets.is_place` | أيّ القوائم أماكن. `hotels` و`camps` — لا القطاعات ولا المراكز ولا التكتلات |
| حقل `check_in_radius_m` | تخصيص النطاق لكل مدخل. **حقل مخطَّط لا عمود**، فيرسم ويُحفظ ويُدقَّق نفسه بلا Dart |
| `place_radius_m(item)` | النطاق، افتراضه ٢٠٠م، محروسٌ بـ regex ومحصورٌ بين ٢٠ و٥٠٠٠ |
| `place_location(item)` | إحداثيات المدخل. النصف الثاني من 0092 مرفوعاً، ويطابق **بالنوع** |
| جدول `place_codes` | السرّ القابل للتجديد، ومتى جُدِّد وبيد من، و`warned_at` للدورة الجارية (0114) |
| `place_code(item)` / `rotate_place_code(item)` | القراءة والتجديد. **بلا مهلة سماح**. `place_code` تُعيد `due_at` منذ 0114 |
| جدول `place_code_policy` | صفٌّ واحد: `rotate_every_days` (افتراضه ٣٠) و`warn_before_days` (٣) و`is_enabled` |
| `run_place_code_rotation()` | المرور اليومي: ينذر ثم يجدّد ما تجاوز مدّته. **لا يُستدعى من عميل** |
| جدول `place_check_ins` | الموقع والمسافة و**النطاق الذي قِيس عليه** — كلّها `not null` |
| RPC `check_in_at_place(…)` | التسجيل. يُعيد اسم المكان والمسافة |
| RPC `presence_board(since, item, season)` | آخر تسجيل لكل شخص في كل مكان، للموسم كلّه |
| RPC `presence_gaps(within, item, season)` | **المعكوس** (0110): المناصب على أماكن بلا تسجيل خلال المدة — بالهواتف |

**ولا عمود `method`.** التسجيل رمزٌ وموقع ولا شيء غيرهما، فنوعٌ بقيمة واحدة لا
يسجّل إلا أن الترحيل كان مختلفاً من قبل.

### 30.3 التزامات الخادم

1. **المسافة تُقاس في الخادم ولا تُقبل من العميل أبداً.** لا سياسة `insert` على
   `place_check_ins` إطلاقاً. وهذا أشدّ إلزاماً مما كان: المسافة **تقرّر** الآن
   بعد أن كانت تعلّق، ومسافةٌ يقدّمها العميل قاعدةٌ يستطيع إطفاءها.
2. **ترتيب الفحوص هو الحجّة.** كل رفض هو **أقبل ما يمكن العمل به** عند تلك
   النقطة، لأن الواقف عند بوابة لا يستطيع العمل إلا بأول ما يُقال له:

   | # | الفحص | الرمز |
   |---|-------|-------|
   | ١ | `is_approved()` | `check_in_not_approved` |
   | ٢ | المدخل قائم ونشط وقائمته مكان | `check_in_not_a_place` |
   | ٣ | الموقع موجود وعلى الكرة | `check_in_needs_a_position` |
   | ٤ | السرّ يطابق الحالي | `check_in_code_expired` |
   | ٥ | `place_location` تُرجع صفاً | `check_in_place_has_no_location` |
   | ٦ | `worst_case <= place_radius_m` | `check_in_too_far` + `detail` بالرقمين |

   الموقع **قبل** السرّ عمداً: «فعّل خدمة الموقع» شيءٌ يفعله وهو واقف مكانه.
3. **السرّ يُخزَّن نصّاً صريحاً، وهذا قرار.** إنه **مطبوعٌ على جدار**: سرّيته
   ماديّة لا تعمّوية، ومن يقف عند البوابة يملكه — وذاك هو التصميم. تعميته تمنع
   الشيء الوحيد الذي يجب أن يحدث: طباعته. الذي يحميه RLS: من يقرأ العمود هو من
   يطبعه، ولا يظهر في أي قائمة ولا خريطة ولا `select *`.
4. **لا سياسة كتابة على `place_codes` إطلاقاً.** سرٌّ يستطيع العميل كتابته سرٌّ
   يستطيع إعادته إلى قيمة صوّرها، وهو نقضٌ كامل للتجديد.
5. **لا مهلة سماح عند التجديد.** إبقاء السرّ القديم حيّاً ساعة هو إعادة فتح الثغرة
   ساعةً كاملة — وهي بالضبط الساعة التي يقف فيها أحدهم ممسكاً بالصورة.
5b. **التجديد المجدوَل يُنذِر قبله، ولا يكون يومياً** (0114). الرمز **ملصق على
   جدار**: تجديده لا يحدّث شيئاً بل يُبطل أربعين ورقة، فلا يُسجَّل وصولٌ في أي مكان
   حتى تُعاد الطباعة والإلصاق. لذلك:

   - المدّة **سياسة** لا ثابت، وحدّها الأدنى `7` بقيد `check` — تجديدٌ كل ٢٤ ساعة
     ممنوع بالبنية لا بالاتفاق، لأنه صباحُ إعادة طباعةٍ كل يوم، واليومُ الذي لا
     يُنجَز فيه هو يومٌ بلا سجلّ وصول أصلاً.
   - يسبقه إنذار بـ `warn_before_days` إلى حاملي `checkin.rotate` — إشعارٌ واحد
     يحمل **العدد** لا إشعاراً لكل مكان، لأن العمل الذي يستدعيه طباعةُ قائمةٍ
     كاملة. و`warned_at > rotated_at` هو الشرط، فيُنذَر مرة واحدة في الدورة.
   - `place_code.due_at` تحمل التاريخ إلى بطاقة الملصق، فيراه من يفتح الصفحة دون
     انتظار إشعار. `null` تعني «لا تجديد تلقائي» (`is_enabled = false`).
   - الترحيل **لا يجدّد شيئاً عند تطبيقه**؛ يعيد ضبط `rotated_at = now()` للجميع،
     فأول تجديد تلقائي يبعد مدّةً كاملة ويسبقه إنذاره.

   وما يشتريه التجديد اليومي قليل: منذ 0098 يُرفض التسجيل خارج نطاق المكان، فحاملُ
   الصورة لا بدّ أن يقف عند البوابة — حيث كان يستطيع مسح الجدار.
6. **القراءة**: تسجيلك دائماً بلا صلاحية، وتسجيلات الجميع لحامل `checkin.board`.
   و`presence_board` **تُعيد ذكر** الشرط لأن `security definer` لا ترث RLS.
7. **العضوية لم تعد بوّابة.** 0087 كان يشترطها؛ والمكان ليس في ملف. الوقوف عنده
   بالرمز أمامك هو الاعتماد.

### 30.4 الرمز

```
hajjops://check-in?p=<item_uuid>&k=<secret>&c=<lat>,<lng>&r=<radius_m>
```

`c` و`r` إحداثيات المكان ونطاقه **منسوخين على الملصق**، ليستطيع هاتفٌ بلا شبكة أن
يرفض بصدق بدل أن يحفظ كذبة. وعليهما قاعدة تُقال مرّة وتُطاع في كل مكان:

> **لا يجوز أن يسبّبا إلا رفضاً، ولا يجوز أن يسبّبا قبولاً أبداً.**

الخادم يعيد حساب الاثنين وهو وحده من يكتب صفاً — فملصقٌ مزوَّر يدّعي أن المخيم حيث
يقف الهاتف يمرّ من الهاتف ويُرفض من قاعدة البيانات. وملصقٌ طُبع قبل هذا الحقل لا
يحملهما، فيأخذ جواب الخادم كما كان: `null` تعني «لا فحص محلي ممكن» لا «لا فحص».

### 30.5 على الجهاز

- **الموقع يُلتقط لحظة الضغط لا لحظة الإرسال** — وإلا حمل التسجيل إحداثيات المكان
  الذي أُرسل منه أخيراً، وقد يكون فندقاً في مكة بعد ساعات.
- **انعدام الموقع يُرفض فوراً ولا يُحفظ في الطابور أبداً.** لا شيء يُنتظر: الخادم
  لن يقبله في أي لحظة لاحقة، وحفظه يعني إخبار الرجل بأن وصوله حُفظ ثم رميه بعد
  ساعات — وهو العطب الذي وُجد الطابور لمنعه، معاداً من الطرف الآخر.
- **إحداثيات الملصق تُستشار على مسار عدم الاتصال وحده.** لو فُحصت قبل كل إرسال،
  لجعل **تصحيحُ دبّوس** في البيانات المرجعية كلَّ ملصقٍ على جدار يرفض رجلاً واقفاً
  في مكانه الصحيح — أرقام الملصق البائتة تغلب أرقام الخادم الطازجة. الخادم
  مرجعٌ وطازج على الإنترنت؛ والنسخة تُستشار حين لا يوجد أفضل منها، وهي الحالة
  الوحيدة التي طُبعت من أجلها.
- **نوع الطابور صار `checkin.place`** بدل `checkin.arrive` — عمداً: صفٌّ كتبه بناءٌ
  أقدم يحمل `module_id` و`node_id` ولا مكان يُرسل إليه. مسار «النوع المجهول»
  **يحجزه** بدل أن يُسقطه، فيقرّر صاحبه بنفسه.
- **التأكيد يسمّي المكان والمسافة**: «سُجّل وصولك — فندق الأنصار، على بعد ٤٠م».
  الفحص الوحيد ضدّ ملصقٍ أُلصق على البوابة الخطأ، وهو الخطأ الذي يجعل السجلّ
  كلّه يكذب.
- **لا يُسجَّل `hajjops://` رابطاً عميقاً على المنصّة**، ويبدو ذلك نقصاً وليس كذلك:
  تسجيله يجعل رمزاً مصوَّراً في مجموعة واتساب قابلاً للنقر — وهو بالضبط ما وُجدت
  قاعدة الموقع لمنعه.

### 30.6 ما تغيّر في الخريطة

`season_map` أُعيد إنشاؤها في **0098 قبل حذف أي شيء**: جسمها `language sql`
بعلامات `$$`، فلا تسجّل Postgres أي تبعية — وحذف `node_check_ins` أولاً كان
لينجح ويترك الدالّة معطوبةً حتى يفتح أحدهم الخريطة.

- عدّاد الحاضرين يُحسب على **المدخل** لا العقدة. فعقدتان على مدخل واحد (المخيم ١٦
  بخيمتيه، وهو سبب وجود `entry_slot` في 0095) تعرضان العدد نفسه — وهو الصواب:
  الحضور حقيقة عن المخيم، وكلا الملفين يريد معرفة أنه مأهول.
- **عمود `can_check_in` جديد.** بدونه تُقرأ عقدةٌ لا تحمل مدخلاً على أنها
  `unmanned` — دبّوسٌ ملوَّن يتّهم لا أحد، إذ لا سبيل لتثبيت رمز عليها أصلاً.
  و`PlaceCondition` كسبت حالةً رابعة `uncoded`.
- **الفنادق السائبة صار لها عدّاد حقيقي** بدل صفر مثبَّت. وهذا أكبر مكسب مفرد في
  الإصلاح: فندق المدينة ليس عقدةً في ملف، ولم يكن يستطيع حمل حضورٍ إطلاقاً.
- وفي الطريق: `loose_hotels` كانت تقرأ `[?&]q=` وحدها من ثلاث صيغ، **فكل فندق
  ثُبّت بزرّ المشاركة من Google كان غائباً عن الخريطة بصمت**. صار يمرّ عبر
  `place_location`.

### 30.6a سجلّي أنا — الحقّ الذي لم يُنفَق

§30.3 البند ٦ يقول منذ 0098: **«تسجيلك دائماً بلا صلاحية»**، وسياسة
`place_check_ins` تفتتح بـ `profile_id = auth.uid()`. ومع ذلك لم يكن في التطبيق
مكانٌ لصرف هذا الحقّ: الشاشة الوحيدة التي تقرأ هذا الجدول كانت لوحة الغرفة، خلف
`checkin.board`. فمن مسح الرمز يستطيع تسجيل وصوله ولا يستطيع بعدها أن يرى أنه
سجّل — وهو الشيء الوحيد الذي قد يريد التحقّق منه، وبالضبط ما يُسأل عنه إن نُوزع
في حضوره.

**لا endpoint جديد ولا صلاحية.** قراءة مباشرة من الجدول عبر ما تسمح به السياسة
أصلاً — لا عبر `presence_board`، وهذا فرقٌ جوهري لا تفصيل: اللوحة تجيب «أين هو»
بصفٍّ واحد لكل شخص في كل مكان (`distinct on`)، والسجلّ الشخصي هو السؤال الآخر
الذي سمّاه 0098 ولم يُجبه: **أين كنتُ كلَّ مرّة**. طيُّ تاريخي إلى آخر زيارة لكل
فندق يحذف بالضبط ما وُجد السجلّ له.

**والتزام واحد على أي backend بديل**: أن يبقى `GET` على تسجيلات الشخص نفسه بلا
حراسة، وأن يُعيد **كل** الصفوف لا آخرها لكل مكان.

**والمسافة تبقى معروضة على كل صفّ**، وهي ليست زينة: القاعدة قبلت هذا الوصول
**بقياسها هي** (§30.3)، وسجلٌّ يُخفي القياس يطلب أن يُصدَّق على كلمته — وهو الشيء
الوحيد الذي بُني هذا الجدول ألّا يحتاجه.

---

### 30.6b الوجه الآخر — من **لم** يسجّل (migration 0110)

`presence_board` يجيب «أين هو». وغرفة العمليات تقضي أيامها الخمسة على النصف
الآخر: **المنصب المشغول على الورق والفارغ في الأرض**.

ولوحة الحضور لا تجيبه بنيوياً. تُقرأ بالبحث عن اسم وعدم إيجاده — وهذا يعمل مع
أحد عشر اسماً، ويصير **إحصاءً** مع أربعمئة. لا أحد يلحظ غياباً بمسح قائمة حضور.

والنظام يعرف مراقبة ما لم يحدث منذ 0086 — «أول شيء في هذا النظام يراقب ما لم
يحدث» — وكانت مصوَّبة على التقارير وحدها. هذه توجّه الفكرة نفسها إلى الحضور.

| البند | القاعدة |
| --- | --- |
| **من يُتوقَّع؟** | حاملُ دورٍ على عقدة يستمدّ اسمها من **مكان** (`reference_sets.is_place`). لا «كل من في الموسم» — فيصير موظفو الإدارة غائبين عن منى |
| **متى يُعدّ غائباً؟** | خارج نافذة افتراضها **١٢ ساعة** — الرقم نفسه الذي تفترضه اللوحة، فلا تختلف الشاشتان على معنى «حديثاً» |
| **لم يُسجَّل قط ≠ سُجِّل قديماً** | جوابٌ واحد عن «أهذا المنصب مشغول؟»، ويُفرَّق بينهما في الخرج: `last_seen` فارغ للأول وزمنٌ للثاني. التاسع ساعةً مكالمة، ومن لم يسجّل قط **قد لا يكون أُعطي الرمز أصلاً** |
| **الهواتف في الخرج** | كل صفّ اسمٌ يوشك أحدهم أن يتصل به. قائمةٌ تُلجئ القارئ إلى البحث عن كل رقم في الدليل تضيف خطوةً إلى الفعل الوحيد الذي وُجدت لتحفزه |
| **بلا صلاحية جديدة** | `checkin.board` يعني «له أن يرى من أين» — ومن **ليس** أين هو الحقيقة نفسها من الجهة الأخرى. كودٌ ثانٍ يتيح منح أحدهما ومنع الآخر، وهو تمييزٌ بلا شيء خلفه |

**ولاحظ عدم التماثل، وهو مقصود وهو من 0098:** الإسناد يقرّر من **يُتوقَّع**، ولا
يقرّر من **يجوز له** التسجيل — فـ0098 شطب العضوية كبوّابة صراحةً («الوقوف عنده
بالرمز أمامك هو الاعتماد»). من يغطّي عن زميل يسجّل ويظهر على اللوحة، وليس هو ما
يبحث عنه هذا التقرير: التقرير يسأل عن **مناصب فارغة**، لا عن غرباء.

**والتزام الخادم**: الحارس يُعاد ذكره داخل الدالة — `security definer` لا يرث
سياسات الجداول، وبدونه تُسلّم الدالة إسنادات البعثة وكل رقم هاتف فيها لأي
مُستدعٍ. والحسابات الموقوفة وغير المعتمدة مستثناة: الحساب الموقوف ليس منصباً
فارغاً بل حسابٌ لا ينبغي أن يكون مُسنَداً أصلاً، ومكانه شاشة أخرى.

---

### 30.7 الصلاحيات

| الرمز | ما يفتحه |
| ----- | -------- |
| — | **تسجيل وصولك أنت**: لا صلاحية. نظامٌ لا يُبلّغ فيه عن موضعه إلا بعضهم نظامٌ لا يعرف موضع أحد |
| `checkin.board` | قراءة حضور الجميع. `/presence` محروسٌ به |
| `checkin.codes` | عرض الرموز وطباعتها ومشاركتها. السرّ قابل للطباعة، فمن يقرؤه هو من يطبعه |
| `checkin.rotate` | التجديد. يستلزم `checkin.codes` — إبطال أربعين ملصقاً يقتضي رؤيتها أولاً |

`module_type_levels.is_place` (0089) **باقٍ ومعناه تغيّر**: يقرّر ما ترسمه
الخريطة، ولم يعد يقرّر أين تُطبع الرموز. تعليق العمود صُحّح، وإلا وصّلها القارئ
التالي بالرموز من جديد.

### 30.8 ما حُذف (0099)

`node_check_ins` و`check_in_here` و`module_presence` والنوع `check_in_method`.
والصفوف **تُحذف ولا تُرحَّل**: صفٌّ قديم بلا سرّ وبلا نطاق ومسافتُه مقيسة تحت
قاعدةٍ كانت تمرّر كل شيء — 0087 قبل تسجيلاً بلا موقع أصلاً، و0094 قبل واحداً على
بعد ٩٠٠م. ترحيلها يضع على اللوحة الجديدة صفوفاً كانت قاعدتها لترفضها، وأول ما
يُسأل عن سجلّ هو هل كل ما فيه يخضع للقاعدة نفسها.

**لم يُنفَّذ بعد.** ونّفذ 0098 أولاً واقرأ تقريره؛ لا تشغّل 0099 حتى يعمل كل شيء.

---

## 30. البلاغ العاجل Incidents (migration 0088)

الشكوى تطلب **حُكماً لاحقاً**؛ البلاغ يطلب **أن يأتي أحد الآن**. سجلّان مختلفان
عن قصد، وكل خاصية في §24 صحيحة هناك وخاطئة هنا.

| | الشكوى (§24) | البلاغ (§30) |
| --- | --- | --- |
| هوية المُبلِّغ | محجوبة بنيوياً | **معروضة أولاً** — يلزم الاتصال به |
| النموذج | فئة + جهة + هدف | **سطر نصّ واحد** |
| الترتيب | الأحدث أولاً | **الأقدم أولاً** بين المفتوحة |
| الغلق | `complaints.lock` | دورة حياة: `open → in_progress → closed` |

### 30.1 ما أُضيف

| الكائن | الدور |
| --- | --- |
| نوع `incident_state` | `open` / `in_progress` / `closed` |
| جدول `incidents` | البلاغ وموقعه ودورته |
| جدول `incident_attachments` | الأدلة |
| دلو `incidents` (السابع) | `{incident_id}/{i}_{file}` |
| `incidents.receive` / `incidents.handle` | صلاحيتان، والثانية تستلزم الأولى |
| `raise_incident(...)` | يرفع البلاغ **ويُنذر** في معاملة واحدة |
| `set_incident_state(...)` | تحريك الدورة |
| `incidents_list(...)` | السجلّ |
| `has_permission_for(profile, code)` | **جديد وعام** — انظر 30.3 |
| `subject_profile_id` / `app_route` / `app_label` | **0120** — عمّ يتكلّم البلاغ، اختياري بالكامل؛ انظر 30.6 |
| `incidents.delete` | **0121** — صلاحية ثالثة، تستلزم `incidents.receive`؛ انظر 30.7 |
| `delete_incident(...)` / `delete_all_incidents(...)` | **0121** — الشطب، وتُرجعان مسارات التخزين اليتيمة |
| `incidents_delete` (سياسة) | **0121** — السياسة التي لم تكن للجدول قط |
| trigger `audit_row` على `incidents` و`incident_attachments` | **0121** — ما أغفلته 0088 (§23) |
| `p_mine_only` على `incidents_list(...)` | **0126** — بلاغاتي؛ يفرض `reporter_id = auth.uid()` بصرف النظر عن الصلاحية؛ انظر 30.9 |
| `/my-incidents` + `IncidentsScope.mine` | **0126** — سجلّ الشخص عن نفسه، بلا صلاحية؛ انظر 30.9 |

### 30.2 التزامات الخادم

1. **الإنذار داخل نفس معاملة الإدراج.** ليس trigger ولا استدعاء من التطبيق: لو
   وُجد الصفّ فقد أُبلغت الغرفة. عميل يكتب الصفّ ثم يموت قبل الإشعار يُنتج
   الفشل الوحيد الذي لا تحتمله هذه الميزة — **طوارئ مسجَّلة لا يعلم بها أحد**.
2. **الرفع بلا صلاحية** (`incidents_insert` تشترط `reporter_id = auth.uid()`
   فقط). نظامٌ لا يُبلِّغ فيه إلا المخوَّلون عن عطل حافلة هو نظام لا يعلم بالعطل.
3. **المُبلِّغ لا يغلق بلاغه** (`incidents_update` لحاملي `incidents.handle`).
   «صار الوضع جيداً» من صاحب البلاغ ليست حقيقة «ذهب أحد وعالجه».
4. **`handled_at` تُختم على أول من يتولّاه ولا تُكتب فوقها** — السؤال هو «كم
   جلس بلا جواب»، وحقلٌ يتحرّك مع كل تعديل لا يجيبه.
5. **المرفقات تُرفع بعد الإنذار** لا قبله (§30.4).

### 30.3 `has_permission_for` — دالة جديدة عامّة

`has_permission` تسأل عن **المستدعي**. الإنذار يسأل عن **الآخرين**، ولم يكن في
المخطط ما يفعل ذلك. تُطابق `has_permission` (0012) حرفياً بما فيه شرطان يسهل
إغفالهما ويَغلى ثمنهما: **الحساب الموقوف وغير المعتمد لا يحمل صلاحية**. بدونهما
يذهب الإنذار إلى من أوقفته الإدارة — ويُحتسب أنه وصل.

### 30.4 ترتيب العمليات في العميل

`raise_incident` أولاً، ثم رفع المرفقات. صورة تستحق الحفظ ولا تستحق الدقيقة التي
تسبق إبلاغ أحد — وعلى شبكة ميدانية قد تكون خمساً.

### 30.5 حالة ثالثة في العميل

`IncidentOutcome` ثلاث لا اثنتان: `sent` / `waitingForNetwork` / `failed`.
«حُفظ وسيُرسل» نجاحٌ في كل مكان آخر في هذا التطبيق و**كذبةٌ هنا**. الحالة
الوسطى تفرض على كل نقطة استدعاء أن تقول صراحة: **لم يُبلَّغ أحد — اتصل هاتفياً**.

### 30.6 عمّ يتكلّم البلاغ؟ (migration 0120)

قاعدة 0088 لم تتغيّر: **سطر نصّ واحد، ولا حقل مطلوب غيره**. كل ما في 0120
اختياري، ولا يُسأل عنه، ويُرسَل البلاغ كما كان يُرسَل تماماً إن لم يُلمَس.

ما أُضيف هو جواب السؤال الذي تسأله غرفة العمليات لحظة وصول البلاغ: **عن ماذا؟**
كان المخطط يحمل جواباً واحداً — الملف الذي كان واقفاً فيه — وهو `null` في أغلب
البلاغات. صارت ثلاثة:

| العمود | متى | لماذا لا يكفي النصّ |
| --- | --- | --- |
| `module_id` / `node_id` | ملف أو برج | كما في 0088، بلا تغيير |
| `subject_profile_id` | **شخص** | «سائق الحافلة ٤ لم يحضر» — الغرفة تحتاج أن **تتصل به**، والاسم داخل النثر لا يُتصل به من السجلّ |
| `app_route` + `app_label` | **صفحة في التطبيق** | «شاشة المناوبات تعرض قائمة أمس» — ليست طوارئ أصلاً، وكانت تصل بشكل الطوارئ تماماً |

**العمودان معاً للصفحة، وليس تكراراً.** `app_route` هو ما يجعلها **رابطاً**
يفتحه السجلّ؛ `app_label` هو ما يجعلها **مقروءة** — هنا وفي نصّ الإشعار — لقاعدة
لا تملك قائمة التطبيق ولا تترجم `/employees` إلى «الموظفون» بلغتين. وهو لقطة بالمعنى
نفسه الذي يكون فيه `body` لقطة: يسجّل ما كانت تلك الشاشة تُسمّى **يوم أُبلغ عنها**،
وإعادة تسميتها بعد ستة أشهر يجب ألّا تعيد كتابة البلاغ.

**من يقرأ لم يتغيّر، وهذا القرار الذي يستحق النصّ.** موضوع البلاغ **لا يكتسب حقّ
قراءته**: `incidents_select` تبقى للمُبلِّغ وغرفة العمليات ولا أحد غيرهما، فمن سُمّي
في بلاغ عاجل لا يستطيع الاطّلاع عليه ولا معرفة من سمّاه. وليست هذه سرّية §24
البنيوية تدخل من الباب الخلفي — اسم المُبلِّغ ما يزال على السجلّ ويُعرض أولاً — بل
فقط أن **كونك مكتوباً عنك ليس مطالبةً بالسجلّ**.

**البلاغ عن النفس يُسقَط لا يُرفض.** `nullif(p_subject_profile_id, auth.uid())`:
رجلٌ في عجلة ضغط على اسمه أرسل بلاغاً صحيحاً، ورفضه من أجل ترتيب عمود اختياري
أسوأ صفقة في الميزة كلها.

#### توقيعان تغيّرا (كسر متوافق مع القديم في الاستدعاء، لا في التعريف)

الدالتان **تُحذَفان وتُنشآن** لا تُستبدلان: معاملان مؤجَّلان إضافيان يعنيان
overload جديداً عند Postgres، والصيغة القديمة الباقية بجانبه تجعل كل استدعاء
ملتبساً؛ و`incidents_list` يتغيّر نوع رجوعها، وهذا ما لا يقدر عليه
`create or replace` أصلاً.

```
raise_incident(
  p_body text,
  p_module_id uuid default null,
  p_node_id uuid default null,
  p_lat double precision default null,
  p_lng double precision default null,
  p_accuracy double precision default null,
  p_subject_profile_id uuid default null,   -- جديد
  p_app_route text default null,            -- جديد
  p_app_label text default null             -- جديد
) returns uuid
```

`incidents_list(p_include_closed, p_limit)` تُرجع خمسة أعمدة إضافية:
`subject_id`, `subject_name`, `subject_phone`, `app_route`, `app_label`.
و`subject_phone` — `phone_sa` ثم `phone_sy` كالمُبلِّغ — لمكالمة ثانية مختلفة:
الغرفة تتصل بالمُبلِّغ لتسأل ماذا جرى، وبالموضوع لتعرف أين هو.

**ترتيب الإشعار:** البرج، ثم الملف، ثم **الشخص**، ثم الصفحة. ترتيب إلحاح لا
ترتيب دقّة: بلاغ يسمّي شخصاً يسبق بلاغاً يسمّي شاشة، لأن أحدهما إنسان قد لا يكون
حضر.

**والأربعة كلّها تُفتَح الآن.** كان الموظف والصفحة يُفتحان والملف اسماً مكتوباً لا
يُفتح — السجلّ يقول للقارئ أين ينظر ولا يدعه. لا شيء في العقد يتغيّر لهذا:
`module_id` و`module_name` و`node_label` كانت تصل من `incidents_list` منذ 0088.

**فتح السجلّ على بلاغ بعينه.** الوصول إليه من إشعارٍ في الصندوق (§16.8) رحلةٌ
أخرى: القارئ يحمل بلاغاً واحداً في ذهنه، لا قائمةً يبحث فيها عنه. وليست هناك
`incident_by_id` — ولا يُضاف واحدة: العميل ينادي `incidents_list` نفسها ويُصفّي،
بشرطين لا ثالث لهما:

1. `p_include_closed = true` — البلاغ قد يكون عولج والقارئ في طريقه إلى الهاتف،
   وفتح السجلّ عليه فلا يجده لأن المغلقة مخفيّة هو التطبيق يفقد ما أعلنه للتوّ.
2. `p_limit = 500` بدل المئة. الترتيب `(state = 'closed'), created_at` — أي
   المفتوح الأقدم أولاً — فبلاغٌ رُفع هذه الدقيقة يقع في آخر القائمة، والمئة مقاسٌ
   صحيح لشاشةٍ تُراقَب وخاطئ للعثور على صفٍّ واحد في موسم.

**التزام على أي backend بديل:** إن أضاف `GET /incidents/{id}` فليكن بالصلاحية
نفسها التي في §30.7 حرفاً بحرف — `incidents.receive`، أو أن يكون الطالب هو
المُبلِّغ. وإن لم يُضِفه، فالطريق أعلاه يعمل بلا شيء جديد. وحين لا يوجد البلاغ في
ما رجع، تُقال الجملة الصحيحة: **«لم يعد هذا البلاغ في السجلّ»** لا «لا بلاغات» —
الثانية عكس ما قيل لصاحبها قبل ثوانٍ.

**لم يُنفَّذ هذا الـ migration على قاعدة حيّة بعد.**

### 30.7 شطب البلاغ وتفريغ السجلّ (migration 0121)

0088 أعطت الجدول سياسة إدراج وسياسة قراءة وسياسة تحديث — **ولا سياسة حذف**. في
جدولٍ عليه RLS هذا ليس إغفالاً في التوثيق: إنه يعني أن **لا أحد** يستطيع الحذف،
ولا الأدمن. وكان ذلك مقبولاً والسجلّ في أسبوعه الأول، ويتوقّف عن كونه مقبولاً في
الأسبوع الثاني من الموسم حين تكون القائمة التي تُقرأ الثالثة فجراً تسعة أعشارها
رسائل تجربة ومكرَّرات ومغلقاتِ ستّة أيام مضت. **سجلٌّ لا يُفرَّغ سجلٌّ يتوقّف عن
أن يُقرأ، وسجلٌّ يتوقّف عن أن يُقرأ هو الفشل الذي بُنيت هذه الميزة لمنعه.**

#### الصلاحية

`incidents.delete` — ثالثة، وتستلزم `incidents.receive` مثل `incidents.handle`
تماماً. **لا تُطوى في `incidents.handle`:** الغلق يقول «ذهب أحدٌ وعالجه»، والحذف
يقول «لم يحدث هذا»، وليس الثاني هو الأول بثقةٍ أكبر. من يجيب الطوارئ ليس بالضرورة
من يُسمح له بمحو أثر أنها رُفعت.

#### السياسة

```sql
create policy incidents_delete on incidents for delete
  using (is_admin() or has_permission('incidents.delete'));
```

**ولا تُمنح للمُبلِّغ عن قصد.** رجلٌ يرفع طوارئ ثم يبدو له غير ذلك يجب ألّا يسحبها
من قائمة الغرفة: الغرفة أُوقظت فعلاً، و«تبيّن أنه لا شيء» شيءٌ **يُغلقونه** لا
شيءٌ يحذفه هو.

#### الدالّتان

```
delete_incident(p_incident_id uuid) returns text[]
delete_all_incidents(p_only_closed boolean default true)
  returns table (deleted int, paths text[])
```

كلتاهما ترفضان بـ `not authorized` لغير حاملي الصلاحية، و`delete_incident` ترفع
`incident_not_found` على معرّف غير موجود.

**لماذا تُرجعان مسارات؟** `incident_attachments` تتسلسل مع الصفّ؛ **الكائنات في
الدلو لا**. لا شيء في Postgres يملكها، وحذفٌ يتركها خلفه هو دلوٌ خاصّ يتراكم فيه
صور طوارئ لم تعد موجودة، لا يقرؤها أحد ويُدفع ثمنها إلى الأبد. فتُقرأ المسارات
**قبل** الحذف — بعده لا يبقى ما يُسأل أين كانت الملفات — وتُعاد إلى العميل ليمسحها
من الدلو. **بأفضل جهد بحكم التصميم:** الصفّ ذهب والسجلّ صار صحيحاً، ونداء تخزين
فاشل ليس شيئاً يُعرض على من حذف بلاغاً للتوّ. سياسة `incident_files_delete`
وُسِّعت لحاملي `incidents.delete` (§19).

**`p_only_closed` هو الأمان، وقيمته الافتراضية هي الجواب الآمن.** تفريغ المغلقة
ترتيبُ بيت؛ تفريغ الكل يرفع طوارئ **مفتوحة** عن شاشة كل من يتابع السجلّ الآن،
وربما أحدهم في الطريق إليها — فيجب أن يُقال صراحةً لا أن يُبلَغ إليه بالضغط على
زرّ التأكيد مرّتين. وواجهة التطبيق تسأل «ما الذي يُحذف؟» بجوابين مسمّيين، لا
«هل أنت متأكد؟».

#### التسجيل — ما أغفلته 0088

0077 علّقت الـ trigger العام على كل جدولٍ كان موجوداً **يوم نفاذها**، وتركت
ملاحظةً للـ migrations اللاحقة بأن تفعل الشيء نفسه لجداولها. 0088 لم تقرأ
الملاحظة: `incidents` و`incident_attachments` **غير مسجَّلين منذ يوم إنشائهما** —
كل تغيير حالة، والآن كل حذف، بلا أثر لمن فعله. 0121 تعلّقه، وتضيف حالة
`incidents` إلى `audit_record_label` (صفّ البلاغ لا يحمل أياً من الأعمدة الستّة
التي ترجع إليها الدالّة، فكان كل سطر يُكتب معرّفاً عارياً؛ صار **أول ٨٠ حرفاً من
`body`**). أي backend بديل ملزم بالمثل — §23.

### 30.8 الإنذار: نافذة وصوت، لا إشعار (بلا migration)

**لا يخصّ الخادم من هذا القسم شيء.** لا جدول ولا عمود ولا endpoint: مصدرا
الإنذار هما تدفّق §20 نفسه ودَفعة §17 نفسها، وكلاهما قائم منذ 0107. القسم مكتوب
لأن **السلوك** جزء من العقد: من يبني الواجهة على backend آخر يجب أن يعرف أن
`data.type == "incident"` ليس إشعاراً كبقيّة الإشعارات.

**الشكوى:** 0088 توصل البلاغ إشعاراً — سطراً في الصندوق (§16)، وسطراً في شريط
الهاتف على قناة `alarm` بأولوية `max` (§17). وذلك صحيح لهاتفٍ في جيب، وخاطئ تماماً
للحالة التي بُنيت الميزة لها أصلاً: **التطبيق مفتوحٌ في المقدّمة على شاشة لا ينظر
إليها أحد**. لافتةٌ تنزلق فوق قائمة ثم تنزلق خارجةً هي، في غرفة عمليات، طوارئ لم
يرها أحد.

**ما صار يحدث** حين يصل صفّ إشعارٍ `type == "incident"` والتطبيق يعمل:

| | |
| --- | --- |
| نافذة | `AlertDialog` على المُوجِّه الجذر — أياً كانت الشاشة المفتوحة، فالطوارئ ليست شيئاً يُشترط لسماعه أن تكون في المكان الصحيح |
| لا يُمرَّر بالخطأ | `barrierDismissible: false` + `PopScope(canPop: false)` — نقرة الحاجب وإيماءة الرجوع فعلان يُفعلان بلا قراءة، ومعناهما هنا إسكات طوارئ بالانعكاس |
| صوت | نغمة المنبّه الافتراضية للجهاز **على `USAGE_ALARM`**، دائرة، مع اهتزاز متكرّر |
| زرّان | «إغلاق» و«افتح السجلّ» |

**لماذا مجرى المنبّه تحديداً:** هو المجرى الذي **لا** يُسكته الوضع الصامت، والذي
تمرّره «المنبّهات فقط» في وضع عدم الإزعاج. صوت إشعارٍ في جيبٍ في الثالثة فجراً هو
صوتٌ لا يسمعه أحد — وتلك هي الشكوى بعينها. ويدور بلا توقّف لأن رنّةً واحدة تنتهي
قبل أن يرفع أحدٌ رأسه؛ **الدوران هو ما يجعله إنذاراً لا إشعاراً بطموح**.

**وزرّ الإغلاق ليس تنازلاً:** الموقَظ قد يكون على بُعد أربع ساعات والشخصَ الخطأ
تماماً، ونافذةٌ لا يستطيع إغلاقها يُسكتها بقتل التطبيق — **فلا يصله الإنذار
التالي**.

**مصدران لا واحد، والمفتاح هو ما يمنع الرنّتين:**

1. **تدفّق §20** (صندوق القارئ نفسه). هذا الذي يعمل في كل مكان — على Windows حيث
   لا Firebase أصلاً، وعلى نسخة Android فشل فيها تسجيل الدفع بصمت.
2. **دفعة المقدّمة** (§17). هذه السريعة، والتي تصل حين يكون socket الـ Realtime
   قد سقط ولم ينتبه بعد.

الإنذار مفتاحه `incident_id` من `data`، فالبلاغ الواصل من الطريقين — وهي الحالة
الطبيعية على هاتف Android سليم — **يرنّ مرّة واحدة**.

**نافذة الطزاجة — ١٥ دقيقة.** أول ما يبثّه تدفّق §20 هو **لقطة الصندوق كاملاً** لا
تغييراً؛ بلا نافذة، كان تسجيل الدخول يوم الخميس سيُطلق إنذاراً عن كل طوارئ الأسبوع
دفعةً واحدة. وبلا أيّ إعادة، كان من قُتل تطبيقه وأعاد فتحه بعد أربع دقائق من طوارئ
حيّة سيجد السجلّ صامتاً — وهي اللحظة التي بُنيت لها الميزة. فالشرط: **غير مقروء،
وعمره أقلّ من ربع ساعة**.

**التزام على أي backend بديل:** لا شيء، سوى أن يبقى `data` على الإشعار كما هو —
`type` و`incident_id` — وأن يظلّ تدفّق §20 يخبر عن الإدراج. الباقي كلّه في العميل.

**والصورة تُعرض في الصندوق أيضاً.** كان `incident_id` مفتاح الإنذار وحده؛ وصار
كذلك المكان الذي يقرأ منه الصندوق أدلّة البلاغ — راجع §16.3. حتى ذلك، كان بلاغٌ
عاجل أُرسل بصورةٍ يصل سطرَ نصّ، والصورة **لا تُرى في أي شاشة من التطبيق**: الصندوق
يسأل `notification_attachments` فلا يجد شيئاً، و`incidents_list` لا تُرجع سوى
`attachment_count`. من يبني الواجهة على backend آخر ملزم بالوصلة نفسها، وإلا فقد
بنى صندوقاً يُخفي الدليل الذي رُفع لأجله البلاغ.

### 30.9 بلاغاتي — سجلّ الشخص عن نفسه (migration 0126)

كانت "غرفة العمليات" و"صاحب البلاغ" يقرآن الشيء نفسه من زاويتين — `incidents_list`
تُرجع للثاني بلاغاته فقط، لكن ذلك أثر جانبي لشكل الشرط، لا وعداً. من يحمل
`incidents.receive` (أو أدمن) يسأل الدالّة نفسها فتُرجع **السجلّ كلّه**، وهو
الجواب الصحيح لغرفة العمليات وخاطئ تماماً لمشرفٍ رفع بلاغاً عن نفسه الأسبوع
الماضي ويريد معرفة ما جرى له فقط.

**معامل ثالث، صريح بدل مُستنتَج:**

```
incidents_list(
  p_include_closed boolean default false,
  p_limit int default 100,
  p_mine_only boolean default false   -- جديد
) returns table (...)   -- الأعمدة نفسها منذ 0120، بلا تغيير
```

`p_mine_only = true` يفرض `reporter_id = auth.uid()` **بصرف النظر عمّا يحمله
المستدعي** — لا `is_admin()` ولا `incidents.receive` يوسّعان الجواب. `false` هو
السلوك القديم حرفاً بحرف. **حُذفت الدالة القديمة ذات المعاملين وأُنشئت من جديد**
لا `create or replace` مجرّد: معاملٌ إضافي يعني، عند Postgres، overload جديداً
يقف بجانب القديم لا يستبدله — والصيغة القديمة الباقية تجعل كل نداء بمعاملين فقط
ملتبساً. نفس الحجة التي في §30.6 بالضبط.

**لا صلاحية جديدة.** `incidents_select` (0088) أصلاً تفتح القراءة لصاحب البلاغ —
`reporter_id = auth.uid()` — والمعامل الجديد لا يفتح باباً كان مغلقاً، هو فقط
يمنع الأوسع من الأبواب المفتوحة أصلاً أن يُجاب بغير ما سُئل.

**العميل:** `IncidentsRepository.fetchList` تكسب `mineOnly` (افتراضها false،
تمريرها المباشر لـ `p_mine_only`)، و`IncidentsScope` تُميّز بين `all` (الغرفة،
`/incidents`، خلف `incidents.receive`) و`mine` (`/my-incidents`، **بلا صلاحية** —
قراءة ما رفعتَه ليست منحةً أكثر من رفعه). شاشة واحدة تجيب عن السؤالين، تماماً كما
تفعل `ComplaintsScope` في §24 — لا سجلّان يُبقيان متطابقين يدوياً.

**الإدارة لا تظهر على بلاغاتي البتّة — لا بالشرط، بقرار الشاشة.** وهي هنا أوسع
من تولّ/أغلق/أعِد الفتح/احذف: **الاتصال بالمُبلِّغ وتحديد موقعه على الخريطة
كذلك**، وزرّ اتصال المضمون داخل سطر "البلاغ عن". هذه كلّها أفعالٌ تخصّ الغرفة —
الاتصال بشخصٍ آخر، تحديد مكانه — لا شخصاً يقرأ حالة بلاغه هو نفسه؛ بل إن أحدها
لا معنى له إطلاقاً هنا: زرّ "اتصال" يطلب رقم صاحب البلاغ نفسه على بلاغاتي، أي
يطلب من القارئ الاتصال بنفسه.

RLS تكفي وحدها لمنع كتابة `incidents_update`/`incidents_delete` من غير حامل
الصلاحية، لكن هذا القسم لا يتوقّف عند "الزرّ سيُرفَض" — فزرّا الاتصال والخريطة
لا تحكمهما RLS أصلاً، فتح رقمٍ هاتفي ليس كتابة على الصفّ. الحاسم قرار الشاشة:
حتى من يحمل كل الصلاحيات معاً — عضو غرفة عمليات رفع بلاغاً عن نفسه ويقرأ
بلاغاتي — لا يرى شيئاً من هذا هنا. من أراد أن يتولّى بلاغاً أو يتصل بصاحبه يفتح
السجلّ، لا سجلّه الشخصي. فالشرط في العميل `!mine && canHandle` (و`!mine &&
canDelete`، و`!mine` وحدها على زرّي الاتصال والخريطة) — الحاسم في السؤال "أين
أنا؟" لا "ماذا أحمل؟" فقط.

**التزام على أي backend بديل:** إن أضاف مكافئاً لـ `p_mine_only` فليُطبّقه بالفرض
لا بالتضييق — `reporter_id = auth.uid()` بصرف النظر عن أي صلاحية، تماماً كما هنا.
وإن بنى شاشة بلاغاتي فوق `incidents_list` بلا هذا المعامل، فعليه أن يُصفّي
`reporter_id` في العميل نفسه قبل العرض — لا أن يعرض السجلّ كاملاً لأدمن ظنّ نفسه
يقرأ بلاغاته فقط.

**لم يُنفَّذ هذا الـ migration على قاعدة حيّة بعد.**

---

## 31. خريطة الموسم Season map (migrations 0090 → 0093)

كل حقيقة في هذا التطبيق مرتبطة بمكان منذ البداية — البرج فندق له عنوان، والمخيم
له إحداثيات ثبّتتها الإدارة، والوصول يسجّل أين كان الهاتف، والبلاغ العاجل يسجّل
أين كان الرجل واقفاً. ولم يُرسم شيء من ذلك مجتمعاً قط: مكتبة الخرائط في المشروع
منذ 0021 ولم تُستعمل إلا **لاختيار نقطة واحدة** في كل مرة.

### 31.1 `season_map(p_season_id uuid default null)`

`p_season_id` فارغاً يعني الموسم الجاري، ويحلّه الخادم — لا الهاتف، كما في كل
شاشة أخرى.

**صفٌّ لكل مكان** — أي لكل مدخل في قائمة الفنادق أو قائمة المخيمات
(`reference_sets.is_place`) مفعّلٍ وله موقع يُحلّ. **لا صفّ لكل عقدة**، منذ 0112.
**الصلاحية:** `map.view` (أو أدمن).

| العمود | المعنى |
| --- | --- |
| `item_id` | المدخل المرجعي الذي **هو** المكان — نفس المعرّف الذي يُسجَّل عليه الوصول ويُثبَّت عليه الرمز (0098) |
| `place_name` | اسم المكان |
| `group_key` | مفتاح ثابت للفلترة — `<set_code>:<group_id>`. لا يُعرض |
| `group_ar` / `group_en` | اسم المجموعة باللغتين — القارئ يختار، والدالّة لا تعلم |
| `lat` / `lng` | من `place_location()` (0098) |
| `posted` | من يحمل منصباً هنا، **DISTINCT** — رجلٌ أسنده ملفّان إلى مخيم واحد رجلٌ واحد |
| `present` | من سجّل وصوله خلال **١٢ ساعة** — **واقعه** |
| `open_incidents` | بلاغات غير مغلقة على أي عقدة قائمة على هذا المكان |

**تحذير على الترقية:** أعمدة `OUT` جزء من هويّة الدالّة و`create or replace` لا
يبدّلها. فـ 0093 و0112 كلاهما يبدأ بـ `drop function if exists season_map(uuid)`،
كما فعل 0080 و0084 قبلهما.

### 31.1-0112 الخريطة ترسم أماكن، لا ملفات

0095 أخرج المكان من الملف، و0098 أتمّ الفكرة: `is_place`، ورمزٌ لكل مدخل يبذره
trigger، و`place_check_ins.item_id` يشير إلى **المدخل**. `season_map` لم تتبع:
فرعها الأول — الذي ينتج أكثر الدبابيس — بقي يبدأ من `module_nodes`. وكل عيب في
الشاشة ينبثق من هذه الحقيقة وحدها:

1. كل دبّوس كان يحمل `module_types.name_ar`، والورقة تطبعه تحت «ضمن الملف
   التشغيلي» — فيُجاب النقرُ على مخيم في منى بـ«توزيع أعضاء مكاتب البعثة على
   مخيمات منى». والمكان ليس في ملف منذ 0095، والجملة لم تكن زائدة بل **خاطئة**.
2. **المجموعة** كانت `'type:' || module_types.code` لكل ما ليس فندقاً، فصارت
   شرائح الفلترة أسماءَ أنواع ملفات. المجموعات الأربع التي تفكّر بها البعثة —
   مخيمات منى، مخيمات عرفات، فنادق مكة، فنادق المدينة — لم تخرج صحيحة إلا للفنادق.
3. **مخيمٌ لا يحمله ملفٌّ قائم كعقدة لم يكن على الخريطة أصلاً**، مع أنه مكان له
   رمز ويُسجَّل عليه الوصول. فرع الإنقاذ الذي أضافه 0098 يغطي `hotels` وحدها.
4. مدخلٌ واحد يحمله ملفّان كان يرسم **دبّوسين على نقطة واحدة**. 0098 انتبه إلى
   نصف المسألة — عرفات تحمل مخيم 16 مرّتين — فصحّح عدّ الحضور ليكون لكل مدخل،
   وترك الصفّين.
5. الصفوف كانت تُضيَّق بـ`is_module_member`، والفرع المجاور لها لا يُضيَّق —
   فيظهر الفندق نفسه أو يختفي بحسب ما إذا صادف أن ملفاً يحمله.

**والصلاحية انتقلت إلى الدالّة.** 0100 أعطى **الشاشة** صلاحية وترك الصفوف تضيّق
نفسها لكل قارئ، وكان ذلك صحيحاً ما دامت الصفوف تخرج من ملفات يمكن أن يكون القارئ
عضواً فيها. ولا عضوية يُضيَّق بها حين تصير الدبابيس أماكن، وفرع الفنادق كان أصلاً
يسلّم كل فندق مثبَّت لأي حساب معتمد. فالقاعدة انتقلت حيث تُقال مرّة واحدة وتوافق
الموجِّه: **حاملُ `map.view` يرى أماكن الموسم، ولا أحد سواه ينادي الدالّة**. وهذا
**يوسّع** ما يراه عضو ملف (مخيماته صارت كلَّها) و**يضيّق** ما يراه حسابٌ معتمد بلا
منحة (كل فندق صار لا شيء)، وكلاهما ما قرّره 0100 أن الشاشة له.

### 31.1a المجموعات — أربع، وليست مكتوبة في أي مكان

مخيمات منى · مخيمات عرفات · فنادق مكة · فنادق المدينة. **تنبثق من البيانات**،
ومنذ 0112 تنبثق كلُّها لا نصفها: المدخل يسمّي مجموعته في `data` نفسه — الفندق
يحمل `city` (0021) والمخيم يحمل `site` (0095)، وكلاهما إشارة إلى قائمة أخرى،
فيُحلّان بالوصلة نفسها. والاسم العربي يُسقط أداة التعريف من اسم القائمة ويضع
المجموعة بعده: «الفنادق» + «مكة» ← «فنادق مكة»، «المخيمات» + «منى» ← «مخيمات
منى». قائمة أماكن خامسة تظهر يوم توجد خامسة، بلا تعديل هنا.

(قبل 0112 كان ذلك صحيحاً في الفنادق وحدها؛ ما عداها يُجمَّع **بملفّه**، فتقرأ
الشريحة «توزيع أعضاء مكاتب البعثة على مخيمات منى» بدل «مخيمات منى».)

**العميل يحفظ المُخفيّ لا المعروض** — فمجموعة تظهر لاحقاً تصل **مرئية**. حفظ
المعروض يجعل كل جديد خفيّاً حتى ينتبه أحد لغيابه.

### 31.1b عيبان كانا يُسقطان الفنادق كلها

**البرج لا عنوان له، لأن البرج هو الفندق** (0092). مستوى البرج لا يعلن حقل موقع؛
يسحب من قائمة الفنادق، والعنوان صفةٌ للفندق (`location_url` على البيانات
المرجعية). و`node_location()` كانت تقرأ بيانات العقدة وحدها — فقُرِئ كل برج كمكان
بلا موقع وأُسقط **بلا كلمة**. صارت تقرأ الاثنين، وقيمة العقدة تفوز حين توجد.

**وفنادق المدينة ليست عُقداً إطلاقاً** (0093). ملف المدينة مستواه الوحيد «شركة
الخدمة» — شركة لا مكان. فالفندق بيانات مرجعية وحسب، ولم يكن ليظهر مهما أُحسن
تثبيته. ولذلك النوع الثاني من الصفوف، مُضيَّقاً بـ«ليس عقدة أصلاً» حتى لا يُرسم
فندق مكة مرّتين ويُعدّ أهله مرّة.

**ما يُستبعد ولماذا:**
- **المستويات غير المكانية** (`is_place = false`، §0089). القطاع ترتيب على ورق
  بلا بوابة ولا إحداثيات؛ رسمه يضع علامة على فكرة.
- **العقد بلا موقع**: `node_location` لا تُعيد صفاً، و`cross join lateral`
  يُسقطها. مكان بلا إحداثيات لا يُرسم، وتخمين إحداثيات له أسوأ من تركه.
- **الملفات المنتهية** (`ends_on < today`). الخريطة عن الموسم كما يُدار.

### 31.1c أشكال روابط الخرائط (0091)

`node_location()` كانت تقرأ شكلين: `?q=lat,lng` و`@lat,lng`. وفاتها الشكل الذي
يُنتجه المدير غالباً: زرّ «مشاركة» في خرائط Google يعطي `maps.app.goo.gl` بلا
إحداثيات إطلاقاً، وتتبُّعه ينتهي إلى:

```
https://www.google.com/maps/search/21.424128,+39.896510?entry=tts&…
```

الزوج في **المسار** لا في معامل، وبينهما `+`. فمكانٌ ثُبِّت من رابط مشاركة كان
يُقرأ كمكان بلا موقع — و`null` هنا لا يُميَّز عن حقل لم يملأه أحد.

**والعميل يحلّ الرابط المختصر عند اللصق ويخزّن الشكل القانوني** `?q=lat,lng`،
فلا تعتمد القاعدة على شكل روابط Google، وتقرأ الموقع دون أن تتبع شيئاً.

### 31.2 البلاغات ليست هنا

طبقة ثانية على الخريطة نفسها، مصدرها `incidents_list` (§30) بإحداثياته هو.
**عمداً:** البلاغ يقع حيث كان المُبلِّغ واقفاً، وهو كثيراً ما ليس مكاناً يعرفه
هذا الملف أصلاً. حافلة متعطلة على طريق عرفات هي الحالة بعينها، وتعليقها على أقرب
مخيم يرسم العلامة حيث لا يحتاج أحد أن يذهب.

ويُجلَب **مستقلاً ويُسمح له بالفشل وحده**: من يرى الخريطة لا يلزم أن يحمل
`incidents.receive`، وخريطة ترفض الرسم لأنه لا يحملها خريطة تعاقبه على ما ليس هو.

### 31.3 التزام الخادم

**تضييق الصفوف مُعاد داخل الدالة**، لأن `security definer` لا يرث RLS. القاعدة:
يرى المستخدم أماكن الملفات التي هو **عضو فيها**، ويرى الجميع إن كان `is_admin()`
أو يحمل `modules.view_all`. **الخريطة يجب ألّا تصير طريقاً لمعرفة قوام ملفات لا
يستطيع فتحها.**

---

## 32. السفر والتنقّل Travel (migrations 0129 → 0134)

**الصلاحية:** `travel.view` هي الباب (تتطلب `employees.view`)؛ `travel.view_all`
للوحة الموسم؛ `travel.edit` للرحلات؛ `travel.assign` للإسناد؛ `travel.confirm`
لتسجيل ما حدث؛ `travel.delete` للحذف.

**وبلا صلاحية إطلاقاً:** الموظف يقرأ مساره هو ويؤكّد حركاته هو. سجلٌّ عن سفر رجل
لا يراه هو ليس سجلاً يستحق الحفظ — وهو الحكم نفسه المتَّخذ في تقديم الشكوى (§24)
وفي رفع البلاغ (§30).

### 32.1 النموذج — ما يعلَّق على ماذا

المِرساة **ليست الموظف** بل مشاركتَه في الموسم (`season_participants`، §7.5).
موسم جديد = صف مشاركة جديد = **مسار فارغ بحكم البناء**، فلا حاجة إلى أي منطق
«لا تنسخ سفر السنة الماضية»: لا يوجد ما يُنسخ.

```
profiles ──1──< season_participants >──1── seasons
                        │
                        └──< journey_legs >──0..1── trips
```

| الكيان                                 | ما هو                                                                   |
| -------------------------------------- | ----------------------------------------------------------------------- |
| `trips`                                | **المركبة وجدولها**، داخل موسم واحد. لا تعرف من عليها. تُنشأ قبل الإسناد |
| `journey_legs`                         | **هذا المشارك على تلك المركبة** — أو متحرّكاً بترتيبه الذاتي             |
| `travel_points`                        | قائمة مرجعية (`reference_sets.code = 'travel_points'`)، غير مقيّدة بموسم |
| `trip_attachments` / `leg_attachments` | نفس أعمدة كل مرفق في هذا المستند (§19)                                  |
| `travel_reminders`                     | دفتر للجدولة: `(leg_id, threshold_hours)` فريد — ليس للعميل             |

**`trips.carrier` حُذف في 0134**: رقم الرحلة يكفي، واسم الناقل حقلٌ ثانٍ يُخطئ في
كتابته من يُدخل أربعين رحلة في جلسة.

### 32.2 الحقل الحاسم: `journey_legs.trip_id` قابل للإفراغ

`trip_id = NULL` يعني **ترتيباً ذاتياً**: سيارة خاصة، أو توصيلة، أو أي وسيلة لم
يحجزها المكتب. **ليست حجزاً مفقوداً** ولا تُعرض على أنها كذلك.

النظام لا يسأل «أين حجز القطار؟» بل **«كيف انتقل هذا الرجل من مكة إلى المدينة؟»**
و«بسيارة خاصة» جوابٌ كامل. ما يستحق الانتباه هو «لا نعرف» وحدها.

### 32.3 مصدر واحد للخطة (BR-4)

عندما توجد رحلة تأتي الخطة **منها**، وعندما لا توجد تأتي **من الحركة**. لا يجوز
الاثنان — يفرضه `check` باسم `leg_plan_has_one_source`.

| `trip_id` | `mode`, `from_point_id`, `to_point_id`, `planned_*` على الحركة |
| --------- | -------------------------------------------------------------- |
| موجود     | **يجب أن تكون NULL** — تُقرأ من الرحلة                         |
| NULL      | **يجب أن تكون مملوءة** (عدا `planned_arrival_at`)              |

فائدته العملية: تأخّر رحلة عليها ستون راكباً = **تعديل واحد**، لا ستون.

### 32.4 ثلاث حالات مختلفة، ولا واحدة تكتب الأخرى

| المستوى               | يخصّ       | القيم                                                            |
| --------------------- | ---------- | ---------------------------------------------------------------- |
| `trips.status`        | المركبة    | `scheduled · delayed · departed · arrived · cancelled`           |
| `journey_legs.status` | هذا الشخص  | `planned · confirmed · completed · missed · cancelled · rebooked` |
| موقع الموظف           | الشخص      | **مشتقّ، غير مخزّن** — وجهة آخر حركة `completed`                 |

> **إلزامي على أي خادم بديل:** لا trigger ولا مهمة خلفية تحوّل `trips.status`
> إلى حالة حركة. هبوط الطائرة حقيقةٌ عن الطائرة ولا تقول شيئاً عمّن كان عليها.
> التطبيق **يعرض** الاقتراح («هبطت SR441 — تأكيد وصول الـ57؟») ويبقى الضغط فعلاً
> بشرياً. والأمر نفسه في تسجيل الوصول (§30): يقترح ولا يكتب.

### 32.5 شكل الحركة — ما تفرضه القاعدة (0133)

| الدور      | الوسيلة                                     | من ← إلى                         |
| ---------- | ------------------------------------------- | -------------------------------- |
| `inbound`  | `air` فقط                                   | مطار سوري ← مطار سعودي           |
| `outbound` | `air` فقط                                   | مطار سعودي ← مطار سوري           |
| `internal` | `rail` / `road` / `other` — **never `air`** | بين مكة المكرمة والمدينة المنورة |

يُفرَض بـ`travel_shape_is_sane(role, mode, from, to)` عبر trigger على الجدولين.
ونقطة السفر تحمل `country_code` (`SY`/`SA`) و`point_kind`
(`airport`/`station`/`city`) و`city` داخل `reference_items.data`، وقيمها محروسة
بـ trigger لأن التطبيق **يعقل عنها** ولا يكتفي بعرضها.

**المطارات مستثناة من الحركة الداخلية ولو وقعت في المدينة الصحيحة**: مطار الأمير
محمد بن عبدالعزيز في المدينة المنورة، فقاعدةٌ تسأل عن المدينة وحدها كانت ستعرضه
وجهةً لسيارةٍ من مكة. ومن يقصد ذلك المطار يبدأ رحلة عودته، وهي صفٌّ آخر بقواعد
أخرى.

### 32.6 مسار موظف واحد

```
GET /participations/{participantId}/journey
```

بديل `employee_journey(p_participant_id)` — يُرجع **الحركات محلولة**: نصف الرحلة
ونصف الرجل مدموجان، مرتّبةً بالوقت المخطط.

**Response:** مصفوفة من:

```json
{
  "leg_id": "uuid",
  "trip_id": "uuid|null",
  "role": "inbound|internal|outbound",
  "mode": "air|rail|road|other",
  "from_point_id": "uuid",
  "from_point_ar": "مطار دمشق الدولي",
  "from_point_en": "Damascus International Airport",
  "to_point_id": "uuid",
  "to_point_ar": "مطار الملك عبدالعزيز الدولي",
  "to_point_en": "King Abdulaziz International Airport",
  "trip_number": "SR441|null",
  "planned_departure_at": "2026-07-25T05:00:00Z",
  "planned_arrival_at": "2026-07-25T08:00:00Z",
  "actual_departure_at": null,
  "actual_arrival_at": null,
  "status": "completed",
  "vehicle_status": "arrived",
  "ticket_ref": null,
  "seat": null,
  "note": null,
  "replaces_leg_id": null,
  "confirmed_by": null,
  "confirmed_by_name": null,
  "confirmed_at": null,
  "self_arranged": false,
  "attachment_count": 0
}
```

> **لا يُرجع «الحالة الحالية».** أين هو الآن، وما التالي، وكم بقي على العودة —
> كلها دوالٌّ على هذه القائمة وتُحسب في التطبيق. عمودٌ لها يعني مكاناً ثانياً
> يمكن أن يخطئ، ويخطئ أوّل مرة يُعدَّل فيها صفٌّ ولا يُعاد الحساب — الحجّة نفسها
> التي ساقتها 0064 عن مجموع المجموعات.

```
GET /me/participation?season_id=…                     → uuid | null   (my_participation)
GET /profiles/{profileId}/participation?season_id=…   → uuid | null   (participation_of)
```

`null` جوابٌ عادي — حساباتٌ كثيرة ليست في أي موسم بعينه — ويُقرأ «لا سفر مسجَّل»
لا «خطأ».

### 32.7 رحلات الموسم

```
GET /seasons/{seasonId}/trips
```

بديل `season_trips`. يزيد على أعمدة `trips`: `assigned_count` (الحركات **الحيّة**
فقط)، و`completed_count`، و`attachment_count`.

```
GET /trips/{tripId}/passengers
```

بديل `trip_passengers` — الحركات الحيّة فقط. من نُقل عنها محفوظٌ في الجدول
ومُستبعَدٌ من الكشف.

### 32.8 إنشاء رحلة وتعديلها

```
POST   /trips           { season_id, mode, role, from_point_id, to_point_id,
                          trip_number?, planned_departure_at,
                          planned_arrival_at?, note? }
PATCH  /trips/{tripId}  (بلا season_id — لا تُنقل رحلة بين المواسم)
DELETE /trips/{tripId}
```

`planned_departure_at` **إلزامي**: الرحلة هي مغادرةٌ مجدولة، وما لا يُعرف وقته
نيّةٌ لا رحلة. و`DELETE` مرفوض ما دام عليها راكب حيّ (`on delete restrict`) —
الإلغاء حالةٌ لا حذف.

### 32.9 الإسناد الجماعي

```
POST /trips/{tripId}/assign   { "profile_ids": ["uuid", …] }
```

بديل `assign_profiles_to_trip` (0132) — وهو **المسار الذي يستعمله التطبيق**، لأن
منتقي الموظفين المشترك (§9.10) يتعامل بـ`profiles.id` وتستعمله خمس ميزات لا تعرف
ما المشاركة الموسمية. والترجمة إلى مشاركات تتم **على الخادم مقابل موسم الرحلة
نفسها** — وهو الموسم الوحيد الذي يمكن أن يكون صحيحاً.

```
POST /trips/{tripId}/assign-participants   { "participant_ids": [...] }   (assign_to_trip)
```

**Response 200:**

```json
{ "assigned": 27, "rebooked": 3, "skipped": 1, "not_in_season": 0 }
```

**ما تفعله العملية لكل شخص، ذرّياً:**

1. **على هذه الرحلة أصلاً** ← `skipped`.
2. **وصل فعلاً** على رحلة أخرى بنفس الدور ← `skipped`. لا يُعاد كتابة التاريخ.
3. **له حركة حيّة بنفس الدور** (`inbound`/`outbound` فقط) ← القديمة
   `status = 'rebooked'` وتُحفظ، والجديدة تحمل `replaces_leg_id` — **BR-5: لا حذف
   أبداً**.
4. يُنشأ الصف، ويُرسل إشعار «أُسندت إليك رحلة».

والحركة الداخلية تتكرّر بحقّ (مكة←المدينة ثم العودة)، فلا تُزيح شيئاً.

```
GET  /seasons/{seasonId}/without-leg?role=inbound   → [profile_id]
POST /legs/{legId}/unassign   { note? }             → status = 'cancelled'
```

`participants_without_leg` هو أنفع اختيارٍ يمكن عرضه على أربعمئة شخص: الثمانون
الذين لا رحلة قدوم لهم بعد. ويحترم `travels`.

### 32.10 تسجيل ما حدث

```
POST /legs/{legId}/confirm   { status, departed_at?, arrived_at? }
```

بديل `confirm_leg`. **من يجوز له:** حامل `travel.confirm`، **والمسافر نفسه
لحركاته هو**. والحالة الثانية ليست تسهيلاً: في السيارة الخاصة لا شركة طيران ولا
بوابة ولا كشف — والوحيد الذي يعلم أنه بلغ المدينة هو من قاد إليها.

`status` من `planned|confirmed|completed|missed` فقط. أمّا `cancelled` و`rebooked`
فقراراتٌ عن الحجز لا ملاحظاتٌ عن الرحلة، ولهما مساراهما (32.9). و`completed` بلا
وقت وصول يُختم بـ`now()` — مسارٌ يقول إنه وصل ولا يقول متى مسارٌ لا يمكن رسمه.

**التوقيع ضيّق عمداً**: RLS لا تستطيع قول «يكتب هذه الأعمدة الأربعة لا رقم
الرحلة»، فالتوقيع يقولها بدلاً منها.

```
POST /participations/{participantId}/self-leg
     { role, mode, from_point_id, to_point_id,
       departure_at, arrival_at?, note?, completed }
```

بديل `record_self_leg` — السيارة الخاصة وما شابهها. وإن كان الدور
`inbound`/`outbound` أزاح أي حجزٍ حيٍّ بنفس الدور إلى `rebooked` بدل أن يصطدم
بالفهرس الفريد برسالةٍ خام.

```
PATCH /participations/{participantId}/travels   { "travels": false }
```

بديل `set_participant_travels`. يقبله `travel.edit` أو
`seasons.participants_manage` — دالّةٌ ضيّقة بدل توسيع سياسة الكتابة على
`season_participants`، لأن ذلك كان سيسلّم فريقَ السفر كشفَ المشاركين كلَّه.

### 32.11 ما لم يُجَب عنه

```
GET /seasons/{seasonId}/travel-gaps
```

بديل `travel_gaps`. أربعة أنواع فقط:

| `kind`           | متى                                            |
| ---------------- | ---------------------------------------------- |
| `no_inbound`     | مشارك مسافر بلا رحلة قدوم حيّة                 |
| `no_outbound`    | بلا رحلة عودة حيّة — **عادي معظم الموسم**      |
| `unconfirmed`    | مضى وقتها المخطط والحالة `planned`/`confirmed` |
| `cancelled_trip` | رحلته أُلغيت وهو ما زال عليها                  |

**وما لا يُدرَج جزءٌ من التصميم بقدر ما يُدرَج:**

- من لا حجز قطار له **ليس ثغرة** — قد يكون ذهب بسيارة.
- حركتان لا تلتقيان نقطتاهما ليستا ثغرة أيضاً، وهذه تستحق التنصيص لأنها قابلة
  للحساب ومغرية: كانت ستُطلق على «مطار جدة ← مكة» لكل قادمٍ في البعثة — باص
  المطار، الذي لا يتتبّعه أحد ولا يحتاج أحد إلى تتبّعه. ولوحةٌ أكثر صفوفها شيوعاً
  صفٌّ لا يُعمل به لوحةٌ تُهمَل في الأسبوع الثاني.
- ومن `travels = false` مستبعَد من الأربعة جميعاً.

### 32.12 المرفقات

```
GET  /trips/{tripId}/attachments     POST /trips/{tripId}/attachments   (multipart)
GET  /legs/{legId}/attachments       POST /legs/{legId}/attachments
```

دلو **ثامن** اسمه `travel`، خاص. ومساران: `trips/{trip_id}/…` و`legs/{leg_id}/…`
— والبادئة هي ما تقرؤه `can_read_travel_file` لتعرف أيَّ صفٍّ تسأل عنه.

- **قراءة**: `travel.view`، أو أن يكون القارئ مسافراً على تلك الرحلة (كشف الركاب
  وجدول الرحلة ممّا يُعطى للراكب أصلاً).
- **كتابة**: `travel.edit` / `travel.assign` / `travel.confirm`، **أو صاحب الحركة
  على حركته هو** — بطاقة صعوده في يده.

جدولان لا جدول واحد: كشفٌ معلَّق على **رحلة** يخصّ الجميع، وبطاقةُ صعودٍ تخصّ
رجلاً واحداً، وهذان سؤالان مختلفان عمّن يقرأ — أي سياستان، أي جدولان (§19،
والحجّة الأصلية في 0031).

### 32.13 الإشعارات

كلها عبر البنية القائمة (§16 ثم §17): صفٌّ في `notifications` ← trigger ←
Edge `send-notification` ← FCM.

| `data.type`           | إلى من                  | متى                            |
| --------------------- | ----------------------- | ------------------------------ |
| `travel_assigned`     | الموظف                  | عند الإسناد                    |
| `travel_return_soon`  | الموظف                  | قبل العودة بـ48 ثم بـ24 ساعة   |
| `travel_trip_changed` | ركّاب الرحلة            | تغيّر الموعد أو الإلغاء        |
| `travel_gaps`         | حاملو `travel.view_all` | ملخّص يومي                     |

**جدولان زمنيان (pg_cron):**

- `remind-upcoming-returns` — كل أربع ساعات. وليس ليلياً: عودةٌ تُحجز الساعة
  التاسعة صباحاً لصباح الغد يجب ألّا تنتظر الثامنة مساءً، وإلّا صار تنبيهُ
  الأربعِ وعشرين ساعة تنبيهَ خمس عشرة.
- `report-travel-gaps` — 17:00 UTC (20:00 بمكة)، الساعة نفسها التي اختارتها 0086
  و0119. وتنبيه «بلا رحلة عودة» لا يبدأ إلّا حين يبقى على نهاية الموسم **21 يوماً
  أو أقل**: قبل ذلك «لا عودة بعد» هي الحالة الصحيحة لكل رجل في البعثة، والتنبيه
  عنها يومياً يدرّب الغرفة على تجاهله.

ومنع التكرار بجدول `travel_reminders (leg_id, threshold_hours)` فريد — لا عمود
`reminded_at` واحد، لأن عتبتين لا تُمثَّلان بختمٍ واحد. وتغيّر موعد الرحلة **يمسح**
تذكيراتها: إن أُعيدت جدولتها وجب إخبار الناس ثانيةً.

### 32.14 منطق الخادم الإلزامي — خلاصة القسم

| #     | القاعدة                                                       | أين                                    |
| ----- | ------------------------------------------------------------- | -------------------------------------- |
| BR-1  | الحركة تعلَّق على مشاركة موسم لا على موظف                     | FK                                     |
| BR-2  | موسم الرحلة = موسم مشاركة الحركة، و`role` يُنسخ من الرحلة      | trigger `journey_legs_agree`           |
| BR-3  | لا نسخ للسفر إلى الموسم التالي                                | بحكم البناء — لا كود                   |
| BR-4  | مصدر واحد للخطة                                               | `check leg_plan_has_one_source`        |
| BR-5  | إعادة الإسناد تُبقي القديم `rebooked` وتربط `replaces_leg_id`  | `assign_to_trip`                       |
| BR-6  | حالة الرحلة **لا** تكتب حالة الحركة                           | لا trigger — فعلٌ صريح                 |
| BR-7  | الموظف يرى مساره ويؤكّد حركاته بلا صلاحية                      | RLS + `confirm_leg`                    |
| BR-8  | حركة بلا رحلة تحتاج تأكيداً يدوياً وتقول ذلك                   | مشتقّ + نص                             |
| BR-9  | `travels = false` يُخرج من الثغرات كلّها                       | شرط في `travel_gaps`                   |
| BR-10 | حركة حيّة واحدة لكل (مشاركة، دور) — للطرفين فقط                | فهرس فريد جزئي `one_live_leg_per_role` |
| BR-11 | حذف رحلة عليها ركّاب مرفوض                                    | `on delete restrict`                   |
| BR-12 | الغياب لا يكون أحمر — الأحمر لـ`missed`/`cancelled` وحدهما     | قاعدة عرض                              |
| BR-13 | شكل الحركة (الدور ⟷ الوسيلة ⟷ النقطتان)                       | `travel_shape_is_sane` (0133)          |
| BR-14 | تأكيدٌ بلا مؤكِّد مرفوض                                        | `check leg_confirmation_is_attributed` |

### 32.15 سجل الأحداث

`trips` و`journey_legs` موصولتان بـ`audit_row` منذ يوم إنشائهما (§23)، ويهمّ هنا
أكثر من غيره: نصف سؤال «ماذا كان مخططاً وماذا تغيّر» — من نقل هذا الرجل عن تلك
الرحلة ومتى — لا يُجاب إلّا من السجل. و`audit_record_label` تسمّي الرحلة برقمها
ومسارها، والحركة باسم صاحبها ثم رحلته أو «ترتيب ذاتي».

---

## ملحق أ — خريطة الاستبدال السريعة

| Supabase الحالي                                                                            | البديل في هذا المستند |
| ------------------------------------------------------------------------------------------ | --------------------- |
| `auth.signUp / signInWithPassword / signInWithIdToken / setSession / signOut / updateUser` | §2                    |
| RPC `my_permissions`                                                                       | §3.2                  |
| RPC `ensure_current_season` / `set_current_season`                                         | §7.1 / §7.4           |
| RPC `assignable_employees` (حارسه يقبل `tasks.assign` منذ 0106)                            | §9.10                 |
| RPC `submit_module_report`                                                                 | §12.3                 |
| RPC `my_module_rating`                                                                     | §13.4                 |
| RPC `copy_reference_items` / `copy_module_sectors`                                         | §14.2 / §14.3         |
| RPC `broadcast_to_module` / `broadcast_to_all`                                             | §16.6 / §16.7         |
| RPC `dashboard_seasons` / `dashboard_stats`                                                | §18                   |
| خمسة أقسام في `dashboard_stats`: البلاغات، الحضور، المهام، التقييمات، الشكاوى (0123)        | §18.3                 |
| RPC `audit_events` / `audit_actors` / `audit_summary` / `log_auth_event`                   | §23                   |
| trigger `audit_row` على `place_check_ins` / `report_misses` / `module_type_teams` (0122)   | §23                   |
| RPC `complaints_list` / `complaints_against_me` / `complaint_thread` / `complaints_against` | §24.3 → §24.6         |
| RPC `complaint_targets`                                                                    | §24.6b                |
| RPC `file_complaint` / `reply_to_complaint` / `set_complaint_lock` / `set_complaint_dismissed` | §24.7              |
| جدول `module_tasks` (وصفي منذ 0105)                                                        | §25.1                 |
| جدولا `personal_tasks` / `personal_task_attachments` + RPC `set_personal_task_state` + دلو `tasks` (0105) | §25.2      |
| جداول `personal_task_comments` / `_events` / `_steps` / `_batches` (0117–0118)             | §25.2 و §25.5         |
| RPC `my_personal_tasks` / `assigned_personal_tasks` / `personal_task_detail` / `personal_task_thread` | §25.2      |
| RPC `create_personal_tasks` / `add_personal_task_comment` / `set_personal_task_steps` / `reassign_personal_task` | §25.2 |
| RPC `my_task_batches` / `task_batch_tasks` / `my_task_stats`                               | §25.2                 |
| الدالّتان الحاكمتان `personal_task_transition_allowed` / `personal_task_actions`            | §25.5                 |
| الكنس اليومي `escalate_late_tasks` (0119)                                                  | §25.2 و §29.1         |
| RPC `evaluation_forms_list` / `evaluation_form` / `evaluations_list` / `evaluation_sheet`  | §26.7                 |
| RPC `evaluations_about_me` / `evaluations_about` / `evaluation_targets`                   | §26.7                 |
| اختيار المُقيِّم ⇐ `assignable_employees`                                                  | §9.10 و §26.7         |
| اختيار المُسنَد إليهم ⇐ `assignable_employees` (متعدّد، والمُسنِد مُسقَط)                    | §9.10 و §25.2         |
| RPC `save_evaluation_form` / `assign_evaluations` / `save_evaluation` / `reopen_evaluation` | §26.8                |
| جداول `evaluation_templates` → `evaluation_answers` (ستة)                                 | §26.5                 |
| Edge `admin-create-user` / `admin-delete-user` / `admin-set-password`                      | §5.1 / §5.6 / §5.7    |
| Edge `admin-set-email` (لموظف / للنفس)                                                     | §5.7b / §2.7          |
| Edge `send-notification`                                                                   | §17.3 (داخلي)         |
| Storage (6 buckets + createSignedUrl + upload + remove)                                    | §19 و §24.8           |
| Realtime (stream notifications)                                                            | §20                   |
| RLS + Triggers (0007→0105)                                                                 | §21 كاملاً و §24.9 و §25.1/§25.2 و §26.9/§26.10 |
| طابور الإرسال (لا endpoint — التزامات إعادة التنفيذ)                                       | §27                   |
| تصدير البيانات (لا endpoint — يقرأ عبر ما سبق)                                             | §28                   |
| pg_cron `escalate-missing-reports` + جدول `report_misses` (0086)                           | §29                   |
| جداول `incidents` / `incident_attachments` + دلو سابع (0088)                               | §30                   |
| RPC `delete_incident` / `delete_all_incidents` + `incidents.delete` + سياسة الحذف (0121)   | §30.7                 |
| RPC `season_map` (0090→0093→0112) + `is_place` (0089/0098) + أشكال الروابط (0091/0092)      | §31                   |
| RPC `check_in_at_place` / `presence_board` + جدولا `place_codes` و`place_check_ins` (0098) | §30                   |
| RPC `employee_journey` / `season_trips` / `trip_passengers` / `travel_gaps`               | §32.6 → §32.11        |
| RPC `assign_profiles_to_trip` / `assign_to_trip` / `unassign_leg` (0130/0132)             | §32.9                 |
| RPC `confirm_leg` / `record_self_leg` / `set_participant_travels`                          | §32.10                |
| RPC `my_participation` / `participation_of` / `participants_without_leg`                   | §32.6 و §32.9         |
| جداول `trips` / `journey_legs` / `trip_attachments` / `leg_attachments` + دلو ثامن (0129)  | §32.1 و §32.12        |
| قائمة `travel_points` + `travel_shape_is_sane` + `is_internal_travel_point` (0133)         | §32.5                 |
| pg_cron `remind-upcoming-returns` / `report-travel-gaps` + `travel_reminders` (0131)       | §32.13                |

## ملحق ب — ما لا يحتاج backend (يبقى في التطبيق)

- الحسابات المحفوظة وتبديلها: التخزين محلي (keystore) — يحتاج فقط §2.4.
- اشتراكات FCM topics: التطبيق يشترك/يلغي بنفسه عبر Firebase SDK؛ الخادم يحتاج
  فقط الالتزام بأسماء الـ topics (§17.3) وبمسار §9.11/`GET /profiles/{id}/assignments`
  لمعرفة ملفات المستخدم (يستخدم التطبيق حالياً استعلامي عضوية خفيفين — يغطيهما
  §11.1 و §11.2 أو endpoint خفيف: `GET /me/module-ids` ⇒ `{ "module_ids": [...] }`
  **يُنصح بإضافته**).
- إعادة حساب الأعمدة المحسوبة في محرر التقارير (تُخزَّن كقيم جاهزة).
- **طابور الإرسال** (`lib/core/offline/`): ملف JSON ونسخ المرفقات على الجهاز، وكشف
  عودة الشبكة عبر `connectivity_plus`. لا جدول ولا endpoint — لكن للخادم التزامات
  تجاهه، وهي في §27.
- **الأجوبة المحفوظة** (`lib/core/offline/snapshots.dart`): النصف الثاني من العمل
  بلا شبكة. الطابور يحفظ ما **كُتب**؛ وهذا يحفظ آخر جواب صحيح لقراءاتٍ معدودة
  (مهامي، ودفتر هواتف الكادر) فيُعرض حين تسقط الشبكة. **لا التزام على الخادم
  إطلاقاً** — يخزَّن **الصفّ الخام** كما أرسله الخادم ويُقرأ بنفس المُحلِّل، فما
  يتغيّر بتغيّر الاستعلام وحده ولا مُحلِّل ثانٍ ينحرف عن الأول. وثلاث قواعد تُقال
  هنا لأنها تصف تعاقداً مع الخادم ولو لم تُلزمه بشيء:
  1. **لا يُعرض المحفوظ إلا على فشل شبكة** — لا على `403` ولا `500` ولا صلاحية
     سُحبت صباحاً. تلك **أجوبة**، وعرضُ كشف الأمس لمن أُخرج من ملف عند الفجر ليس
     صموداً بل مخاصمةً للخادم على ما هو صحيح.
  2. **المفاتيح تحمل هوية المستخدم، وتُمسح عند الخروج.** الهاتف في غرفة العمليات
     مشترك، وقراءة الرجل التالي لدليل من قبله ليست شاشة قديمة بل **تسريب**.
  3. **الشاشة تقول متى**، لا «نسخة محفوظة» فحسب — الوقت هو المُدخَل الوحيد للقرار
     الوحيد المتاح لمن يقف عند بوابة: أأعمل بهذا أم أسأل أحداً؟
- **إنذار البلاغ العاجل** (`IncidentAlarm` + `AlarmSound.kt`): النافذة والصوت
  اللذان يوقفان التطبيق حين يصل بلاغ. **لا جدول ولا endpoint ولا صلاحية** —
  مصدراه تدفّق §20 ودفعة §17، وكلاهما قائم؛ والنغمة نغمة المنبّه الافتراضية
  للجهاز تُشغَّل عبر MethodChannel مكتوبة باليد (لا حزمة صوت: التطبيق يحتاج صوتاً
  واحداً بطريقة واحدة). ما يلزم الخادمَ منه سطرٌ واحد: **أن يبقى `data` على
  الإشعار حاملاً `type` و`incident_id`**. §30.8.
- **تقارير الأعطال** (Crashlytics): تذهب إلى Firebase مباشرة، لا إلى هذا الـ API.
- **تصدير البيانات** (`lib/features/export/`): يقرأ عبر الاستدعاءات الموثّقة ويبني
  الملف على الجهاز. لا endpoint — لكن التزام السرّية عليه في §28.2.
- **الأسطح المعتمة** (`settings.solidSurfaces`): مفتاحٌ لكل جهاز يُخرج الزجاج من
  نظام الواجهة — ألواح معتمة، بلا ضبابية، بحدود أثقل، وخلفية مسطّحة. **لا علاقة
  للخادم به إطلاقاً**، ولا يُرفع معه شيء ولا يُحفظ في حساب. يُذكر هنا لأنه إجابة
  عن شرطين ميدانيين يخصّان هذا النظام: شمس ذي الحجة — اللوح الشفّاف ينفق تبايناً
  فقدته العين أصلاً في الوهج — والأجهزة المتواضعة، إذ كل ضبابية **طبقةُ حفظ**
  يُعيد المُركِّب قراءة كل ما تحتها في كل إطار.
- مواقيت الصلاة: تُحسب على الجهاز بالكامل (`adhan_dart`) من إحداثيات يقرأها
  `geolocator` وتُخزَّن في `shared_preferences`، مع سقوط إلى مكة عند تعذّر
  الموقع. لا endpoint لها ولا جدول — الخادم لا يعلم بها.
